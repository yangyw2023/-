# 船载离线文档问答系统 —— 实施方案

**版本**:v0.13 · **日期**:2026-09-22 · **作者**:Arya (Yang Yawen) · **所属**:Kuok / AI COE
**方法论**:essence-first-engineering(《人月神话》工程法),任务分档 = C 档(系统级)

---

## 0. 怎么读这份文档

### 0.1 本文档负责什么、不负责什么

**本项目有五份文件,各管一段。混在一起是上一版最大的问题,本版按职责重切。**

| 文件 | 回答什么问题 | 类别 |
|---|---|---|
| **实施方案(本文件)** | **为什么这么做、要做成什么样、怎么判断做对了** | 规划类 |
| `执行手册.md` | **按什么顺序做、每步敲什么命令、怎么验证这一步过了** | 操作类 |
| `DECISIONS.md` | 每个决定是什么时候、基于什么数据做的 | 日志类(只追加) |
| `core/contracts.py` | 组件之间怎么对话(数据结构、阈值、接口的**代码**) | 契约类 |
| `ClaudeCode_任务序列.md` | 机械任务怎么交给实现工具 | 操作类 |
| `DOCUMENT_MAP.md` | 每个文件干什么、改动走什么流程 | 索引类 |

**本文件里没有的东西**(去对应文件找,不要在这里找):

- ❌ 安装命令、服务启停、健康检查 → `执行手册.md`
- ❌ 分步执行顺序、每步的验收动作 → `执行手册.md`
- ❌ 数据结构与阈值的**代码** → `core/contracts.py`(本文件只讲**理据**)
- ❌ 文件逐个说明、Git 规则、维护流程 → `DOCUMENT_MAP.md`
- ❌ 给实现工具的 prompt → `ClaudeCode_任务序列.md`
- ❌ 历次决定的原始记录 → `DECISIONS.md`

### 0.2 自包含承诺

**本版可以单独阅读,不需要查任何历史版本。** 所有术语在 §0.3 定义,所有决定写明原因。
历史版本只在 §6"迭代记录"里作为**教训来源**出现,不作为理解本文的前提。

> **注意**:"自包含"指**不需要查历史版本**,不是"不需要查任何别的文档"。
> 跨文档引用是对的,跨版本引用才是错的。

### 0.3 术语表

| 术语 | 含义 | 为什么重要 |
|---|---|---|
| **RAG** | 检索增强生成。提问时先从文档库检索相关片段,连同问题一起交给模型 | 本系统主架构。让答案**有出处、可核对**,文档更新只需重建索引 |
| **微调 / LoRA** | 用特定数据继续训练模型,改变权重。LoRA 只训练少量新增参数(几十 MB) | Faraz 认为微调能"让模型掌握公司文档"。本项目用实验检验(§13) |
| **chunk** | 文档切分后的最小可检索单元 | 检索的基本粒度,直接决定检索质量上限 |
| **BM25 / 向量检索 / 混合检索** | 关键词检索 / 语义检索 / 两者并行召回后融合 | 手册里全是设备编号、条款号,BM25 比向量准;自然语言描述反之。**都需要** |
| **rerank** | 用更精细的模型对召回结果重新排序 | **是否引入尚未决定**,见 §11.3 |
| **prefill** | 模型"读完"输入提示词的阶段,发生在生成第一个字之前 | **本项目最大的性能瓶颈**(§10) |
| **TTFT** | 首字延迟。用户实际感受到的等待时间 | **必须测它,不能看 tok/s** —— 后者在 RAG 场景严重高估体验 |
| **GGUF / llama.cpp** | 船端唯一可用的模型格式与推理引擎 | 模型能否被 llama.cpp 加载是一票否决项(§12.1) |
| **MLX** | Apple 芯片专用框架。Mac 上最快,**不能跨平台** | 上不了船,M6 必须迁移 |
| **量化 / Q4_K_M** | 权重从 16 位压到 4 位,体积与内存降到约 1/4 | 4B 模型能在 3GB 内存跑起来的前提 |
| **MoE** | 混合专家。总参数大但每次只激活一小部分 | 内存按**总参数**算,不是激活参数。选型易被骗 |
| **2×2 因子实验** | 同时改变两个变量(RAG 开/关 × 微调 开/关),得 4 种组合 | §13 的核心设计。能同时答"各自多大用"和"能否叠加" |
| **GATE(门)** | 候选模型必须依次通过的筛选条件,任一不过即淘汰 | 让"废掉"发生得**早且便宜**(§12.1) |
| **预注册** | 实验开始**之前**写下"想推翻什么假设、什么数字算推翻" | 防 HARKing。Faraz 有先验立场,事后定标准会被动机性推理污染 |
| **HARKing** | 看到结果后倒推假设 | 最常见的自欺形式 |
| **不一致对** | 配对比较中两个方案结果不同的题(b = A对B错,c = A错B对) | **McNemar 只看 b 和 c,不看净差**(§13.2) |
| **实用效应闸** | 预注册的"多大差异才值得改变决策"的阈值 | 与统计显著性是**两件事**,两者都过才接受 |
| **盲评** | 判分时隐去答案来自哪个实验臂 | 判分者=方案设计者时的必需措施(§13.4) |
| **judge** | 自动判定答案对错的模型 | 24/31 题依赖判分裁量,**judge 影响可能大过被测模型** |
| **L1/L2/L3/[M]** | 架构决策 / 实验参数 / 实现细节 / 方法约束 | 决策分级。详见 `DOCUMENT_MAP.md` |
| **象限①/④** | ①=我机器上能跑的程序;④=能交付给别人用的产品。工作量差约 8 倍 | 汇报时必须说清交付的是哪个 |
| **ISM Code / DPA** | 国际安全管理规则 / 岸上指定人员 | 语料(KAIVA 手册)就是这套体系的落地文件 |

### 0.4 目录

```
1.  情景 Situation
2.  任务 Task
3.  限制条件 Constraints（A 物理 / B 组织 / C 认知）
4.  我具备的条件 Resources
5.  行动 Actions —— 七个决定，每个追溯到约束
6.  迭代记录 —— 已犯的错误与五条纪律
7.  项目骨架：三条轴
8.  里程碑 M0–M7
9.  语料现状
10. 性能约束与资源预算
11. 关键参数的语义决定
12. 模型选型
13. 实验设计（M2）
14. 评测集设计与边界
15. 技术栈决策
16. 显式失败面
17. 风险登记
18. 分工原则
```

---

## 1. 情景 Situation

Arya 是 Kuok 集团 AI COE 的实习生。经理 Faraz 口头交代:让远洋船舶上的船员能在**没有网络**的情况下,用自然语言从公司文档中查到答案。

四个特征决定了整个项目该怎么做:

**① 他描述的是一个解法,不是一个问题。**
Faraz 说的是"用公司文档微调小模型"——这是**方案**。真正的**问题**是"船员在没网的船上怎么查到手册里的答案"。
→ 第一步是把解法还原回问题。否则会花三个月精确地做错一件事。

**② 他同时是甲方、资源方、和需要被说服的人。**
文档、船端硬件规格、要不要投微调,都在他手上。
→ 每个技术判断都要配一个问题:**这个结论怎么让他接受?**

**③ 环境是安全敏感的。**
语料是 ISM 体系手册,内容包括应急响应程序、事故报告时限、船长交接。答错不是"体验不好",是事故。
**而且船上没有 IT 支持——系统挂了没人修。**

**④ 语料是临时的。**
Faraz 原话:"this is only for testing… future can be totally different set of files and number"。
→ 现在做的一切都不能假设当前 9 份手册的版式会一直成立。

---

## 2. 任务 Task

必须分清的**双层任务**:

| | 表层任务(最终目标) | 当前阶段的真实任务 |
|---|---|---|
| 目标 | 建一个船上能用的离线问答系统 | **产出一张能让 Faraz 决策的表** |
| 交付物 | 一套软件 | 一组数字 |
| 时间尺度 | 几个月 | 两到三周 |
| 成功标准 | 船员愿意用 | 微调 vs RAG 的差异是可测量的 |

**现在真实任务压倒表层任务。** 在"要不要投微调"定下来之前,系统形态是不确定的。
**先消除不确定性,再建系统。**

这直接解释了 §8 里程碑为什么把 M2(对照实验)排在 M4(自建代码库)之前。反过来做就是在猜测上盖楼。

---

## 3. 限制条件 Constraints

约束必须**分三类**,因为它们各自约束不同的东西。混在一起,决策的因果链就断了。

### 3.1 A 类:物理约束 —— 决定架构

| # | 约束 |
|---|---|
| A1 | 完全离线,不能调任何 API |
| A2 | 船端模型 ≤5B 参数,越小越好 |
| A3 | 船端大概率是 CPU 推理,无独立 GPU |
| A4 | 船上无 IT 支持,坏了没人修 |
| A5 | 公司文档不能离开本机 |

**这五条推出架构决定:岸-船分离。**

```
岸端（Mac Studio，有网，算力充足）          船端（离线，CPU，无人维护）
──────────────────────────────────         ───────────────────────────
文档解析 / 分块 / 向量化 / 建索引
微调训练 / 合成数据生成                  →  只做：检索 → 生成 → 展示出处
打包成"索引包"（可校验）                     收到索引包直接用
```

*为什么*:A3+A4 意味着船端每多一个组件就多一个故障点。把重活挪到岸上,船端复杂度降一个数量级。
A5 意味着微调与数据生成也必须本机做,不能用云端服务。

### 3.2 B 类:组织约束 —— 决定顺序

| # | 约束 | 影响 |
|---|---|---|
| B1 | Faraz 相信微调能解决问题,且要证据 | **微调必须先行取证**,不能排到最后 |
| B2 | 文档由他提供,给多少给什么由他定 | 语料规模不可控,架构须容忍变化 |
| B3 | 船端硬件规格未知 | 性能预算按保守值定,不按 Mac 实测定 |
| B4 | 许可证门:原定仅接受 Apache 2.0 / MIT | 模型选型的硬筛子(§12.2) |
| B5 | **Arya 独立完成,无协作方,无上升对象** | 凡"找另一个人复核"的环节一律替换为**单人可执行协议**(§14.6) |

**B 类不改变架构,但决定先做什么。**
技术上 RAG 该先做,但组织上必须先让 Faraz 看到微调的数字——这是 §13 实验设计的直接来源。

### 3.3 C 类:认知约束 —— 决定什么该推迟

| # | 结构性未知 |
|---|---|
| C1 | 不知道船员真正会问什么 |
| C2 | 不知道未来真实语料长什么样 |
| C3 | 不知道船上具体是什么机器 |

**这三条不是"待解决问题",是结构性未知。**
处理方式**不是去猜**,而是让系统对它们不敏感:模型可换、解析器可换、预算可调。

> 这就是"组件化 + 契约先行"的真实理由——不是为了代码优雅,
> 而是因为**你注定会猜错,得让猜错的代价小**。

---

## 4. 我具备的条件 Resources

### 4.1 硬件

| 项 | 规格 |
|---|---|
| 机型 | Mac Studio(公司配发) |
| 芯片 | Apple M3 Ultra |
| 内存 | **96 GB 统一内存** |
| 系统 | macOS 15.6 |
| 存储 | 894 GB 可用 / 994.66 GB |
| 权限 | 开发者级;公司 MDM + 代理环境 |

**96 GB 统一内存是最大的一张牌**:让 70B–120B 模型能在本机 4-bit 运行,
意味着可以用顶级模型当 teacher 生成合成数据,而**公司文档一个字都不出机器**(直接解掉 A5)。

**同时也是最大的陷阱**:这台机器和船上那台未知规格的 CPU 机器是两个世界。
**Mac 上测出来的一切都是乐观值。**

### 4.2 已完成的环境(M0)

Ollama / MLX-LM(GPU 可用)/ Kiln(断网可用)/ llama.cpp / `qwen3:32b` teacher / poppler。
**具体版本、安装步骤、健康检查见 `执行手册.md`。**

### 4.3 语料

9 份 KAIVA 手册 PDF，共 1335 页，全部为英文。

**S5 baseline（Phase A 全部测量的基准）**：

    corpus sha256 = 8c6bee0aa952b387e20a2de76253d4384ffe859a8050e6fe8fee9166c956aeaa
    chunks        = 3383
    parser/builder provenance commit = 545947a

该 baseline **可复现**：用入库代码重建，sha 与工作区逐字节一致。

**⚠️ 它是 S5 baseline 与 Phase A measurement baseline，不是 Phase B 后的最终 corpus。**
Phase B ingestion remediation 会改变 chunk 数量、长度、per-page ordinal 与 corpus sha。


### 4.4 人

**只有 Arya 一人。** 无协作同事,无可上升对象。
这不是资源不足,而是**必须写进方法论的约束**(见 B5 与 §14.6)。

---

## 5. 行动 Actions

七个决定。**每一个都能追溯到 §3 的某条约束**,没有一个是"因为最佳实践"。

### ① 立一句话心智模型

> **这个系统是一个离线的、带出处的文档导航器。**
> 它把船员的问题变成"文档里的哪一段";
> **生成的自然语言只是那段依据的摘要,不是答案本身。**

*应对*:C 类
*为什么*:这个系统有 6–8 个组件、几十个决定。需要一个东西保证它们是**同一个系统**而不是拼贴。

| 推论 | 落地约束 |
|---|---|
| 依据链是本体,模型是零件 | 模型层可插拔;出处元数据不可妥协 |
| 没有依据就不输出答案 | 检索不到 → **拒答**,绝不喂空上下文让模型硬编 |
| 模型死了系统还能用 | 生成层挂掉 → 降级为**纯检索模式** |
| 原文优先于摘要 | 原文与答案同屏,一键跳转原始 PDF 页 |

> **每加一个功能,先拿这句话卡一下:它服务于"导航到依据"吗?**

### ② 把争论改成实验

*应对*:B1
Faraz 要的是证据不是同意。而且**我也可能错**——微调万一在这个语料上真的有效呢?
实验既尊重他的判断,又保护了项目。设计见 §13。

### ③ 岸-船分离

*应对*:A1–A5。详见 §3.1。

### ④ 契约先行 + 组件可换

*应对*:C1–C3
不知道选哪个模型 → 让模型可换;不知道未来语料格式 → 让解析器可换。
**契约(`core/contracts.py`)是让"换"变便宜的机制。** 理据见 §11,代码见契约文件。

### ⑤ 先测量,再定预算

*应对*:A3 + B3
所有人(包括我)都假设**生成速度**是瓶颈。实测发现是 **prefill**——
4096 token 上下文的首字延迟达 **57 秒**。这条如果靠猜,会一路错到船上。数据见 §10。

### ⑥ 设"门"(GATE),把致命否决项提前

*应对*:A2 + A3 + B4
`qwen3.5:4b` 在 Ollama 上跑得好好的,但 **llama.cpp 载不进**——而船端只能用 llama.cpp。
拖到最后才发现,前面所有实验的主角都上不了船。

> **门的价值是让"废掉"发生得早且便宜。** 详见 §12.1。

### ⑦ 建索引之前先勘察语料

*应对*:C2
真去看了才发现 KAIVA 手册的印刷页码**按章节独立编号**,"Page 1" 在一份手册里出现很多次。
这意味着 `(文件名, 页码)` 二元组**根本不能唯一定位**。
若未在勘察阶段发现,会一路做到船员核对出处时才爆——那时整个索引都要重建。

---

## 6. 迭代记录:已犯的错误与纪律

**这一节比任何技术选型都重要。** 它记录的是方法论层面的错误,而这类错误会重复发生。

### 6.1 版本简史

| 版本 | 主要内容 | 暴露的问题 |
|---|---|---|
| v0.1 | 完整前置规划 | 瀑布式,把 M6 才需要的信息设成 M0 阻塞项 |
| v0.2–v0.3 | 改迭代式,加契约与失败面 | 假设上下文能塞 4000 token,未验证 |
| v0.4 | 微调优先取证、Mac+Kiln | 假设"Ollama 能跑 = 能上船",未验证 |
| v0.5 | 真实语料、引用三元组 | 无 S/T/C 层;约束未分类;先写代码后补文档 |
| v0.6 | 自包含重写;模型池扩到 10 个基座家族 | 评测集建成后暴露三个可执行性缺口 |
| v0.7 | 预注册、决策分级、统计功效 | McNemar 判定规则写错;判分函数未定义 |
| v0.8 | 两道闸、判分协议、盲评 | 上下文预算与 TTFT 预算互相矛盾 |
| v0.9 | 修正预算矛盾、temperature、混杂披露 | `pack_context` 语义歧义;M1c 引入向量库 |
| v0.10 | prefix 语义、`[M]` 类别、去向量库 | **把执行手册与 DOCUMENT_MAP 的内容写进了本文件** |
| **v0.11** | **按职责重切文档边界** | — |

### 6.2 已犯的错误(按类型归纳)

| # | 错误 | 类型 |
|---|---|---|
| 1 | 瀑布式计划,前置一堆闸门 | 方法错 |
| 2 | 接受"微调能灌知识"的前提 | 他人的认知错 |
| 3 | 假设上下文能塞 4000 token | **未验证的假设** |
| 4 | 假设"Ollama 能跑 = 能上船" | **未验证的假设** |
| 5 | 假设 (文件名, 页码) 能定位 | **未看真实数据** |
| 6 | 决定做了没写进 DECISIONS(三次) | **流程错** |
| 7 | 先写代码后补文档,产物与上一版断连 | **顺序错** |
| 8 | 想修改许可证门去迁就已有选择 | **顺序错** |
| 9 | 评测集建完才发现 `gold_chunk_ids` 全空 | **交付物定义不完整** |
| 10 | 39 题写完才算样本量,只能检测大效应 | **没做功效分析** |
| 11 | `is_trap`/`type`/`expected` 三份编码同一事实 | 违反自己的单点定义原则 |
| 12 | 从"只能塞 3 个 chunk"推出"rerank 必需" | **超前推断** |
| 13 | 用"净差 ≥6 题"当 McNemar 判定规则 | **统计学误用** |
| 14 | 预注册锁了阈值却没锁判分函数 | **产物与消费者的接口未定义** |
| 15 | `gold_chunk_ids` 放在评测集里 | 同上,分类错误 |
| 16 | TR01 是拒答题却带 citation | 同 #11 |
| 17 | "1500 ÷ 292 ≈ 3"算术不成立(应得 5.1) | **算术错误** |
| 18 | 上下文预算与 TTFT 预算互相矛盾 | **两个数字从未交叉核对** |
| 19 | `CHARS_PER_TOKEN_EST` 的免责注释在改动后失效 | 同上 |
| 20 | M2 从未规定 temperature | 随机性未受控 |
| 21 | `pack_context` docstring 自相矛盾 | 语义歧义 |
| 22 | M1c 引入向量库与"少一个依赖"自相矛盾 | 内部不一致 |
| 23 | **实施方案里写了执行手册与 DOCUMENT_MAP 的内容** | **职责边界错** |
| 9 | **把分发渠道失败当成模型失败** | `granite4:tiny` 与 `smollm3` 的 `ollama pull` 返回 `manifest missing`，被记为 SKIP。后续查明两者均存在直接 GGUF 路径 —— 缺的是 Ollama 的一个 tag，不是模型 | 台账中 acquisition status 与 gate status 分为两套字段，永不混用（§12.5） |
| 10 | **把带作用域的测量结论当成永久判决** | GATE-2 的加载失败只对"这一份 artifact + 这一版 runtime"成立。旧版 llama.cpp、量化产物 bug、GGUF 元数据 bug、CLI 参数不适配都会导致失败，与模型架构无关 | 每条 GATE-2 结果必须记 runtime 版本与 artifact 哈希；runtime 升级后此前 FAIL 的候选需重测（§12.1） |
| 11 | **把品牌名当成基座家族** | §12.3 曾把 `DeepSeek-R1-Distill-Qwen-1.5B` 列为第 9 个基座家族。它是 Qwen2.5 的架构与词表被蒸馏，提供不了横评追求的归纳偏置多样性 | 家族按 `family_key` 判定并去重，不按发布方；蒸馏/微调自某基座的模型继承其基座（§12.6） |
| 12 | **分节替换只做了加法** | v0.12 的 §12.3 应用后，两段式新表加上了，带裸绿勾的旧表没删，两张表并存且互相矛盾。§10.3 的 945 与 §10.2 的 765 同理 | 分节替换的变更清单里，凡标「整节替换」的，**必须写出被替换掉的旧内容的起止锚点**；应用后逐条核对旧锚点是否已消失 |
| 13 | **把"不部署"当成"不受许可约束"** | §12.4 的标题「不上船,不受 5B 限制」顺带免除了 GATE-3,teacher 表因此全是裸的 🟢/🟡 且无许可门。但 teacher 的输出 → 合成数据 → LoRA adapter → **上船**,许可顺着产出传递 | 判据改为"**它的产出会不会上船**"而非"它上不上船";辅助模型一律先问这一句(§12.8) |
| 14 | **把"一个说得通的机制"当成结论** | 三次实例：① 由 `BLAS,MTL` banner 断言"`-ngl 0` 未生效"；② 由 `think=True` 返回 HTTP 400 断言"不可能泄露推理"（反证就在同批 Qwen 实验里）；③ 由缺失元数据字段断言"极可能是 Ollama 转换未写入" | **每写一句因果性陈述，问一句：「有哪个实验能区分这个解释和它的替代解释？」没有 → 降级为假设，并把那个能区分的实验记成待办。** 三次里有两次事后补了该实验（GPU 正对照、thinking 对照），结论都变了 —— 说明这条检查有效，只是事前没做 |
| **E14** | **把"一个说得通的机制"当成结论** | 已记录 **9 次**实例。九次**全部**发生在「把观察写成一个字段值、一条判据或一句因果陈述」的那一刻；测量与分析环节均未出错 | **落笔检查**：每写 `field: value` 或"所以应该改 X"时问一句——**我观察到的那个量，和我要填的这个字段，是同一个东西吗？** 九次里九次答案都是"不是" |


### 6.3 从错误中提炼的六条纪律

> **纪律一(源自 #3 #4 #5):任何进入契约的数字,必须有一次实测或一次真实数据勘察作为依据。**

> **纪律二(源自 #6 #7 #8):流程要写死在文档里,不能靠自觉。**
> 这三个错误都是"知道该怎么做但没做",解法不是提醒自己,是把流程变成文件。

> **纪律三(源自 #9 #10 #14 #15 #16):任何产物开工之前,必须先写下
> "它要支撑哪个判断"以及"消费它的代码怎么读它、读出来会不会被污染"。**

> **纪律四(源自 #12 #13):任何写进契约或架构的推断,必须能指出它是从哪个实测数字
> 推出来的;推不出来就降级成"待实验决定"。**
> 这类错误比事实错误更难发现,因为它读起来很有道理。

> **纪律五(源自 #17 #18 #19):契约里任何两个能互相推导的数字,必须显式核算一次,
> 并把核算结果写在旁边。**
> 每个数字单看都有依据,放在一起才暴露。所以核对不能只做"这个数从哪来",
> 还要做"这个数和旁边那个数一致吗"。

> **纪律六(源自 #23,本版新增):每份文件只写自己职责内的东西。**
> "自包含"指不需要查**历史版本**,不是不需要查**别的文件**。
> 跨文档引用是对的,跨版本引用才是错的。把执行步骤写进方案,
> 结果是两处都要维护、两处都会过期。

---

## 7. 项目骨架:三条轴

**如果觉得这个项目乱,根本原因是三件性质不同的事被混在一条时间线上讲。**

```
━━━ 证据轴（当前跑在最前面）━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  评测集 → 检索基线 → 2×2 对照实验 → 模型横评 → 一张给 Faraz 的表
  目的：消除不确定性         节奏：两到三周，快，允许脏
  产物：数字，不是软件

━━━ 产品轴（等证据轴出结果）━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  语料层 → 检索层 → 生成层 → 船端打包
  目的：建可交付的系统       节奏：几个月，慢，要求可维护
  产物：软件

━━━ 记录轴（全程并行，永不停）━━━━━━━━━━━━━━━━━━━━━━━━━━
  DECISIONS.md（只追加）/ contracts.py（改动走仪式）/ 台账（状态来自实测）
  目的：让三周后的自己能接上   节奏：每个决定当天
  产物：文档
```

**当前状态**:

| 轴 | 状态 |
|---|---|
| 证据轴 | 🔶 评测集 v5.2 已建成(39 题),**待收口三项**(§14.3) |
| 产品轴 | ✅ 语料层完成(3309 chunks);等契约冻结 |
| 记录轴 | ⚠️ 历史 185 行未随仓库迁移(见 `DECISIONS.md` 起始声明) |

**当前判断**:剩下的问题已经属于"**实验会回答的问题**",而不是"继续写文档能回答的问题"。

---

## 8. 里程碑 M0–M7

每个里程碑有**时间盒**(到点交付当前状态)和**只能答是/否的完成判据**。
**具体执行顺序与每步动作见 `执行手册.md`。**

| 编号 | 名称 | 这是什么 | 完成判据(是/否) | 象限 | 状态 |
|---|---|---|---|---|---|
| **M0** | 环境 + 裸基线 | 装好工具链,断网状态下能回答问题 | 断网可问答;TTFT 已记录 | ① | ✅ |
| **M1a** | 语料层 | PDF 手册 → 可检索的 chunk | 9/9 解析成功,section 元数据 100%,一键复现 | ① | ✅ |
| **M1b** | 评测集 | 建一把尺子,定义什么叫"答对了" | 见 §14.5 六条判据 | ① | 🔶 |
| **M1c** | 检索基线 | BM25 / 向量 / 混合三路跑通 | Recall@k 已测量;`MIN_RELEVANCE` 已校准;reranker 决策有数据 | ① | ⏳ |
| **M2** | **2×2 对照实验** ⭐ | **当前核心。** 四种配置:裸模型 / 只 RAG / 只微调 / RAG+微调 | 预注册已提交;四臂结果落盘;文档更新响应测试完成 | ① | ⏳ |
| M3 | 模型横评 | 在 M2 胜出配置下比较【通过全部四道门的全部候选】 |
     完成判据：漏斗各层数字与淘汰理由完整；家族数按 family_key 去重后如实报告 |
| **M4** | 自建代码库 | 把实验固化成可维护的组件化代码 | 换模型只改一行配置;能复现 M3 数字(±3%) | ① | ⏳ |
| **M5** | 迭代优化 | 调检索、prompt、微调,把指标推到可用线 | 幻觉率 <5% 且引用准确率 >90% | ①→② | ⏳ |
| **M6** | 船端落地 | Mac(MLX)→ 船端(llama.cpp + GGUF),工程加固 | 拔网线 + 断电重启后自恢复;§16 失败面各测一次 | ④ | ⏳ |
| **M7** | 单船试点 | 装到真船,收真实日志 | ≥100 条真实船员提问 + 归因分析 | ④ | ⏳ |

**关于象限**:M0–M5 交付的都是**象限①**(我机器上能跑的程序);**M6 才要求象限④**。
两者工作量差约 8 倍,差的部分全在 §16 失败面、§10 预算、测试与部署工程里。
**汇报时必须说清交付的是哪个象限**,否则 Faraz 会以为快能上船了。

---

## 9. 语料现状

| 手册 | doc_id | 页数 | chunks | 签发部门 |
|---|---|---|---|---|
| Fleet Maintenance Manual | FMM | 308 | 812 | STM |
| Quality Management Manual | QMM | 164 | 408 | DPA |
| Emergency Response Manual | ERM | 151 | 369 | DPA |
| Crew Management Manual | CMM | 133 | 361 | GM, Crewing |
| Environmental Management Manual | EMM | 173 | 359 | CCM |
| Safety Management Manual | SMM | 98 | 296 | DPA |
| Navigational Procedures Manual | NPM | 88 | 286 | DPA |
| Cyber Risk Management Manual | CRM | 100 | 239 | FLEET IT |
| Training & Competency Assurance | TACM | 120 | 179 | Training Dept |
| **合计** | | **1335** | **3309** | |

**这 1335 页不是"一艘船几千份文档"。** Faraz 明说这是测试集。
→ 架构须假设未来 10–100 倍规模,但**现在不为此优化**。

### 9.1 勘察发现的五件事

- **发现 1 · 文本层存在，但并非全部可用。**
  判断文本可用性的依据是 `pdffonts` 输出的 **`uni` 列**（字体是否有 ToUnicode 映射），
  **不是字体表是否非空**。
  实测：TACM p.38–103（66 页）为 Type 3 / Custom encoding / `uni=no`，
  文本层存在但抽出的是原始字符码，语义为空。

  其余 8 份手册**未观察到与 TACM p.38–103 同型的 Type 3 / `uni=no` failure**。

  > **⚠️ `uni=yes` 只支持「存在 Unicode mapping」这一判断。**
  > 它**不单独证明** page content complete、citation metadata correct，
  > 或整页 native extraction usable —— 那是三个独立的轴（见 §9.3）。


**② 必须用 `pdftotext -layout`** ⚠️ — 默认模式下项目符号会与正文分离,语义直接破坏。

**③ 页眉是结构化元数据,不是噪声** ⭐ — 8/9 份用统一表格
(`DOCUMENT ID / ISSUED BY / SECTION / REV. NO.` + 章节标题),第 9 份(EMM)用 `Chapter N – Title` 变体。
→ 必须**从正文剥掉**(否则污染每个 chunk)且**提取进元数据**(免费得到章节结构)。

**④ 印刷页码按章节独立编号 —— 阻断级** ⭐⭐
页脚是 `Page 1 of 7` / `Page 1 of 3`,**"Page 1" 在一份手册里出现很多次**。

> **单独的印刷页码不能唯一定位一段文字。**

**引用契约因此是三元组 `(doc_id, section, pdf_page)`**,`printed_page` 仅供船员核对、绝不用于定位。
[L1] `falsified_if`: 找到能唯一定位的更简形式。

**⑤ 语料高度适合做评测集** ✅ — 自动抽出 403 条含明确数值约束的片段,覆盖全部 9 份手册。

### 9.2 S5 baseline 的可复现性与已知限制

**可复现性**：parser 与 builder 已入库（`545947a`）；
用入库代码重建 corpus，sha256 与工作区逐字节一致。

**已知限制**（均为 residual risk，不阻塞 Phase A 结论）：

- 轴 1：TACM p.38–103 共 66 页 native 文本不可用
- 轴 2：16 个 confirmed page-content-incomplete 实例，跨 6 本手册；
  全语料 prevalence 未估计
- 轴 3：`title_line_fallback` 可把任意看起来像标题的字符串写进 `section`
- 轴 4：视觉内容存在不等于值得索引；判据属 policy 而非 detector 阈值

**⚠️ S5 baseline 的验收是 shape-based（数量、长度、确定性），
不构成 content-correctness PASS。**


### 9.3 解析层的四个正交轴

| 轴 | 问题 | 已确认证据 |
|---|---|---|
| 1 native body **quality** | 抽到的正文能不能读 | TACM p.38–103：Type 3 / `uni=no` |
| 2 page content **completeness** | 可读 native text 是否覆盖页面全部业务内容 | 16 个 confirmed examples，跨 6 本手册 |
| 3 citation **metadata** correctness | 出处字段是否语义正确 | 96 anomaly candidates，15 clearly wrong |
| 4 **content policy** | 页面有内容是否等于值得索引 | form/template 样本证明二者不同 |

**四轴互不蕴含。** 特别是：

    native_text_usable  ≠  page_content_complete
    body_quality        ≠  citation_metadata_correct
    visual_content      ≠  ocr_worthy

**不得重新合并成单一的 `page_good` / `needs_ocr` 字段。**

**OCR 是 failure remediation mechanism，不是这四个状态的同义词。**

### 9.4 Phase B：native extraction → selective OCR → citation-safe ingestion

Phase A / A.1 / A.2 / A.2b 的 measurement line 已关闭。
后续问题属于 architecture / policy，**不继续用无边界调查替代设计决策**。

#### 岸端数据流

    raw PDF（immutable）
      ↓
    native extraction + page signals
      ↓
    native body state
      ↓
    对 policy 选中的页面执行岸端 OCR
      ↓
    frozen digest-addressed OCR artifact
      ↓
    parser 读取 native + accepted OCR artifact
      ↓
    body gate  +  independent citation metadata gate
      ↓
    admit / quarantine / reject
      ↓
    chunks.jsonl  +  ingest/page_quality.jsonl

#### OCR 不上船

OCR 属于**岸端 ingestion remediation**。船端仍只消费：
frozen corpus · frozen index · model · provenance manifest。

**因此 OCR runtime 不增加船端依赖与故障面**（A4：船上每个依赖都是故障点）。

#### artifact freeze 是确定性的边界

**parser / build 不得隐式执行 OCR。**

OCR artifact identity 至少绑定：

- source PDF sha256 · pdf_page
- rasterizer + version · raster DPI
- OCR engine + version · language-pack identity/digest
- OCR params digest

accepted artifact 另记 `ocr_artifact_sha256`，且 **immutable**。

同 cache key 再次运行 OCR 得到不同 bytes：
**不得静默覆盖**；记 reproducibility event，重新走 acceptance。

    ⚠️ OCR execution determinism 与 corpus-build determinism 是两个问题。
    OCR execution 允许存在重复运行差异；
    但 frozen artifact → parser → corpus 必须 deterministic。

#### 五个状态轴必须分离

    native_body_status:              usable / unusable / insufficient / uncertain
    page_content_completeness_status: presumed_complete / visual_content_suspected / not_assessed
    ocr_body_status:                 not_attempted / usable / degraded / failed
    citation_metadata_status:        explicit_supported / uncertain / failed
    content_policy_status:           admit / hold_for_review / reject_noncontent

**`explicit_supported` 不表示**：
人工验证过 section 语义 · section 与目录 ground truth 一致 ·
citation 整体 verified · body usable 蕴含 metadata correct。

**它只表示**：存在明确结构标签，成功抽出一个满足机械完整性要求、
且 provenance 可追踪的 metadata candidate。

> **⚠️ 状态命名纪律**：枚举值中不出现 `verified`。
> 「成功抽到一个长得正常的字符串」不等于「语义位置正确」——
> 已知反例：某页 section 抽出 `"9"`，结构合法且与该文档真实章节号碰撞，语义仍然错误。
> 每个枚举值的 docstring 必须写明**它不表示什么**。

#### 禁止的合并

**CASE R · native body unusable**

    native_body_status = unusable
    + accepted OCR body
    → OCR 成为唯一 primary body

native 乱码**只进入 audit，不进入 chunk body**。
citation metadata **不因 OCR body usable 而自动通过**；
metadata candidate 独立进入 citation gate。

**CASE S · native body insufficient**

    native_body_status = insufficient
    + accepted OCR body
    → OCR 成为 primary body

native 与 OCR 的 metadata candidate **分别进入 citation gate**。
**不得冻结「metadata 一律取 native」或「metadata 一律取 OCR」。**

> 依据：`native body 干净` 与 `native metadata 正确` 是两件事。
> 预先规定 metadata 的来源通道，等于把刚拆开的两个轴重新绑回去。

最终 page audit 必须记录：

- selected body channel
- selected metadata channel
- native metadata candidate
- OCR metadata candidate
- citation gate result
- remediation action

**CASE A · native body usable but page completeness suspected**

Phase B **不自动 OCR augment existing body**。
只记录状态、证据与 future interface。

未来若实现 augmentation，使用**有 provenance 的 ordered `TextBlock`
或等价的结构化块**；**不得退化成字符串拼接**。

#### Phase B 不变量

> **同一 admitted page 的 primary body 在本阶段只能来自一个 channel。**

因此 native / OCR 的正文重复**在结构上不可能发生** ——
这比"禁止字符串拼接"这条禁令更强：它让那种拼接无从发生。


---

## 10. 性能约束与资源预算

### 10.1 prefill 实测（预算的依据）

**⚠️ 本表是【模型相关】的，不是物理常数。**

runtime scope：`llama.cpp 0.4.0 build 10809 commit 5266f24da` / `ggml 0.23.0` /
`ollama 0.33.3` / macOS 15.6 arm64 / bench args `-ngl 0 -t 8`

| 模型 | pp512 | pp1024 | pp1200 | TTFT=10s 的 prompt 上限 |
|---|---|---|---|---|
| llama3.2:3b | 117.05 | 107.17 | 101.87 | ≈1061（内插） |
| **phi4-mini（M2 主模型）** | 107.47 | 98.15 | 92.91 | **950（直接实测：950 PASS / 975 FAIL）** |
| qwen3:4b | 87.95 | 78.81 | 75.40 | ≈824（内插） |

**`cpu_only_verified: true`** —— 依据：同 artifact 三组对照，
`-ngl 99` 的 pp1024 为 `-ngl 0` 的 **23.3 倍**，证明该旋钮能区分 GPU 路径，
故 `-ngl 0` 与 `--device none` 的一致性构成"未 offload"的证据。

**⚠️ 边界**：本条证明的是**无 GPU offload**，**不是**这些数字适用于船端。
`-ngl 0` 路径仍含 BLAS（Apple Accelerate）；船端 x86 无 Accelerate、
无统一内存带宽。x86 CPU 估算为 35–85 t/s。船端外推仍然乐观。

**⚠️ prefill 速率可能非单调**（tile 对齐效应，多次独立运行均出现）。
TTFT 仍单调递增，交叉点良定义；这正是"直接实测优于细粒度插值"的理由。


### 10.2 ⚠️ 一处必须消除的矛盾(纪律五)

早期版本并排写着两个数,**它们互相推导的结果对不上**:

```
MAX_CONTEXT_TOKENS = 1500      TTFT_BUDGET_S = 10.0
```

在 1024→2048 之间线性插值:`rate(1500) = 100.4 t/s` → **TTFT = 14.9 s**,不是 10 s。
反推:要守住 10 秒,上下文只能到 **约 1050 token**(1050 / 107.0 = 9.81 s)。

**三个选项**:

| 方案 | 上下文 | TTFT | 代价 |
|---|---|---|---|
| A. 收紧上下文 | 1050 | 9.8 s ✅ | 中位 chunk 只能装 3 个,recall 可能受损 |
| B. 放宽延迟 | 1500 | 14.9 s | 船员等 15 秒;M6 真硬件只会更慢 |
| **C. 先按 A,让 M1c 测出代价** ✅ | 1050 | 9.8 s | 无 |

**推荐 C**:两个数都是 L2、owner 都是 M6,现在谁也不是终值。
现在要做的不是挑一个,而是**让它们自洽并把取舍量化**——
要求 M1c 在 ~1050 与 ~1500 两档预算下各报一次 Recall。
这样如果收紧代价很大,你手上有数据去争取更宽的延迟预算或更好的硬件。

> **记入 BACKLOG(M6 做)**:prompt cache。系统提示词固定不变,只有 context 块在变。
> llama.cpp 支持缓存固定前缀,能砍掉 prefill 的固定部分。
> **这是唯一能同时放宽两个预算的手段**,但现在不做——它属于工程优化,不属于取证。

#### 两档 Recall 的 k 值(契约 v0.2.0 后重算)
 
契约把 prompt 预算与 chunk 预算显式分开后,实际能装的 chunk 数比 v0.11 假设的少:
 
    单 chunk 估算 = 中位 1169 字符 ÷ 4 + 引用头 14 tok ≈ 306 tok
 
| prompt 预算 | chunk 预算 | 中位 chunk 能装 |
|---|---|---|
| 1050 | (1050−200)×0.90 = 765 | **2 个** |
| 1500 | (1500−200)×0.90 = 1170 | **3 个** |
 
**因此 §10.2 要求的两档 Recall 是 Recall@2 与 Recall@3,不是 @3 与 @5。**
M1c 同时报 Recall@1/@2/@3/@20 —— @1 与 @20 分别给出下界与召回上限。
 
**连带两条**:
 
1. **CD01 在 765 预算下可能结构性不可答**(gold 是 3 个 chunk 跨 2 份手册)。
   **这条必须写进 M2 预注册的"预期最可能结果"** ——
   事先预测了它是证据,事后解释它是借口。
2. 上下文从 3 个掉到 2 个不是缺陷,**是之前的预算把 prompt 开销白送了**。
   真实代价由 M1c 量化,不靠调预算把数字变好看。

### 10.3 预算状态

**⚠️ S4a.9 尚未 CLOSED。**
但这不表示 9c-final 从未执行 —— 它已在 S5 baseline
`8c6bee0a… / 3383 chunks` 上完成一次。
Phase B 会改变 corpus，因此新 corpus 产生后必须重跑，
之后才能冻结最终 packing 参数。

| 量 | 当前值 | 状态 |
|---|---:|---|
| `MAX_PROMPT_TOKENS` | 950 | TTFT 侧已直接校准；950 PASS / 975 FAIL |
| `PROMPT_OVERHEAD_RESERVE_TOKENS` | 206 | 已实测（B/D rendering shape） |
| `CONTEXT_PACK_MARGIN` | 0.86 | 当前 candidate |
| `CONTEXT_PACK_BUDGET_TOKENS` | 639 | 当前 provisional 参数的派生值 |
| `CHARS_PER_TOKEN_EST` | 4 | **baseline 9c-final 已证明尾部不够保守，不得冻结** |
| realized k | variable | **绝不冻结为全局常数** |

#### baseline 9c-final 的已知结果

在 `8c6bee0a… / 3383 chunks` 上：

- 3379 个模拟窗口中，**70 个**在 `D = 0` 时仍有 `O + actual_context > 950`
- 排除当时已定义异常标签后的诊断性 counterfactual 中，
  仍有 **19 / 3218 = 0.59%** 超预算
- `none_observed` chunk 的 rendered `actual / est_prompt_tokens`：
  **p50 = 0.7770 · p95 = 1.0327 · max = 1.6640**
- **102 个** `none_observed` chunk 的 ratio > 1.15
- 真实相邻 rendered chunk 的 separator 增量：**observed max = 1 token**

因此当前主要未关闭的问题**不是**「separator 到底是不是 5 token」，而是：

> **一个全局 `CHARS_PER_TOKEN_EST = 4` + 固定 ±15% 误差假设，
> 不能覆盖真实 chunk tokenization 的尾部。**

estimator 的偏差是**不对称**的：中位保守（0.777），尾部危险（1.664）。
对一个余量很薄的预算，这是最坏的形状 —— **预算按中位行为定，被尾部击穿。**

Phase B 新 corpus 生成后必须重新执行 9c-final，
因为 OCR remediation 会改变 chunk 文本、长度与分布。

**在新 9c-final 完成之前**：
- 不冻结 `CHARS_PER_TOKEN_EST`
- 不冻结最终 `CONTEXT_PACK_BUDGET_TOKENS`
- 不宣布 S4a.9 CLOSED
- 不把 `k = 2` 写成全局事实


---

## 11. 关键参数的语义决定

> 本节讲**理据**。具体取值、类型、接口定义在 `core/contracts.py`,不在这里重复。

### 11.1 `TOP_K_CONTEXT` 的语义:预算内的**连续前缀**,不是"固定塞 k 个"

早期推导写"1500 ÷ 中位 292 token ≈ 3 个 chunk"。**这个除法算错了,1500/292 = 5.1。**
真正得出 3 的是**最坏情况**(max chunk ≈473 token)。**数字 3 是蒙对的,推导是错的。**

正确的机制:chunk 长度分布很宽(中位 292 / p95 373 / max 473 token),固定 k 在两头都不合适。
→ **`MAX_CONTEXT_TOKENS` 是真正的约束,`TOP_K_CONTEXT` 只是上限保护。**

**填充语义锁定为 prefix**(遇到第一个装不下的即停止,不跳过后面的短块)。两个理由:

**理由一(审计清晰)**:M1c 测的是检索排序。rank #3 因太长被预算挡在外面,
**这本身就是一个真实观察**,应记 `context_truncation`,而不是靠 packing 技巧绕过去。

**理由二(消除一个不可见的混杂)** ⭐:
> skip-and-fill 会**系统性偏好短 chunk**——所有题都倾向装进更多短块。
> 而 chunk 长度与内容类型相关(清单、表格 vs 连续散文),
> 这等于给整个实验加了一个**贯穿全部四臂、且从结果里看不出来的偏倚**。

prefix 严格按 relevance 取连续前缀,不对内容形态做隐性选择。
**在要下架构结论的阶段,一个没有隐性偏倚的基线比多用几百 token 值钱。**

skip-and-fill / knapsack packing 记入 BACKLOG,M5 作为对照臂再比。
**`TOP_K_CONTEXT = 5` 是真实约束，不是不绑定的上限保护。**
actual-token 模拟显示 **12.7% 的窗口顶到 5**。

**realized k 是逐题变量**：中位长度的 chunk 可装 2–3 个，p95 及以上只装 1 个。
其分布由 `EvalItemResult.n_chunks_in_context` 记录，
**是测量结果，不是设定值**。


### 11.2 预算的安全不等式

设 `X` = prompt 总预算，`O` = 固定开销，`m` = margin，`err` = 估算误差因子。

**必须满足**：

    (X − O) × m × err + O  ≤  X
    等价于   m ≤ 1 / err

`err = 1.15` → `m ≤ 0.8696` → **m = 0.86（向下取整）**

> **⚠️ 上界约束的取整方向只能向下。** `0.87 × 1.15 = 1.0005 > 1`，不满足约束。

**⚠️ 自洽性检查必须同时验两条**：
1. 参数本身满足上界（`m ≤ 1/err`）
2. 派生不等式成立（`B × err + O ≤ X`）

**只验第 2 条会被 `int()` 截断掩盖** —— 同一个越界的 `m` 在某些 `O` 取值下
会"侥幸通过"第 2 条，换一组输入才暴露。


### 11.3 `rerank` 是否引入:由数据决定,不预先写进架构

早期版本从"只能塞 3 个 chunk"推出"rerank 从可选升为必需"。**这一步跨越没有数据支撑,已撤回。**

能推出的只有:**k 很小时,排序质量的杠杆显著变大。** 推不出"必须引入独立 reranker 模型"。
混合检索 + RRF 融合完全可能已经把 gold chunk 稳定推进前几名;而额外的 reranker 会带来
延迟、内存、模型文件、新的故障面——**直接顶撞 A4(船上无 IT 支持)**。

**改为可测判据,由 M1c 回答**:

| M1c 观测 | 结论 |
|---|---|
| Recall@k_context ≈ Recall@20 | 融合排序已够用,**不引入**。省下的延迟和故障面直接是收益 |
| Recall@20 高但 Recall@k_context 明显低 | 排序有提升空间 → 值得试,但仍须过延迟预算 |
| Recall@20 本身就低 | 问题在召回不在排序,加了也没用 |

**M1c 出数之前,不把 reranker 写进任何架构图。**

### 11.4 thinking 模式全链路禁用

实测:问 "What is SOLAS?" 这种简单问题,开启 thinking 的 4B 模型输出 **2014 token / 36.7 秒**——
而这还是**零检索上下文**的情况。关闭后降到 380 token。

三个理由:(a) 超出延迟预算,船端 CPU 会放大数倍;(b) 与心智模型冲突,模型应"摘要依据"而非自由推理;
(c) **thinking 正是模型脱离依据自由发挥的地方,幻觉最易从此进入**。

### 11.5 生成采样一律关闭(`temperature = 0`)

早期版本只写了"不要一边跑一边改参数",**从未规定随机性怎么控制**。

如果 temperature > 0 且四臂 seed 不同,臂间差异里就混进了采样噪声。
**n=31 的规模下,几道题因采样翻面就能移动 6 个百分点**——正好是实用效应闸的量级。

→ 四臂必须完全一致。**开工前先实测确定性**:同一 prompt 跑两遍,输出逐字节相同才算通过;
不通过则该模型跑 3 次取多数并注明。

---

## 12. 模型选型

### 12.1 四道门(按顺序,任一不过即淘汰)
 
**这是顺序门,不是加权评分表。** 前一道不过的候选【不进入】下一道。
性能数字只在"同时通过全部门"的候选之间充当 tie-breaker,不参与是否通过的判定。
 
把门当评分表用会产生一种很难察觉的错误:一个许可证有问题但跑得飞快的模型
会在"综合分"里胜出,而那正是这四道门要挡住的东西。
 
| 门 | 判据 | 为什么在这个位置 | 成本 |
|---|---|---|---|
| **GATE-1** 参数 | **总参数 ≤ 5B** | 零成本,查规格即可。不该让不合格的模型占用下游任何时间 | 0 |
| **GATE-2** 运行时 | llama.cpp 可加载并跑出 bench | 船端运行时是 llama.cpp。这条不过,实验做完主角上不了船 | 低 |
| **GATE-3** 许可 | 商用可接受(见 §12.2 分级) | 需人工核官方来源,可能还要法务。放在实测之后,是为了带着数据去谈 | 中 |
| **GATE-4** 多语种 | **输出是否使用了提问所用的语言**(是/否) | 排除性筛子,不用于排名 | 低 |
 
#### GATE-1 的参数判据锚定在【总参数】
 
**[L1] falsified_if**:船端 runtime 实测证明某 MoE 模型的常驻内存接近激活参数
(例如通过 expert 分页 / mmap 按需加载),且在船端存储介质上不产生不可接受的延迟抖动。
 
约束 A2 写的是"≤5B 参数",**未区分总参数与激活参数**。对 MoE 两者可差 7 倍。
 
| 维度 | 由谁决定 |
|---|---|
| 权重存储规模(索引包体积、船端磁盘、模型内存预算) | **总参数** |
| 单 token 推理算力 | 激活参数 |
 
**依据**:在当前部署假设下,MoE 的权重存储规模由总参数决定。
**不能因为每 token 只激活部分 expert,就把未激活的 expert 从模型内存/存储预算里消失。**
 
> 措辞注意:这里说的是"当前部署假设",不是"所有 runtime 永远把全部 expert 锁在物理 RAM"。
> 后者会与上面的 falsified_if 打架 —— 一句当物理事实说、一句当工程可能说,是自相矛盾。
 
**实例(2026-09-07 核查,IBM 官方)**:
 
| Granite 4.0 型号 | 架构 | 总参数 | 激活参数 | GATE-1 |
|---|---|---|---|---|
| Micro | dense transformer | 3B | 3B | ✅ |
| H-Micro | hybrid dense | 3B | 3B | ✅ |
| H-Tiny | hybrid MoE | **7B** | 1B | ❌ FAIL |
| H-Small | hybrid MoE | 32B | 9B | ❌ FAIL |
 
**这条判据顺带解释了一件事**:`granite4:tiny` 拉取失败其实是运气 ——
那个 tag 指向的正是 7B 的 H-Tiny,本来就该被 GATE-1 淘汰。
 
**纪律**:tag 里的数字不是参数量证据。MoE 的 tag 常常标的是激活参数。
`params_total_b` 必须有 `evidence_url` + `evidence_date`,不能从 tag 名推断。
 
#### GATE-2 的结论必须限定作用域
 
**加载失败只对"这一份 artifact + 这一版 runtime"成立**,不得外推为
"该模型架构永久不兼容"。即便是模型所有者的官方 GGUF,失败也可能来自:
llama.cpp build 过旧 · 量化产物 bug · GGUF 元数据 bug · CLI 参数不适配。
 
每条 GATE-2 结果必须同时记录:
 
````
gate2_llamacpp          PASS / FAIL / INCONCLUSIVE / TODO
gate2_tested_runtime    llama.cpp 版本 + ggml 版本 + llama-cli 的 build/commit
gate2_tested_artifact_sha
gate2_tested_date
````
 
**没有这四项的 GATE-2 结果不成立** —— 那个"限定作用域"会变成一句无法核验的话。
 
**连带常规动作**:llama.cpp 升级后,此前 FAIL 的候选需重测。
`gate2_tested_runtime` 是判断某条结果是否已过期的唯一依据。
 
> 为什么 ggml 版本也要记:两者在 brew 里独立版本化,
> 而 qwen3.5 那次的 `rope.dimension_sections` schema 失败是 ggml 层的事
> (2026-08-17 补充 3 记的是 "brew ggml 0.20.0")。
 
#### GATE-4 的判据必须可客观核对
 
评测集只有 3 道多语种题,撑不起一道打分门。
**一道无法测量的门是空门** —— 看起来在筛,实际不筛任何东西。
 
但"能否产出连贯输出"同样不是一个可核对的判据 —— "连贯"由谁判、按什么标准判,都没定义。
主观判据和空门的危害是一样的:结论无法被别人复核。
 
**判据(是/否,可客观核对)**:
 
> **模型的回答是否使用了提问所用的语言,而不是回退成英文?**
 
做法:固定 5 个提示词(zh / tl / hi / en 混合),逐条记录"是/否"。
 
- **是/否 决定过门**,这一项完全客观
- 输出质量的主观印象**单独记在 notes,不影响过门判定** ——
  保留信息,但不让主观判断进入门的判据
 
定位:排除性筛子,只淘汰"完全不会这门语言"的,不用于排名。
真正的多语种评估记入 BACKLOG,M5 做。

| **M3 横评池** | **通过全部四道门的全部候选**（不设数量目标，见 §12.6 第三节） | 高 |
> **不设数量目标是刻意的。** 家族数由漏斗决定，不由目标决定。
> 无论最终通过几个，都按 §12.6 第四节报告漏斗全貌；
> **若数量少到横评做不出有意义的比较，这一事实本身即为 M3 的结论**，
> 并作为"是否放宽某道门"讨论的输入。

**门的顺序不因性能改变**：许可 AMBER 的候选不进 tie-break，哪怕它最快。

**但在多个 GREEN 候选之间，prefill 性能是结构性决定项**，
不是无结构影响的末位排序 —— 它决定 TTFT 预算下能装几个 chunk，
进而决定 B/D 臂的上下文、跨文档题是否可答、以及检索基线报哪几档 Recall。
实测差异可达 2 个 chunk vs 1 个。

tie-break 必须**带着这个后果**做。


### 12.2 ⚠️ 许可证门冲突

项目原定"仅接受 Apache 2.0 / MIT",但目前唯一 GATE-2 实测通过的 `llama3.2:3b`
用的是 **Llama 3.2 Community License**(MAU 阈值条款 + 可被 Meta 单方面更新的 AUP)。

**许可证分级**:

| 级别 | 含义 | 处理 |
|---|---|---|
| 🟢 GREEN | Apache 2.0 / MIT | 直接可用 |
| 🟡 AMBER | 其他开放权重许可 | 须法务**书面确认**后方可进入 M2 以后 |
| 🔴 RED | 禁止商用 / 不明 | 直接淘汰 |

**解法:先拿数据,再谈政策**
1. 测 🟢 GREEN 阵营模型的 GATE-2
2. 有通过的 → 主模型选它,**许可证门不动**
3. 全都失败 → 才需要修订门,且届时带着实测证据去谈

> ⚠️ 阻塞项的正确措辞是"**至少一个 🟢 GREEN 模型通过 GATE-2**",
> 不是"qwen3:4b 必须通过"——绑在单个模型上会制造假的单点故障。
>
> ⚠️ **许可证是会变的外部事实**(Google 已把 Gemma 转为 Apache 2.0)。
> 执行 GATE-3 时**重新核官方 license**,不依赖本表的记录。

### 12.3 船端候选池
 
> 用同一家族的参数变体凑数是自欺。**横评的意义在于覆盖不同的归纳偏置** ——
> 不同家族的训练数据、tokenizer、架构不同,失败模式也不同。
> 同家族换尺寸只能回答"多大够用",不能回答"哪种模型适合这个任务"。
>
> **家族按 `family_key` 判定并去重,不按出品方**(§12.6 第一节)。
> 标题不写数字:数量是漏斗的产出,不是调查的目标(§12.6 第三节)。
 
#### 候选家族
 
| # | family_key | 目标型号(≤5B 总参数) | 总参数 | license_claim(来源 · 日期) | gate3_license | **预期的差异来源**(可被 M3 证伪的预测) |
|---|---|---|---|---|---|---|
| 1 | **qwen** | Qwen3-4B / Qwen3-1.7B<br>⚠️ Qwen3.5-4B 是 MoE,需先核总参数 | 4B / 1.7B | "Apache 2.0"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 多语种训练占比高 → 预期在 ML01/02/03 三题上明显强于其他家族;在英文 fact_lookup 上**无**差异 |
| 2 | **gemma** | Gemma 3 4B / 1B | 4B / 1B | "&lt;原文&gt;"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 训练数据分布与 Qwen 差异大 → 预期**失败的题不同**。若两者错的是同一批题,说明差异来自任务而非模型 |
| 3 | **phi** | Phi-4-mini | 3.8B | "MIT"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 合成数据为主的训练 → 预期在 procedure 多步骤题上强、在多语种上弱 |
| 4 | **granite** | **Granite 4.0 Micro / H-Micro** | **3B** | "Apache 2.0"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 企业 RAG 优化 + 原生 JSON → 预期在**引用格式遵从率**上更高(三元组完整、不编页码) |
| 5 | **llama** | Llama 3.2 3B / 1B | 3B / 1B | "Llama Community"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 指令跟随保守 → 预期**拒答率偏高**:陷阱题强,但可答题可能过度拒答 |
| 6 | **nemotron**<br>⚠️ arch_lineage 待查 | Nemotron 3 Nano 4B | 4B | "NVIDIA Open"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 推理优化 → ⚠️ **与全链路禁用 thinking 冲突**。预期关闭 thinking 后质量下降幅度**大于**其他家族 |
| 7 | **mistral** | Ministral 3 3B Instruct 2512 | 3B | "Apache 2.0"(mistralai 官方 GGUF repo · 2026-09-07) | TODO | 欧洲训练分布,声明语言**不含 tl / hi** → 预期 ML02(tl) 与 ML03(hi) 明显弱,ML01(zh) 正常 |
| 8 | **smollm** | SmolLM3-3B | 3B | "Apache 2.0"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 训练数据完全公开 → 预期整体最弱,但**失败模式最可解释**(可追到训练数据缺口) |
 
#### 参考基准(不进候选池)
 
| family_key | 型号 | 总参数 | license_claim | role | 用途 |
|---|---|---|---|---|---|
| aya | Tiny Aya | 3.35B | "CC-BY-NC" —— **明确非商用** | `research_reference` | 多语种能力的**上限参考**。GATE-3 必然 RED,不进商用候选,但可回答"如果不受许可约束,多语种能做到多好" |
 
#### 明确排除(附理由,避免重复调研)
 
| family_key | 排除理由 |
|---|---|
| **qwen**(DeepSeek-R1-Distill-Qwen-1.5B) | **不是独立基座家族。** 蒸馏自 Qwen2.5,架构与词表均继承 → `family_key: qwen`。见 §12.6 第一节的硬规则与 §6.2 错误 #11 |
| **llama**(DeepSeek-R1-Distill-Llama-8B) | 同上继承 `llama`;且 8B > 5B,GATE-1 FAIL |
| DeepSeek 主线 | V3/V4 为百亿至千亿级 MoE,总参数远超 5B,GATE-1 FAIL。**结论:DeepSeek 目前没有可用的独立小模型基座** |
| Granite H-Tiny / H-Small | **7B / 32B 总参数,GATE-1 FAIL**(IBM 官方 · 2026-09-07)。旧版写"Granite 4 tiny"是错的 |
| Kimi | 无 ≤5B 型号,GATE-1 淘汰。K 系列参数量千亿至万亿级,权重数百 GB,本机连当 teacher 都不够 |
| GLM | 主力型号 ≥9B,超出 GATE-1;GLM-Edge 系列在备选池 |
| Yi | Yi-1.5 6B > 5B,GATE-1 FAIL |
| Falcon / MiniCPM / GLM-Edge | 备选池。按 §12.6 的停止规则决定是否继续调查 |
 
> ⚠️ **本表的许可与参数是"某日在某来源看到的声明",不是已核验的状态。**
> 按风险 #21,license 是会变的外部事实,GATE-3 执行时必须重新核官方来源。
>
> **所有候选的 `gate3_license` 一律 TODO,包括 Qwen。不给任何模型裸的 🟢/🟡。**
> 理由与契约里把"已校准/未校准"阈值物理分成两节相同:
> **"看到的声明"和"已核验的状态"必须在视觉上就能区分**。
> 一旦表格里出现一个绿勾,注释里写多少遍"不预判"都会被忽略 ——
> v0.12 的第一版就是这么失败的:新表加上了,旧表的绿勾没删。
>
> ⚠️ **`arch_lineage` 必须查证并附来源。** 出品方是独立公司,不代表基座是独立的。
> Nemotron 尤其要查 —— 若其谱系继承自某个已在表内的家族,应合并 `family_key`。
>
> ⚠️ **「预期的差异来源」这一列是 M3 的预注册。**
> 写"能力强/弱"是事后可以解释一切的描述;写"**预期它在哪一类题上和别人不一样**"
> 才能被结果证伪。M3 出数时直接对照"预期 vs 实测",**预期错了本身就是发现**。
> 只写能力描述的话,结果出来只能事后编解释,那就是 HARKing。

### 12.4 Teacher 模型池
 
**teacher 不上船,因此不受 GATE-1(≤5B 总参数)与 GATE-2(llama.cpp 可加载)约束。**
→ 在这两个维度上应当挑最强的,且应该不止一个。
 
**但 teacher 不受 GATE-3 约束这个推论不成立。**
 
teacher 的产出 → 合成训练数据 → LoRA adapter → **上船**。
链条中间没有任何一处切断许可的传递。许多开放权重许可**专门规定了这个场景** ——
"用本模型的输出去训练或改进其他模型"往往带有派生物命名、标注或授权义务。
 
> **要问的不是"它上不上船",而是"它的产出会不会上船"。** 见 §12.8。
 
#### teacher 的两道门
 
| 门 | 判据 | 为什么 |
|---|---|---|
| **GATE-T1** 输出用途许可 | 该许可是否限制"用其输出训练/改进其他模型"?若有条款,义务具体是什么? | **硬门。** 义务会附着到要上船的 LoRA adapter 上 |
| **GATE-T2** 本机可行性 | 4-bit 内存 + 批量生成吞吐,在 96 GB 统一内存上实测 | 换 teacher 不能只换名字。内存装得下 ≠ 吞吐够跑完批量生成 |
 
**部署许可对 teacher 是 N/A** —— 但台账里必须显式写 `N/A(不部署)`,**不能留空**。
留空看起来像"还没查",而 N/A 是一个已经做出的判断。
 
#### 候选表
 
| # | 候选 | 规模 | 4-bit 内存 | license_claim(来源 · 日期) | **GATE-T1 输出用途** | GATE-T2 本机 | 角色 |
|---|---|---|---|---|---|---|---|
| 1 | `qwen3:32b` | 32B 稠密 | ~18 GB | "Apache 2.0"(&lt;来源&gt; · &lt;日期&gt;) | TODO —— **Apache 2.0 无此类条款,预期 PASS** | ✅ 已实测 26.09 tok/s | **M2 首轮基准 teacher** |
| 2 | `gpt-oss:120b` | 120B MoE | ~63 GB | **TODO**(&lt;来源&gt; · &lt;日期&gt;) | TODO | ⚠️ **63/96 GB 是边界情况**,批量生成吞吐须实测 | 第二 teacher 候选,须先过 T1 + T2 |
| 3 | `Qwen3.5-35B-A3B` | 35B MoE | ~20 GB | "Apache 2.0"(&lt;来源&gt; · &lt;日期&gt;) | TODO | 待测 | 速度与质量平衡,适合大批量生成 |
| 4 | Llama 3.3 70B | 70B 稠密 | ~40 GB | "Llama Community"(&lt;来源&gt; · &lt;日期&gt;) | ⚠️ **已知含派生模型条款** —— 命名要求与 "Built with Llama" 标注义务。须核当前版本原文 | 待测 | **降级为备选。M2 首轮不用** |
| 5 | GLM-4.5-Air | 106B MoE | ~55 GB | **TODO —— 原表连许可名都没写** | TODO | 待测 | **降级为备选。M2 首轮不用** |
 
**GATE-1 不适用**(teacher 不上船),但仍受本机内存物理限制:
DeepSeek V4(~284B,4-bit ≈150 GB)、Kimi K2.5/K3(权重 594 GB)—— 超 96 GB,**不可用**。
 
#### M2 首轮的决定
 
**单 teacher 跑通,用 `qwen3:32b`。**
 
理由不是法律风险的大小,而是**干净方案零成本**:
它已下载、已实测吞吐、许可无此类条款。用它不需要牺牲任何东西 ——
这不是权衡取舍,是免费规避一个未量化的风险。
 
双 teacher 交叉验证(见下)推迟到第二个 teacher 同时过 T1 与 T2 之后。
**不因为想要交叉验证就提前用一个输出条款未核的 teacher。**
 
#### 具体后果:一个很难向法务解释的局面
 
若用 Llama 3.3 70B 当 teacher:
 
    学生模型 = Qwen(Apache 2.0)
    训练数据 = Llama 3.3 70B 生成
    → 上船的 adapter 可能需要以 "Llama" 开头命名 + 标注 "Built with Llama"
 
**一个 Apache 2.0 的学生,因为老师的缘故,交付物要挂另一家的名字。**
这不是抽象风险,是一个具体的、需要签字的合规状态。
在 Kuok Group 的语境下,这会引出"为什么船上的系统叫 Llama 什么"这种问题。
 
#### 为什么要多个 teacher —— 三个技术理由(不变)
 
**① 交叉验证过滤劣质样本。** 用两个不同家族的 teacher 对同一 chunk 各生成一次。
**两者不一致的条目 = 该 chunk 有歧义或 teacher 出错 → 自动标记可疑。** 几乎免费的质量闸门。
 
**② 避免把单一 teacher 的偏好蒸馏进学生。** 只用一个,student 学到的是"这个 teacher 的
风格和盲点",而不是"任务本身"。
 
**③ judge 必须与 student 不同家族。** 若 teacher / student / 评分器都是同一家族,
会出现 **self-preference bias**(模型偏爱自己家族的输出风格),评分虚高。
 
> ⚠️ 这三条都成立,但它们是**质量论证**,不是**许可论证**。
> 第二个 teacher 的引入时点由 GATE-T1 决定,不由这三条理由决定。
> M2 首轮单 teacher 的代价是失去 ①,那是可接受的 —— 交叉验证是质量增益,不是正确性前提。
 
#### 合成数据必须记 teacher 来源
 
`EvalItemResult` 已有 `teacher_digest`,覆盖 M2 结果的可追溯性。
**但合成训练数据文件本身也要记**:teacher 的 model_id + digest + license_claim + 生成日期。
 
理由:若某个 teacher 后来被发现有输出条款问题,你必须能立刻回答
"哪些训练数据受影响、哪些 adapter 是用它训的"。
没有这条记录,答案是"全部重来"。

### 12.5 模型 / artifact / provenance / 运行时是四件事

    GATE-2 的 PASS / FAIL   = 永远只关于「这一份 artifact + 这一版 runtime」的实测事实
    artifact_provenance     = 独立维度
      verified    → 才允许赋 source_trust（official / upstream / community）
      unverified  → 不赋分级（分级本身就是一个 provenance 判断）

    要把 artifact 级失败提升为「model × runtime」的更强负面证据
      → 换一个 provenance 已知的独立 artifact 复测

**⚠️ 取得渠道不等于 provenance。**
"文件位于某工具的 blob store" 只证明本轮通过该工具取得，
**推不出 GGUF 由谁转换、由谁发布**——
主流工具均支持直接导入外部 GGUF，其 hub 的模型定义也可引用别的 base/source。

**provenance 的验证动作**：

    shasum -a 256 <本地.gguf>
    对照模型所有者 HF 仓库文件页显示的 SHA256（LFS 文件的 OID 即 sha256）

    一致   → artifact_provenance: verified
    不一致 → 排除"官方原件"，provenance 仍未知

精确地说，它验的是「这个文件就是仓库 X 发布的那一份」，
**不是「仓库 X 亲手转换的」**。


### 12.6 家族的定义、调查的停止规则、漏斗数字
 
#### 一、"基座家族"必须有定义,否则家族数没有意义
 
v0.11 §12.3 曾把 `DeepSeek-R1-Distill-Qwen-1.5B` 列为第 9 个基座家族。
**但它是 Qwen2.5 的架构与词表被蒸馏** —— 同一套架构谱系、同一个词表、
同一批预训练数据分布。它提供不了横评所追求的那种归纳偏置多样性。
 
按品牌名计入家族,就是用换了个牌子的同一个东西凑数。
 
**台账新增三个字段**:
 
```yaml
arch_lineage:      qwen2.5        # 架构谱系,不是发布方
tokenizer_family:  qwen           # 词表家族
family_key:        qwen           # ⚠️ 人工判定,必须附 rationale
family_key_rationale: "蒸馏自 Qwen2.5-Math-1.5B，架构与词表均继承"
```
 
**`family_key` 是人工判定,不是机械拼接。**
理由:很多模型借用 Llama 的 tokenizer,但架构与预训练数据完全独立;
机械拼接会把它们错误地合并。`arch_lineage` 与 `tokenizer_family` 是**判定的证据**,
不是判定的公式。真正要的是让这个判断**显式且可审计**,
而不是从品牌名读出来,也不是从两个字段自动算出来。
 
**一条硬规则**:
 
> **蒸馏 / 微调自另一个基座的模型,`family_key` 继承其基座。**
 
这条直接捕获:
 
| 型号 | family_key | 后果 |
|---|---|---|
| DeepSeek-R1-Distill-Qwen-1.5B | `qwen` | 不算独立家族。**DeepSeek 目前没有可用的独立小模型基座,从候选池移到"明确排除"表** |
| DeepSeek-R1-Distill-Llama-8B | `llama` | 且 8B 已 GATE-1 FAIL |
| 任何未来的蒸馏模型 | 继承基座 | 规则一次写死,不用逐个争论 |
 
**同样要查而不能想当然的**:Nemotron、以及任何"某厂自研"但可能基于既有谱系的模型 ——
`arch_lineage` 必须查证并附来源,不能因为发布方是独立公司就默认是独立家族。
 
**家族数按 `family_key` 去重。** 汇报必须同时给出两个数:
 
````
models investigated        = N
base families investigated = M    ← 按 family_key 去重
````
 
只报前者会让"同一家族凑数量"看起来像"覆盖面广"。
 
#### 二、四层漏斗
 
````
调查池  →  获取池  →  GATE 池  →  M3 横评池
````
 
| 层 | 内容 | 成本 |
|---|---|---|
| **调查池** | 尽量广。**优先增加不同 family_key**,同家族的尺寸梯度是第二维度 | 零(桌面调研) |
| **获取池** | 有明确 acquisition route 的(Ollama / 官方 GGUF / 可信 GGUF) | 低(下载) |
| **GATE 池** | 正式跑 GATE-1→2→3→4 | 中 |
| **M3 横评池** | 通过全部门的 | 高 |
 
#### 三、调查的输入是时间盒,不是数量
 
**不设"目标 N 个型号"或"目标 M 个家族"。** 数量是**产出**不是**输入** ——
设成输入就会诱导为凑数找模型,那正是本节要防的。
 
````
调查预算：≤4 小时桌面调研（时间盒）
 
停止规则（满足任一即停）：
  ① 时间盒用尽
  ② 连续 3 个新调查的家族都被零成本门淘汰
     （无 ≤5B 型号 / 无任何 acquisition route / license 明确非商用）
     —— 说明搜索空间已经见底
  ③ §12.3 已列的全部家族都已有处置（纳入或带依据地排除）
````
 
> 规则 ② 里的 3 本身是判断:2 容易被检索顺序的偶然性触发,
> 5 又太晚。这个数是 [L3],跑一轮后可调,调了要记。
 
**调查完是 8 个还是 16 个都行,重要的是能说清"为什么停在这里"。**
 
#### 四、漏斗每一层的数字本身就是交付物
 
**调查可以"失败",而失败本身是有效结论。**
 
````
调查池        N 个型号 / M 个家族
  ↓ GATE-1    淘汰 a 个（附每个的总参数与来源）
获取池        ...
  ↓ GATE-2    淘汰 b 个（附 runtime 版本与 artifact 哈希）
  ↓ GATE-3    淘汰 c 个（附许可来源与日期）
M3 横评池     X 个型号 / Y 个家族
````
 
**如果 Y < 5(§12.1 对 M3 的要求),结论不是"调查失败了",
而是"当前约束下可用基座家族仅 Y 个"** ——
**这本身就是给 Faraz 的有效结论**,它直接支撑"要不要放宽许可证门"这个讨论。
 
比硬凑到 5 个诚实,也更有用。这与 §15 "只找大效应、不扩题"是同一个取向:
**把限制转成业务判断,而不是想办法让数字达标。**
 
#### 五、完成判据(过程性,不是数量性)
 
> ① 每个纳入项有完整 provenance(family_key + rationale / 总参数 / 激活参数 /
>    arch_lineage / tokenizer_family / acquisition route / source_trust /
>    license claim + 来源 URL + 查证日期)
> ② **每个排除项有带依据的排除记录**(哪道门、什么数据、哪个来源)
> ③ acquisition 与 gate 分离
> ④ 汇报同时给 models 数与 families 数(后者按 family_key 去重)
> ⑤ 漏斗各层的数字与淘汰理由完整
> ⑥ 停止规则触发了哪一条,记录在案
 
**第 ② 条最重要。** "我们排除了 X,因为 Y"比"我们测了 15 个"更能证明调查是系统的。
 
#### 六、为什么这件事不该阻塞 M2
 
**M2 只需要一个合法、可运行、足够代表这个量级的实验载体。**
它是 2×2 架构对照(RAG × 微调),**不是选型实验** ——
"在这个量级的模型上 RAG 优于微调"这个结论不依赖于用了哪一个具体模型。
 
而 M3 是 M2 **之后**的里程碑(§8)。把 M3 的调查放在 M2 前面做,有两个代价:
 
1. **顺序倒置**,正是 §6.3 纪律要防的
2. **可能在调查一个还没定形的问题** —— 如果 M2 显示 RAG 完胜微调,
   M3 的重点会从"哪个基座最强"变成"**哪个模型最会照着依据说话、最愿意拒答**",
   那是一套不同的筛选维度
 
**因此候选池调查是一条独立的并行轨道,不设截止,不阻塞任何取证步骤。**
执行层如何切分见 `执行手册` 的 S4a / S4b。
 
#### 七、许可 pre-check 与正式 GATE-3
 
选定 M2 主模型时需要一个**轻量的许可 pre-check**,但这不等于 GATE-3 已执行:
 
```yaml
license_claim:            # 某日在某官方来源看到的声明
  text: "Apache 2.0"
  source_url: ...
  checked_date: ...
gate3_license: TODO       # 正式核验尚未进行
```
 
**这里有两个不同级别的决定,不要混在一起记:**
 
| 决定 | 级别 | 为什么 |
|---|---|---|
| **"pre-check 足以解锁 M2"** | **[L1]** | 这是一个关于"什么条件下允许往前走"的方法性边界 |
| "M2 用哪个具体模型" | **[L2]** `owner_experiment: M3` | 实验载体的选择。**可以被 GATE-3 否掉,而否掉之后 M2 的结论不变** —— 一个能被推翻却不影响结论的决定不该是 L1 |
 
**[L1] 那条的 `falsified_if`**:
 
> M3 横评显示 RAG vs 微调的相对优劣**在不同基座家族之间反转**。
 
若真发生,说明 M2 的结论对模型选择高度敏感,
"任何一个合法载体都够用"这个前提就不成立,M2 需要重做。
**这条把 M3 变成了 M2 前提的一次检验**,而不只是一个后续里程碑。
 
**为什么这个捷径是有理由的,而不是抄近路**:
M2 是架构对照实验。**即便主模型后来在 GATE-3 被否,M2 的结论仍然成立** ——
需要更换的是船端的那个模型,不是"RAG vs 微调"这个判断。
所以 pre-check 足以解锁 M2,而正式 GATE-3 应在 M3 / M6 之前完成。
 
**这句话必须写进决策日志。** 不写,它就是一个默默的抄近路;
写了,它就是一个有明确边界与后果的选择。

### 12.7 embedder 有自己的一套门
 
`role: embedder` **不等于"不过门",而是"过另一套门"。**
 
**embedder 也要上船。** 船端必须能算出查询向量,否则检索层根本起不来。
把它的 gate 字段留空,等于把一个上船组件的可行性问题推迟到 M6 才发现。
 
| 门 | 判据 | 为什么 |
|---|---|---|
| **GATE-E1** 运行时 | 船端运行时可加载并算出查询向量 | 与 GATE-2 同理:Ollama 可用 ≠ 船端可用(§12.5 的三件事分离) |
| **GATE-E2** 许可 | 商用可接受 | 与 GATE-3 同理 |
| **GATE-E3** 多语种 | **覆盖评测集的全部语言(en / zh / tl / hi)** | 见下,这一条是承重的 |
 
#### ⚠️ embedder 的多语种比 generator 的更要命
 
对 generator,GATE-4 是**烟雾测试** —— 答不好那门语言,顶多是那道题答得差。
 
对 embedder,多语种是**承重的**:
 
> embedder 不懂印地语 → ML03 在**检索阶段**就死了 → B/D 臂拿什么模型生成都无所谓。
 
**检索是第一道。第一道死了,后面的实验测的就不是它想测的东西。**
 
因此 GATE-E3 **不是烟雾测试,是硬门**,而且判据不是"能否产出该语言",
而是"**该语言的查询能否召回正确的英文 chunk**" ——
语料是英文,查询是中文/他加禄语/印地语,这是**跨语言检索**,比同语言检索难。
 
**这条应在 M1c 直接测**:评测集的 3 道多语种题在检索层的 Recall,单独报,不与英文题合并。
若多语种题的 Recall 显著低于英文题,那是 embedder 的问题,不是生成模型的问题 ——
**这个归因区分必须在 M2 之前做出来**,否则 M2 的多语种表现会被错误归因到臂上。
 
#### 台账字段
 
```yaml
- id: bge-m3
  role: embedder
  dim: 1024                    # 影响索引包体积与岸船一致性校验(见 contracts.IndexManifest)
  gate_e1_runtime:      TODO   # 船端能否算查询向量
  gate_e2_license:      TODO
  gate_e3_multilingual: TODO   # ⚠️ 硬门,不是烟雾测试
  gate_e3_evidence:     null   # M1c 的多语种题 Recall
  notes: "Ollama 可用 ≠ 船端可用。同 §12.5 的三件事分离。"
```
### 12.8 "不上船"不等于"不受约束"
 
§12.4 原标题写的是「Teacher 模型池(**不上船**,不受 5B 限制)」。
那个括号在做两件事:
 
1. **正确地**免除 GATE-1(参数)与 GATE-2(llama.cpp 运行时) —— teacher 确实不上船
2. **顺带地**免除了 GATE-3(许可) —— 这一步不成立
 
**判据不是"它上不上船",而是"它的产出会不会上船"。**
 
#### 三类组件,三种约束
 
| 角色 | 自身上船? | 产出上船? | 受哪些门约束 |
|---|---|---|---|
| candidate(生成模型) | ✅ | ✅ | GATE-1~4(§12.1) |
| embedder | ✅ | ✅(向量进索引包) | GATE-E1~E3(§12.7) |
| **teacher** | ❌ | ✅**(合成数据 → LoRA adapter)** | **GATE-T1~T2(§12.4)** |
 
**第三行是最容易漏的,因为它的"不上船"是真的。**
漏的原因是把"部署"当成了唯一的约束传递路径,而**许可可以顺着产出传递**。
 
#### 这条规则的其他适用场景
 
以后每引入一个"辅助模型/工具",都要问一次同样的问题:
 
| 场景 | 产出上船吗? | 结论 |
|---|---|---|
| LLM judge(M3/M5 引入) | 产出是分数,不进交付物 | 风险低。**但若用 judge 做拒绝采样来筛训练数据,产出就间接上船了** —— 那时要重新问 |
| 数据清洗/标注模型 | 产出进语料或训练集 | **同 teacher,受 GATE-T1** |
| 代码生成工具 | 产出是要上船的代码 | 需单独核其输出条款 |
 
#### 记录纪律
 
**"不适用"必须显式写 `N/A(理由)`,不能留空。**
留空看起来像"还没查",而 N/A 是一个已经做出的判断。
这与 §12.3 里"不给裸绿勾"是同一个手法:**让"没查"和"查过了不适用"在视觉上可区分。**
---

## 13. 实验设计:M2 的 2×2 因子对照

### 13.1 预注册:实验开始前必须写下的东西

**M2 不允许在没有预注册文件的情况下开跑。写完提交后不许改。**

*为什么*:Faraz 有明确的先验立场。跑完再决定"什么算微调有效",无论结论朝哪个方向
都会被动机性推理污染——这就是 HARKing。**预注册把判定标准锁死在看到数据之前。**

**四个臂**(同一模型、同一评测集、同一语料):

| 臂 | 配置 | 回答什么 |
|---|---|---|
| **A** | 裸模型,不检索不微调 | 对照组基线 |
| **B** | 仅 RAG | 检索的贡献 |
| **C** | 仅微调 | **微调的贡献 ← Faraz 最关心** |
| **D** | RAG + 微调 | 是否叠加 |

**三个假设**(主指标 = 答案正确率,在 31 道可答题上):
H1: B > A · H2: C > A · H3: D > max(B, C)
> **k 值更新(契约 v0.2.0 后)**:判据里的 `k_context` 现在是 **2**(1050 预算)
> 或 **3**(1500 预算),不是 v0.11 假设的 3 或 5。
 
| 观测 | 结论 |
|---|---|
| Recall@2 ≈ Recall@20 | 融合排序已够用,**不引入 reranker**。省下的延迟与故障面直接是收益 |
| Recall@20 高但 Recall@2 明显低 | 排序有提升空间 → 值得试,但仍须过延迟预算 |
| Recall@20 本身就低 | 问题在召回不在排序,加 reranker 无用,回去调检索或分块 |
 
**⚠️ 槽位从 5 掉到 2,排序质量的杠杆显著变大。**
同一条判据在 5 个槽位和 2 个槽位下会给出不同结论 ——
所以判据里的 k 必须跟着预算走,不能沿用 v0.11 的数字。

### 13.2 判定规则:两道闸,不是一个数字

早期版本写"净差 ≥6 题 ≈19 pp → 可下结论"。**这是统计学误用,已撤回。**

McNemar 看的是**不一致对**,不是净差。反例(实算):

> B = 25/31,C = 19/31,净差正好 6 题(19.3 pp)。
> 若不一致对是 b=8、c=2,双侧精确检验 p = 2 × P(X≥8 | n=10) = **0.109,不显著**。
> 只有 6 个不一致对**全部同向**(b=6, c=0)时,p = 2 × (1/2⁶) = **0.031** 才显著。

**同样的净差,可以显著也可以不显著,取决于两个方案是互有胜负还是单向改善。**

| 闸 | 问什么 | 通过线 |
|---|---|---|
| **闸一:实用效应闸** | 差异大到值得改变决策吗? | Δ答案正确率 **≥ 19 pp**(≈6/31 题) |
| **闸二:统计检验** | 差异能与噪声区分吗? | McNemar 精确检验 **双侧 p < 0.05** |

**两道闸都过 → 接受假设。** 只过一道 → 记为提示性,不接受。

> **为什么"只过闸二"也不接受**:n=31 时统计显著但幅度只有 8 pp 的差异,
> 换算成运维现实就是"微调让 2.5 道题答得更好,代价是每次手册修订都要重训重发"。
> **决策不该被这种差异翻转。**

**硬数据要求**:McNemar 需要逐题逐臂的对错矩阵(31 × 4)。
→ 结果必须记录到**单题粒度**。**这一条如果漏了,M2 跑完也算不出检验,只能重跑。**

### 13.3 判分协议

**预注册锁了阈值却没锁"怎么算对",防 HARKing 的作用只剩一半。**

**问题规模(实测)**:31 道可答题中**只有 7 道有结构化 `answer_key`**;
其余 24 道只能靠 `required_elements` 或人判。`required_elements` ≥4 个的有 **9 道**
(PR01、PR04 各 **10 个**;PR03 7 个;PR05 6 个;CN04 5 个;PR02/PR06/FL06/CD01 各 4 个)。

PR01 有 10 个 required element。**漏 1 个算对吗?漏 3 个呢?答对 8/10 但多编了一个危险步骤呢?**
不先回答这些,B=77% vs C=71% 的差距可能纯粹来自判分时的手松手紧。

**三级判分**:

| 判级 | 定义 |
|---|---|
| **Correct** | 命中全部 `required_elements`,且无与 gold 冲突的内容 |
| **Partial** | 命中 ≥60%,无冲突内容 |
| **Incorrect** | 命中 <60%,**或**出现冲突内容,**或**该拒答却作答 |

**Partial 一律映射为 Incorrect,只作诊断维度单独报告。**
*理由*:引入部分分会让主指标变成连续量,McNemar 就用不了了;而且"多少分算过"又是一个
需要预注册的新参数。**n=31 的规模上,少一个自由参数比多一点分辨率更值钱。**

**"与 gold 冲突"**指:数值、时限、职责主体、步骤顺序与 gold 明确矛盾。
**不包括** gold 没提到但也不矛盾的补充信息(属冗余,不扣分)。
`source_boundary_ambiguity` 已标注的边界(如 PR06 的"恰好 1h/8h")、`FL07` 的源文冲突数字,
**两种答法都不判错也不加分**——源文自己没定义,不能要求模型定义。

### 13.4 谁判分:人工盲评

| 方案 | 成本 | 风险 |
|---|---|---|
| **A. Arya 手工盲评全部** ✅ | 4 臂 × 31 题 = **124 条,约 2–3 小时** | 无 judge 变量 |
| B. LLM judge | 约 20 分钟 | **judge 影响可能大过被测模型本身** |

**推荐 A**:24/31 的题依赖判分裁量,judge 是这个实验里**杠杆最大的单个组件**。
n=124 的规模上人工判分完全可行,而且**直接把最大的效度威胁从实验里消掉**。
LLM judge 留到 M3(答案条数上百)或 M5 再引入,届时用 M2 的人工结果做校准基准。

#### 🔒 盲评是强制的

**判分者(Arya)同时是方案设计者,且已有"RAG 优于微调"的先验立场。
这正是预注册要防的偏见,而手工判分会把它重新引进来。**

流程:跑完四臂 → 脚本把答案**打乱顺序、隐去臂标签** → 只在盲表上判分 →
判完再还原臂标签 → 两份文件都进 Git,时间戳可查。

**成本接近零,消掉的是这个实验最大的一个可疑点。**
汇报时可以直接说:**判分是盲的。** 这句话的分量比多跑 20 道题大得多。

#### 一致性自检(替代第二判分者)

单人项目没有第二双眼睛,用**时间差**代替:判完全部 124 条后,**隔至少 24 小时**,
重新盲评随机抽取的 20 条,计算一致率。**低于 90% → 判分标准本身太模糊,收紧规则后重判。**
一致率写进 M2 报告——这是可审计的自证,不是"我觉得我判得挺准"。

### 13.5 已知混杂:四臂的 prompt 天然不同

**这是本设计固有、无法消除的混杂,必须写进预注册而不是事后解释。**

A 臂没有 context 块;C 臂用微调时的格式。所以 `prompt_config_sha256` 在四臂之间**按构造就不同**。
含义:**B − A 的差值里同时包含"有证据"和"prompt 格式不同",本设计分离不了。**

**压到最小**:A 臂的 prompt **必须是 B 臂减去 context 块**,其余措辞逐字一致;
C 臂的微调格式**必须与 A 臂的提问格式一致**。四条 prompt 全文进预注册附录。

> **汇报重点**:**D − B(微调净增益)和 D − C(检索净增益)是这个设计里
> 唯一两个 prompt 形态可比的对比。** 重量应放在它们上,而不是全压在 B − A 上。

### 13.6 补充实验:文档更新响应测试(决定性证据)

这个实验比 2×2 更能说明架构选择,而且完全公平:

1. 取 CMM §9 的"晋升船长前须并行航行**至少 5 天**",改成"**至少 7 天**"
2. 测 B/D 臂:重建索引需多久 → 预计**分钟级**
3. 测 C 臂:重造合成数据 + 重新 LoRA → 预计**小时级**
4. 记录两者的**正确率恢复情况与耗时**

*为什么重要*:ISM 手册会修订、公司程序会更新。**这是真实运维需求,不是假想场景。**

### 13.7 C/D 臂的训练数据

用**本机 teacher** 从 3309 个 chunk 生成合成问答对(公司文档不出机器,约束 A5)。
**必须含 ≥20% 的"上下文里没答案 → 正确拒答"负样本**——这是让小模型学会说"我不知道"的唯一办法。

⚠️ **C/D 是微调的乐观上界**:训练语料与评测出处 chunk 重合,属 favorable condition。
如果 C 仍不赢 B,结论非常强;如果 C 赢,则需再进入泛化实验才能下结论。

### 13.8 三种可能结果与对应动作

| 结果 | 动作 |
|---|---|
| **D 最好**(最可能) | 采用 RAG + 微调组合。皆大欢喜,微调有明确位置 |
| **B ≈ D,C 明显低** | 采用纯 RAG;微调降为可选优化,只修正具体行为缺陷 |
| **C 意外地好** | 认真分析原因(评测集是否太窄/太模板化),扩充后重跑再下结论 |

**无论哪种,M2 结束时手上都有一张可以直接汇报的表。这是这个里程碑的全部价值。**

---

## 14. 评测集设计与边界

### 14.1 实际构成(v5.2,实测统计)

| 类型 | 题数 |
|---|---|
| `fact_lookup` 事实查找 | 15 |
| `concept` 概念定义 | 4 |
| `procedure` 程序步骤 | 6 |
| `cross_doc` 跨文档 | 3 |
| `trap` 陷阱 | 8 |
| `multilingual` 多语种(zh/tl/hi 各 1) | 3 |
| **合计** | **39** |

可答题 31 / 拒答题 8。语料覆盖 9 份手册全部。
`answer_key` 覆盖 7/31(其余 24 道依赖判分裁量,见 §13.3)。

### 14.2 字段协议(已冻结)

**冻结的是协议,不是题数。** 这套协议比继续加题更有价值:
`gold_answer` / `answer_key` / `required_elements` / `acceptable_elements` /
`citations`(三元组 + 原文引语) / `known_distractor` / `trap_subtype` /
`at_risk` + `exclude_from_primary_score` / `source_boundary_ambiguity` /
`safety_critical` / `pair_id` / `rationale`。

**字段的类型与契约定义在 `core/contracts.py` 的 `EvalItem`,不在这里重复。**

### 14.3 待收口项

| # | 缺口 | 影响 |
|---|---|---|
| 1 | **`gold_chunk_ids` 39/39 全空** | **检索层 Recall 无法测量**;实验出负结果时无法区分"检索没找到"与"模型没用好" |
| 2 | **`gold_chunk_ids` 归错了类别** | 它是 citations 在**某套语料+分块配置**上的投影,**可再生**;而 citations 是人的判断、不可再生。混在一起 → M5 一做分块消融就全部作废且**没有再生路径** |
| 3 | **TR01 是拒答题却带 citation** | 8 道拒答题里唯独它有。那条引文实际是**干扰项来源**,与 `known_distractor.source` 重复编码;更要紧的是**回填脚本会给拒答题生成 gold chunk,污染 Recall 统计** |

**处置**:`gold_chunk_ids` 移出评测集,改为按 (评测集版本, 语料哈希, 解析器版本) 命名的
**派生产物**;TR01 的 citation 移入 `known_distractor.source`(改后 citation 总数 39→38,
**这是个免费的一致性检查**)。

### 14.4 ⭐ 统计功效:这套评测集能检测多大的效应

可答题 **n = 31**。四臂跑同一批题,属**配对数据**。

| 臂间差异 | 换算 | 统计判断 |
|---|---|---|
| 1–2 题 | 3–6 pp | 纯噪声 |
| 3–5 题 | 10–16 pp | 提示性,**不下结论** |
| **≥6 题** | **≥19 pp** | **可下结论** |

**含义:如果微调只带来 5–10 个百分点的提升,这套评测集分辨不出来。**

**但这不构成扩题的理由**:5–10 pp 的提升本来也不足以支撑微调的运维成本——
每次手册修订都要重造合成数据、重训、重新分发(见 §13.6)。
一个只带来边际提升、却把更新周期从分钟级拉到小时级的方案,数据再显著也不该采纳。

→ **预注册"我们只找大效应",并把这个限制写进汇报,而不是事后解释。**

#### 陷阱题的样本量问题

陷阱题 8 道,TR02 已排除 → **实际计分 7 道**,分型后 `absent_no_hit` 2 道 +
`absent_with_distractor` 5 道。**在 n=2 和 n=5 上算"拒答正确率"没有意义**——
一道题就是 50% / 20% 的波动。

→ `trap_subtype` 保留(它是好标注),但**报告按个案定性呈现,不报率**。
写法是"D 臂在 TR08 被 30000 m³ 干扰项误导,B 臂正确拒答",而不是"抗干扰拒答率 80% vs 60%"。

> 扩充陷阱题至 ≥15 道 → 记入 BACKLOG,**M5 安全验证阶段再做**。
> M2 的任务是架构决策,不是安全验收。

#### 由此产生的一条推论:`MIN_RELEVANCE` 应当取保守偏低值

**拒答阈值不只影响拒答表现,它直接影响主指标**:

| 情形 | 后果 |
|---|---|
| 可答题检索分低于阈值 → 拒答 | **记 Incorrect,直接扣主指标** |
| 陷阱题检索分低于阈值 → 拒答 | 正确,但**只做个案呈现,不进主指标** |

**两边代价不对称。** 既然陷阱表现本来就不报率,为它抬高阈值就是拿会计分的换不计分的。

→ 校准判据是"**尽量不误拒 31 道可答题**",不是"最大化陷阱拒答率"。
拒答的正式校准(独立校准集)排到 **M5**,那时它才是主角。

### 14.5 完成判据(M1b)

| # | 判据 | 状态 |
|---|---|---|
| ① | 题目 ≥35 且陷阱 ≥8 | ✅ 39 / 8 |
| ② | 每题带三元组出处 | ✅ |
| ③ | 4 个 distractor 已补录、`is_trap` 已删、孤儿 `pair_id` 已修 | ✅ v5.2 |
| ④ | CMM 4 题对照原文核验 | ✅ |
| ⑤ | TR01 citation 移入 `known_distractor.source` | ⏳ |
| ⑥ | `gold_chunk_ids` 外置为派生产物,映射生成率 100% | ⏳ |
| ⑦ | 通过 §14.6 单人自审协议 | ⏳ |
| ⑧ | 评测集冻结并记录 sha256 | ⏳ |

### 14.6 单人自审协议(替代"找同事 review")

**本项目由 Arya 独立完成,没有可以分担 review 的同事(约束 B5)。**
"找另一个人复核"在这里不是可选项,必须用**可单人执行的机制**替代。三条都不依赖第二个人:

**① 时间差自审(用时间代替第二双眼睛)**
评测集定稿后**放置至少 48 小时**,再重新核验 7 道 `safety_critical` 题的 gold answer 与出处。
*理由*:这 7 道答错直接关联安全后果;而隔 48 小时后你已不记得当时的推理路径,接近一个新读者。

**② 反向检索自检(机器代替人)**
拿每道题的 `gold_answer` 当查询去语料检索,看能否命中它自己的 citation。
命不中的题要么 gold 抄错了,要么 citation 指错了。**纯机械,可以塞进回填脚本顺带做掉。**

**③ 边界诚实标注(把不确定性写下来而不是消灭它)**
ML02(他加禄语)/ ML03(印地语)的措辞无法自审。
**不要因此删掉这两题**,也不要假装它们已核。如实标注,汇报时一并说明。
**把"我不确定这一条"写进报告,比找一个人签字更有信息量。**

> 三条的共同逻辑:**单人项目的质量保证不靠"更多眼睛",靠"更多角度"**——
> 时间差、机器交叉验证、对不确定性的诚实标注。

### 14.7 已知边界(汇报时必须标注)

| 边界 | 汇报写法 |
|---|---|
| 可答题仅 31、计分陷阱仅 7 | "该集合设计用于识别**大效应**与**主要失败模式**" |
| SMM 半本(TR02 已排除) | "已知缺失章节不计入拒答分母" |
| 跨文档 / 多语种各 3 道 | "样本小,仅供参考,不做结构性结论" |
| ML02/ML03 措辞未经母语者核 | 如实说明 |

**这 39 题不是最终 benchmark**,应称为 **M1/M2 决策诊断集**。
不要说"39 题证明 A 比 B 好",要说:

> "在当前 39 题诊断集上观察到 X。该集合用于识别大效应和主要失败模式;
> 小效应与细分类别的结论需要后续扩展评测验证。"

**明确说出自己边界的结果,比宣称全面的结果更难被推翻。**

- **评测集未被设计为系统覆盖解析层的 body-quality 与 completeness failure。**
  现有 gold 分布对这两类 failure 的覆盖为**零或极弱**。
  因此 M2 的结果**不得**被解释为「系统在全语料上的 ingestion completeness 已验证」。
  M2 的主要作用域是：**当前 gold 所覆盖、且已进入 corpus 的知识上的
  RAG × fine-tuning 架构对照**。
- **question anchors 的 provenance 尚未证实。**
  若 anchors 从 parser 输出生成，则解析失败的页产不出锚点、不会出题。
  该假设记为 `selection-bias hypothesis remains unresolved`。
- **page-content-completeness failure 为已知 residual risk。**
  16 个 confirmed examples 跨 6 本手册；28 个 gold page 中 2 个受影响。
  受影响的 4 条 citation 其 quote fragments **10/10 命中 corpus**，故当前 gold 仍可答。
  **⚠️ 该 10/10 可能部分受评测集构造方式影响，不完全是独立证据。**

---

## 15. 技术栈决策

| 角色 | 选择 | 为什么 |
|---|---|---|
| Mac 推理 | Ollama(MLX 后端)/ MLX-LM | MLX 是 Apple Silicon 上最快的方案 |
| Mac 微调 | **MLX-LM LoRA** | 无 CUDA → Unsloth 核心跑不了。MLX-LM 原生最稳 |
| 合成数据 teacher | **本机大模型**(§12.4) | 96 GB 跑得动;**文档不出机器**(A5) |
| 文档解析 | **poppler `pdftotext -layout`** | 版式规整且有文本层,**零 Python 依赖**——对船端是实打实的减负 |
| 船端运行时 | **llama.cpp + GGUF** | MLX 不跨平台,必须迁移 |

### 15.1 ⚠️ Kiln 做不了检索与评测

**调研发现**:Kiln 的 RAG 流程是**它自己的 extractor 把 PDF 转文本、自己分块**,
没有"喂入已分好的 chunk"这条路径。两个后果都是硬的:

| 后果 | 严重性 |
|---|---|
| `gold_chunk_ids` 在 Kiln 里没有对应物——它的 chunk 是另一批对象。**在 Kiln 里算的 Recall 测的是另一个系统** | 🔴 M1c 不能用 Kiln |
| 引用三元组产生不出来——`(doc_id, section, pdf_page)` 是我们的解析器从页眉抠出来的,Kiln 的 extractor 不保留它 | 🔴 **Citation First 这条 L1 在 Kiln 里无法实现** |

**→ M1c 与 M2 的评测必须自己写。**

#### 那还留不留 Kiln?这必须是显式决定,不是继承

若把 Kiln 缩小为"合成数据 + LoRA":LoRA 本来就走本地 MLX-LM;
合成数据生成 = 循环调本地 teacher ≈ 50 行 Python。
**那 Kiln 实际只剩一个数据集管理的图形界面**,代价是多一个应用、更新与遥测面、
source-available 许可、每次都要复测的断网验证。

> **注意一致性**:项目已用"每个依赖都是故障点"否掉了向量库的全文索引、改用纯 Python BM25。
> **同一条逻辑对 Kiln 同样成立**——依赖成本不分岸船。

三个选项:仅用于合成数据的人工审阅 / 仅用于 LoRA 运行管理 / **不用,全部自建**。
无论选哪个,记进 `DECISIONS.md` 并写 `falsified_if`。

### 15.2 ⚠️ M1c 的向量检索:不上向量库

**3309 chunks × 1024 维 × 4 字节 = 13.55 MB。** 全量 cosine 扫描不是规模瓶颈。

M1c 要回答的是**"向量检索有没有价值"**,不是"哪个向量库最快"。
上向量库会额外引入 Python 依赖、Arrow、索引格式、持久化兼容性、
以及**船端部署时又一个要验证的东西**——而这些买不到任何 M1c 需要的答案。

**这与本方案自己的原则一致**:架构须假设未来 10–100 倍规模,**但现在不为此优化**。

三个必须一并定的实现细节:

**① numpy 算不算依赖** — 建议 M1c 放行。理由:向量路径本来就需要一个 embedding 模型运行时,
"零依赖"在这条路上不可能达成;numpy 是范围明确、接口稳定的数值库,与带索引格式和
持久化语义的向量库不是一个量级。**但这是 M1c 的实验裁定,不是船端架构决定。**

**② embedding 落点** — 去掉向量库后必须明确,否则 `IndexManifest` 的启动完整性校验
也一并没了。建议 `.npy` + 同名 manifest 记 embedder 指纹与语料哈希。

**③ 向量与 chunk 的行序对应** — `.npy` 第 i 行必须对应 `chunks.jsonl` 第 i 行,
加载时先比对行数。**这是最容易出、也最难 debug 的一类错**:
向量错了位,检索结果看起来正常但全是乱的,且不报任何错。

### 15.3 ⚠️ 实验实现 ≠ 船端架构

**M1c 用 Ollama 的 bge-m3 做 embedding,是实验后端,不是船端 runtime 决定。**

否则会重复犯已经犯过一次的错:**"Ollama 能跑 ≠ llama.cpp 能跑"**——
`qwen3.5:4b` 就是这么被淘汰的。embedding 模型同样要过 GATE-2 才能上船。

**在 M6 之前,不把任何实验后端写成架构事实。**

---

## 16. 显式失败面

*应对约束*:**A4(船上无 IT 支持)**。系统必须在自己出问题时给船员一个明确、可理解的状态。

| 失败点 | 检测时机 | 约定行为 |
|---|---|---|
| 索引包损坏 | **启动时** checksum | **拒绝启动**并明确报错,绝不静默用坏索引 |
| Embedding 模型与索引不匹配 | **启动时**指纹校验 | **拒绝启动**。最难 debug 的故障,必须在启动挡住 |
| 向量行数与 chunk 数不符 | **启动时** | **拒绝启动**。向量错位时检索看起来正常但全是乱的 |
| 生成模型加载失败 / OOM | 启动时 | **降级为纯检索模式**,界面提示"当前仅显示原文" |
| 生成超时 | 运行时 | 中断,返回原文片段,标记 `degraded` |
| 检索最高分 < 阈值 | 运行时 | **拒答**,不喂空上下文 |
| 单份文档解析失败 | 建索引时 | 跳过 + 记入失败清单,**不中断整批** |
| 磁盘满,日志写不进 | 运行时 | 日志静默丢弃,主流程不受影响 |

**两条贯穿原则**:
1. **能在启动时挡住的,绝不留到运行时。**
2. **运行时的失败,一律有降级路径而不是白屏。**

---

## 17. 风险登记

| # | 风险 | 状态 | 应对 |
|---|---|---|---|
| 1 | Mac 性能在船上兑现不了 | 未缓解 | 每配置额外测 CPU-only 作悲观下界;预算按保守值 |
| 2 | 模型 llama.cpp 不兼容 | ⚠️ **已发生** | GATE-2 提为前置门;qwen3.5 已淘汰 |
| 3 | prefill 拖垮延迟 | ✅ 已缓解 | 上下文降至 1050;测 TTFT 而非 tok/s |
| 4 | 云端微调导致文档出境 | ✅ 已缓解 | 本地 MLX-LM + 本地 teacher |
| 5 | 评测集太窄致结论不可靠 | 部分缓解 | 陷阱 ≥8;边界如实标注 |
| 6 | 文档解析质量差 | 部分缓解 | 9/9 成功、元数据 100%;**人工抽检仅个位数,需补到 ≥200** |
| 7 | 模型许可证不允许商用 | ⚠️ **冲突已浮出** | §12.2 先拿数据再谈政策 |
| 8 | 规模上去后问题爆发 | 未缓解 | 工作量约规模的 **1.5 次幂**,不做线性外推 |
| 9 | 决定未入库 | ⚠️ **已发生三次** | 流程见 `DOCUMENT_MAP.md` |
| 10 | 未来语料格式完全不同 | 未发生 | 解析器按版式命名;新格式写新 parser |
| 11 | 单一 teacher 偏好被蒸馏进 student | 未发生 | 双 teacher 交叉验证;judge 与 student 不同家族 |
| 12 | **judge 影响大过被测模型** | ⚠️ 已识别 | M2 改人工盲评,把 judge 变量整个消掉 |
| 13 | **判分者即方案设计者,已有立场** | ⚠️ 已识别 | 强制盲评 + 隔 24h 复判测一致率 |
| 14 | **McNemar 需要单题粒度数据** | ⏳ | 结果记到单题粒度。**漏了 M2 跑完也算不出检验** |
| 15 | **评测集统计功效不足** | ⚠️ 已量化 | 预注册"只找大效应";汇报标注此限制 |
| 16 | **上下文预算与 TTFT 预算矛盾** | ⚠️ 已量化 | §10.2 三选一,推荐收紧到 1050 |
| 17 | **token 估算误差静默超预算** | ⏳ | 加 10% 余量 + 记录实际 token 数 |
| 18 | **采样噪声混入臂间差异** | ⏳ | `temperature=0`;先测两次输出是否逐字节相同 |
| 19 | **四臂 prompt 天然不同** | ⚠️ 无法消除 | 压到最小 + 预注册披露;汇报重量放 D−B / D−C |
| 20 | **Kiln 做不了检索与评测** | ⚠️ **已确认** | M1c/M2 自建;Kiln 还留不留需显式决定 |
| 21 | **记录轴断档**(历史 185 行未迁移) | ⚠️ **已发生** | 见 `DECISIONS.md` 起始声明;**禁止凭摘要反推重建** |
| 22 | 拒答阈值过拟合只披露未量化 | ⏳ | 加留一交叉验证,把"偏乐观"变成一个数字 |
| 23 | **分发渠道失败被当成模型失败** | ⚠️ **已发生** | acquisition 与 gate 字段分离;GATE-2 接受三条输入路径(§12.5) |
| 24 | **GATE-2 的 FAIL 被外推为架构永久不兼容** | ⏳ | 每条结果记 runtime + artifact 哈希;runtime 升级后重测(§12.1) |
| 25 | **候选池调查阻塞取证路径** | ⏳ | S4a / S4b 切分;M2 只需一个合法载体(§12.6) |
| 26 | **embedder 的船端可行性与跨语言检索能力未验** | ⏳ | GATE-E1~E3(§12.7);多语种题的检索 Recall 在 M1c 单独报 |
| 27 | **teacher 的许可限制其输出用于训练模型,义务附着到要上船的 LoRA adapter** | ⏳ | GATE-T1(§12.4);M2 首轮用 Apache 2.0 的 `qwen3:32b` 单 teacher;合成数据文件记 teacher 来源 |
````
 
**同时修订现有第 2 行**（模型 llama.cpp 不兼容）的应对栏：
 
````
GATE-2 提为前置门;qwen3.5 的 FAIL 限定于当时的 artifact + runtime,
llama.cpp 升级后需重测(见 §12.1 作用域规则)

---

## 18. 分工原则

| 角色 | 负责 | 不负责 |
|---|---|---|
| **Arya** | 全部 L1 架构决策 · 评测集题目与标准答案 · 判分(盲评)· 拒答阈值取值 · "不做什么"清单 · 缺陷归因 | — |
| **方案层对话** | 拆解问题 · 设计实验 · 审查代码与结果 · 判断外部意见 · 维护本文件与决策日志 | 不生成大段代码 |
| **实现层(Claude Code)** | 按契约生成组件代码 · 机械任务 | **不碰 `contracts.py` · 不碰评测集内容 · 不定阈值 · 不判分** |

**绝对不交给 AI 的四样**:`core/contracts.py` 本身 · 评测集的题目与标准答案 ·
拒答阈值的取值 · "这一版不做什么"清单。**这四样是判断,不是生成任务。**

### 18.1 这一版明确不做什么

图片 / OCR / 图表理解 · 多轮对话与记忆 · 用户账号与权限 · agent / 工具调用 / 多步推理 ·
增量索引更新(M6 前一律全量重建)· 多语言界面 · 联网检查更新 · 多用户并发 ·
流式输出 · 云端微调 · 独立 reranker(待 M1c 数据)· 向量库(M1c 用穷举 cosine)。

**纪律**:任何人(包括自己、包括 AI 主动加上的)提议加这些 → **记进 `BACKLOG.md`,不进这一版**。
AI 生成时自作主张加了不在清单里的功能,**删掉**。

---

*v0.12。本文件只讲"为什么与是什么";"怎么做"见 `执行手册.md`,
"每个文件干什么"见 `DOCUMENT_MAP.md`,数据结构与阈值见 `core/contracts.py`。*