# 执行手册 v4 · 船载离线文档问答系统

> **本手册跟随 `DECISIONS.md`，不产生新决定。** 两者冲突时以 DECISIONS 为准。
> **章节状态标记**：⭐ 已完成 checkpoint · 🟡 并行不阻塞 · ⬛ 当前主线


---

## 目录
 
| 步骤 | 做什么 | 在哪 | 里程碑 | 预计 | 阻塞谁 |
|---|---|---|---|---|---|
| **S0** | Mac 基础环境自检与补齐 | Terminal | M0 复核 | 30–60 min | S1 |
| **S1** | 建仓库骨架 + 归位已有文件 | Terminal | M0 收口 | 20 min | S2 |
| **S2** | 安装 Claude Code + 写 `CLAUDE.md` | Terminal | — | 20 min | S5 |
| **S3** | 契约会（你自己，不开 Claude Code） | 编辑器 | 契约冻结 | 2 h | **S5 起全部** |
| **S4a** | **M2 主模型就绪**（GATE-1 → GATE-2 → license pre-check） | Terminal | M0 补齐 | 半天 | **S5** |
| **S4b** | M3 候选空间调查 | 桌面调研 + Terminal | M3 前置 | 并行，不设截止 | **谁都不阻塞** |
| **S5** | 任务 1：Parser + 语料构建 | Claude Code | **M1a** | 半天 | S6 |
| **S6** | 任务 2：gold chunk 映射 | Claude Code | **M1b 收口** | 1 h | S8 |
| **S7** | 任务 3：GATE-2 批量脚本化 | Claude Code | 服务 S4b / M3 | 半天 | **不阻塞** |
| **S8** | 任务 4：检索层 + 检索基线 | Claude Code | **M1c** | 1 天 | S9 |
| **S9** | `MIN_RELEVANCE` 校准 | Terminal + 你判断 | M1c 收口 | 半天 | S10 |
| **S10** | 冻结评测集 + 写 M2 预注册 | 编辑器 | M2 前置 | 半天 | S12 |
| **S11** | Kiln 装机 + 合成数据 + LoRA | Kiln | M2 准备 | 1–2 天 | S12 |
| **S12** | 跑 M2 四臂 + 盲评 | Claude Code + 你判分 | **M2** | 2–3 天 | — |

---

## 环境地图（一次性说清"地方"）

| 地点 | 怎么打开 | 只做什么 | 绝不做什么 |
|---|---|---|---|
| **Terminal** | 聚焦搜索 `Terminal` / 或 iTerm | 装环境、跑脚本、git、ollama、llama.cpp | — |
| **Claude Code** | Terminal 里 `cd ~/ship-rag && claude` | 按任务边界生成代码、跑脚本、贴输出 | 改 `contracts.py` / 改评测集内容 / 定阈值 / 判分 |
| **编辑器**（VS Code / 任意） | `code ~/ship-rag` | 你自己改契约、改评测集、写预注册、写 DECISIONS | — |
| **Kiln 桌面 app** | 应用程序里双击（S11 才装） | 合成数据生成、LoRA 微调 | **不做检索基线、不做引用评测**（原因见 S8 开头） |
| **Ollama** | 菜单栏羊驼图标；服务在 `localhost:11434` | 模型后端 | — |
| **本对话（Claude 审核脑）** | 浏览器 | 结果解释、决策审核、判断外部意见 | 不生成大段代码 |

**⚠️ Claude Code 必须是本机安装的版本，不能用网页版。**
网页版跑在云端沙箱：要它读 KAIVA 手册就得把 46 MB 公司 PDF 传上云——**直接违反约束 A5（公司文档不能离开本机）**；而且它够不着本机的 `ollama` / `llama-bench` / `pdftotext`，S5 和 S7 根本跑不了。

---

## 决策日志怎么落实（贯穿全程，先读这一节）

**问题**：你的 `DECISIONS.md` 上一次更新是 08-31，中间做的决定都散在对话里。风险登记 #10 已经标了"已发生两次"。

**解法：把"写日志"绑进每一步的验收，而不是靠自觉。**

### 三条铁律（v0.8 §20.2）

1. **只追加。** 结论变了写新条目说明"推翻 X 月 X 日的某决定，原因是…"，不回去改旧的。
2. **决策必须带依据。** "决定用 X" 不够，要有 "因为实测 Y = Z"。
3. **待办要写进去。** 未完成项列在条目末尾，下次翻日志能接上。

### 条目格式（v0.8 §20.5，从本版起一律带级别）

```markdown
## YYYY-MM-DD 补充 N · 一句话标题

### [L1] 决策标题
- falsified_if: 什么情况下这条会被推翻
- 事实: （带数字）
- 依据: （实测命令或输出）
- **决策: ...**
- 待办: ...

### [L2] 决策标题
- owner_experiment: 哪个实验会重新校准它
- 依据: ...
- **决策: ...**

### [L3] 决策标题
- （无强制字段）
```

| 级别 | 含义 | 强制字段 |
|---|---|---|
| **L1** 架构决策 | 几乎不能改，改一次整个项目要重新解释 | `falsified_if:` |
| **L2** 实验参数 | 允许被实验推翻，替换是正常的 | `owner_experiment:` |
| **L3** 实现细节 | 重构不影响结论 | 无 |

### 落地机制：一个 shell 函数

S1 里会把这段加进 `~/.zshrc`。以后在任何步骤结束时敲 `dlog`，它会自动往 `DECISIONS.md` 追加一个带日期的模板并打开编辑器：

```bash
dlog() {
  local f=~/ship-rag/DECISIONS.md
  local d=$(date +%F)
  local n=$(grep -c "^## $d" "$f" 2>/dev/null)
  n=$((n+1))
  cat >> "$f" <<EOF

## $d 补充 $n · <一句话标题>

### [L?] <决策标题>
- falsified_if / owner_experiment:
- 事实:
- 依据:
- **决策:**
- 待办:
EOF
  ${EDITOR:-open -e} "$f"
}
```

**纪律：本手册每一步的验收清单里都有"本步应入库的条目"。那一项没打勾，这一步不算完成。**

---

# S0 · Mac 基础环境自检与补齐

> **在哪**：Terminal
> **里程碑**：M0 复核
> **原则**：先查后装。已经装好的不要重装，重装会打乱 digest 记录。

### S0.1 逐项自检

**一次全跑完，把输出留着**：

```bash
echo "=== 系统 ==="
sw_vers
uname -m                       # 期望 arm64

echo "=== Homebrew ==="
brew --version                 # 没有 → 见 S0.2-①

echo "=== Git ==="
git --version

echo "=== Python 3.12 ==="
/opt/homebrew/bin/python3.12 --version    # 没有 → S0.2-②

echo "=== poppler（解析器唯一外部依赖）==="
pdftotext -v 2>&1 | head -1    # 没有 → S0.2-②
pdfinfo -v 2>&1 | head -1

echo "=== Ollama ==="
ollama --version               # 没有 → S0.2-③
curl -s localhost:11434/api/tags >/dev/null && echo "Ollama 服务在跑" || echo "Ollama 服务没起"

echo "=== llama.cpp（GATE-2 依赖）==="
which llama-bench llama-cli    # 没有 → S0.2-④

echo "=== 磁盘 ==="
df -h ~ | tail -1              # 需要 ≥100 GB 可用
```

### S0.2 缺什么补什么

**① Homebrew**（其他全部依赖它）

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
# 装完按提示把 brew 加进 PATH，通常是：
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

> ⚠️ 公司 MDM + 代理环境下 brew 可能被拦。如果 `curl` 超时，先确认代理设置：
> `echo $HTTP_PROXY $HTTPS_PROXY`。拦了就找 IT，不要绕过公司代理。

**② Python 3.12 + poppler**

```bash
brew install python@3.12 poppler
pdftotext -v && pdfinfo -v     # 验证
```

> 为什么要 3.12：macOS 自带 3.9 太老，MLX 需要 3.11+。

**③ Ollama**

```bash
brew install --cask ollama
open -a Ollama                 # 启动，菜单栏出现羊驼图标
curl -s localhost:11434/api/tags | python3 -m json.tool | head
```

**④ llama.cpp**

```bash
brew install llama.cpp
llama-bench --help | head -3
llama-cli --help | head -3
```

> 这两个命令是 GATE-2 的核心。**没有它们，S7 整步做不了。**

### S0.3 ✅ S0 验收清单

- [ ] `sw_vers` 显示 macOS 13.0 以上
- [ ] `brew --version` 有输出
- [ ] `/opt/homebrew/bin/python3.12 --version` 有输出
- [ ] `pdftotext -v` 和 `pdfinfo -v` 都有输出
- [ ] `ollama --version` 有输出，且 `curl localhost:11434/api/tags` 不报错
- [ ] `which llama-bench llama-cli` 两个路径都打出来了
- [ ] `df -h ~` 显示可用空间 ≥100 GB
- [ ] **本步应入库的条目**：`[L3] 环境自检` —— 记下各工具的版本号（brew / python / poppler / ollama / llama.cpp）。这份记录就是 `ENVIRONMENT.md` 的第一笔

---

# S1 · 建仓库骨架 + 归位已有文件

> **在哪**：Terminal
> **里程碑**：M0 收口
> **前置**：S0 全绿

### S1.1 目录与 Git

```bash
# 如果 ~/ship-rag 已存在且里面有那 6 个文件，先备份一份到别处再继续
mkdir -p ~/ship-rag && cd ~/ship-rag
ls -la                          # 确认现在有哪些文件

git init
git branch -M ship-rag          # 用 ship-rag 作主分支名，与你 GitHub 上的一致

mkdir -p core components/{parsers,chunkers,embedders,retrievers,rerankers,generators} \
         ingest serve eval experiments scripts \
         raw corpus index models
```

### S1.2 `.gitignore`

**判断标准**：能一条命令重新生成的 → 不进 Git，但生成脚本必须进。
**唯一例外**：`eval/testset_*.jsonl` 是人工产物、不可再生，**必须**进 Git。

```bash
cat > .gitignore <<'EOF'
# 判断标准：能一条命令重新生成的 → 不进 Git，但生成脚本必须进。
# 唯一例外：eval/testset_*.jsonl 是人工产物、不可再生，必须进 Git。

.venv/
__pycache__/
*.pyc
.DS_Store

raw/*
!raw/.gitkeep
corpus/*
!corpus/.gitkeep
index/*
!index/.gitkeep
models/*
!models/.gitkeep
EOF

touch raw/.gitkeep corpus/.gitkeep index/.gitkeep models/.gitkeep
```

**立刻验证 ignore 规则真的对**（不要相信"应该对"）：

```bash
git check-ignore -v eval/testset_v5_2.jsonl   # 期望：无输出（= 不被忽略）
git check-ignore -v corpus/chunks.jsonl       # 期望：命中 corpus/* 这一行
git check-ignore -v "raw/KAIVA - Manuals/SMM.pdf"  # 期望：命中 raw/* 这一行
```

### S1.3 归位那 6 个文件

```bash
cd ~/ship-rag

# 假设 6 个文件现在在 ~/Downloads 或 ~/ship-rag 根目录，按实际路径调整
mv 船载离线文档问答系统_实施方案_v0_8.md  实施方案_v0.8.md
mv contracts_建议版.py                   core/contracts.py
mv testset_v5_2.jsonl                    eval/testset_v5_2.jsonl
mv evaluation_set_v5_2.md                eval/evaluation_set_v5_2.md
mv decisions.md                          DECISIONS.md
# KAIVA_-_Manuals.zip 解压到 raw/
unzip KAIVA_-_Manuals.zip -d raw/
rm KAIVA_-_Manuals.zip

ls "raw/KAIVA - Manuals/"*.pdf | wc -l   # 期望：9
du -sh "raw/KAIVA - Manuals/"            # 期望：约 49 MB
```

**⚠️ `raw/` 不进 Git，所以它是唯一不可再生的输入。现在就单独备份一份到仓库之外**（外置硬盘 / 公司网盘）：

```bash
cp -R "raw/KAIVA - Manuals" ~/Documents/ship-rag-raw-backup-$(date +%F)
```

### S1.4 Python 虚拟环境

```bash
cd ~/ship-rag
/opt/homebrew/bin/python3.12 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install mlx mlx-lm lancedb

# 验证 MLX 用上 GPU（必须打印 Device(gpu, 0)）
python -c "import mlx.core as mx; print(mx.default_device())"
```

> 注意：`ingest/build_corpus.py` 和 `components/parsers/kaiva_pdf.py`（S5 产出）**只用标准库 + poppler 命令行**，不依赖上面这些包。装 mlx/lancedb 是给 S8 和 S11 用的。

### S1.5 便利别名 + 决策日志函数

```bash
cat >> ~/.zshrc <<'EOF'

# --- ship-rag ---
alias shiprag='cd ~/ship-rag && source .venv/bin/activate && export PYTHONPATH=$(pwd)'

dlog() {
  local f=~/ship-rag/DECISIONS.md
  local d=$(date +%F)
  local n=$(grep -c "^## $d" "$f" 2>/dev/null)
  n=$((n+1))
  cat >> "$f" <<TPL

## $d 补充 $n · <一句话标题>

### [L?] <决策标题>
- falsified_if / owner_experiment:
- 事实:
- 依据:
- **决策:**
- 待办:
TPL
  ${EDITOR:-open -e} "$f"
}
EOF

source ~/.zshrc
shiprag        # 以后每次开工第一条命令就是它
```

### S1.6 建三个空文件（内容 S1.7 填）

```bash
cd ~/ship-rag
touch BACKLOG.md ENVIRONMENT.md README.md
```

**`BACKLOG.md` 第一批内容**（这些是已经识别但明确不在本版做的）：

```bash
cat > BACKLOG.md <<'EOF'
# BACKLOG

这一版不做、但记下来的想法。加入规则见实施方案 §21。

## M5 阶段做
- 扩充陷阱题至 ≥15 道（当前计分陷阱仅 7 道，无法做率统计）
- 真正的多语种能力评估（当前评测集仅 3 道多语种题，撑不起 GATE-4）
- 若 C 臂表现好：另建一批"训练时排除源 chunk"的题做泛化验证
- 人工抽检 ≥200 个 chunk（当前只抽检了个位数）

## 待定
- 独立校准集（20–30 题，只标"语料里有/没有"），用于避免在评测集上校准 MIN_RELEVANCE
EOF
```

**`ENVIRONMENT.md` 第一笔**（把 S0.1 的自检输出填进去）：

```bash
cd ~/ship-rag && source .venv/bin/activate
{
  echo "# ENVIRONMENT"
  echo
  echo "环境快照。依赖变更时更新。记 digest 不记 tag。"
  echo
  echo "## $(date +%F)"
  echo
  echo '```'
  sw_vers | head -2
  echo "brew      $(brew --version | head -1)"
  echo "python    $(python --version)"
  echo "poppler   $(pdftotext -v 2>&1 | head -1)"
  echo "ollama    $(ollama --version 2>&1 | head -1)"
  echo "llama.cpp $(brew list --versions llama.cpp)"
  echo '```'
  echo
  echo "## pip freeze"
  echo '```'
  pip freeze
  echo '```'
} > ENVIRONMENT.md
```

**`README.md`**：

```bash
cat > README.md <<'EOF'
# ship-rag

**一句话定位**：这个系统是一个离线的、带出处的文档导航器。
它把船员的问题变成"文档里的哪一段"；生成的自然语言只是那段依据的摘要，不是答案本身。

## 目录

| 路径 | 是什么 | 进 Git |
|---|---|---|
| `core/contracts.py` | ⚠️ 唯一可信来源。数据结构 + 阈值 + 接口。改它要走变更仪式 | ✅ |
| `components/` | 可替换组件。⚠️ 各子目录之间互不 import，只 import core.contracts | ✅ |
| `ingest/` | 岸端：把文档变成索引包 | ✅ |
| `serve/` | 船端：查询服务（M6 才写） | ✅ |
| `eval/testset_*.jsonl` | ⚠️ 评测集。人工产物，不可再生，**必须**进 Git | ✅ |
| `eval/results.csv` | ⚠️ 只追加，永不删行 | ✅ |
| `experiments/` | 实验配置与模型台账 | ✅ |
| `scripts/` | 一次性工具脚本 | ✅ |
| `raw/` | ⚠️ 原始 PDF。只读，不进 Git，**必须单独备份** | ❌ |
| `corpus/` `index/` `models/` | 可重新生成 | ❌ |
| `DECISIONS.md` | ⚠️ 决策日志。只追加，永不修改 | ✅ |

## 常用命令

```bash
shiprag                                  # 进目录 + 激活 venv
python3 ingest/build_corpus.py "raw/KAIVA - Manuals" corpus   # 重建语料（S5 后可用）
dlog                                     # 追加一条决策日志
```

## 纪律

契约变更要走仪式（单独会话 + `contract:` 前缀 commit + 同步写 DECISIONS），见 `DECISIONS.md` 与实施方案 §20.3。
EOF
```

### S1.7 首次提交

```bash
cd ~/ship-rag
git add -A
git status --short              # 检查：raw/ 下的 PDF 不应出现
git commit -m "chore: 初始化仓库结构、评测集入库、环境快照"
```

**接 GitHub**（如果 `yangyw2023/-` 已存在）：

```bash
git remote add origin https://github.com/yangyw2023/-.git
git fetch origin
git push -u origin ship-rag
```

### S1.8 ✅ S1 验收清单

- [ ] `ls ~/ship-rag` 能看到：`core/ components/ ingest/ serve/ eval/ experiments/ scripts/ raw/ corpus/ index/ models/ .gitignore README.md DECISIONS.md BACKLOG.md ENVIRONMENT.md 实施方案_v0.8.md`
- [ ] `ls "raw/KAIVA - Manuals/"*.pdf | wc -l` = **9**
- [ ] `git check-ignore -v eval/testset_v5_2.jsonl` **无输出**（唯一例外生效）
- [ ] `git check-ignore -v corpus/chunks.jsonl` **有输出**（被正确忽略）
- [ ] `git ls-files eval/` 能看到 `testset_v5_2.jsonl`
- [ ] `python -c "import mlx.core as mx; print(mx.default_device())"` 打印 `Device(gpu, 0)`
- [ ] `raw/` 已备份到仓库之外
- [ ] 新开一个终端敲 `shiprag` 能直接进目录且 venv 激活
- [ ] **本步应入库的条目**：`[L3] 仓库初始化` —— 记 `.gitignore` 的唯一例外已实测验证；记 `raw/` 备份位置

---

# S2 · 安装 Claude Code + 写 `CLAUDE.md`

> **在哪**：Terminal
> **为什么必须本地装**：见本文开头的环境地图（约束 A5）

### S2.1 安装

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

装完**新开一个终端窗口**（让 shell 读到 PATH 变化），然后：

```bash
claude --version
which claude                    # 期望 ~/.local/bin/claude
```

如果 `claude` 找不到：

```bash
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc
```

### S2.2 登录

```bash
cd ~/ship-rag
claude
```

首次运行会走浏览器 OAuth。**如果默认浏览器是 Firefox 且卡住/报 OAuth 错，换成 Chrome 或 Safari 再试**——这是已知问题（CLI 只监听 IPv6 回环，Firefox 优先走 IPv4）。

### S2.3 写 `CLAUDE.md`（关键：把纪律固化，不用每次贴）

Claude Code 会自动读取项目根目录的 `CLAUDE.md`。**把通用会话前缀写进去，以后每个任务只需要贴任务本身。**

```bash
cat > ~/ship-rag/CLAUDE.md <<'EOF'
# 项目：船载离线文档问答系统（ship-rag）

这是一个 RAG 系统，最终要部署在远洋船舶上离线运行。
心智模型：**离线的、带出处的文档导航器**。生成的自然语言只是依据的摘要，不是答案本身。

## 每次会话必须遵守的工作方式

1. 我给的任务边界是硬约束。只创建/修改我指定的文件，不要顺手改别的。
2. 不要主动添加我没要求的功能。觉得某个功能有用，写在回复里告诉我，不要写进代码。
3. 出错一律抛异常。禁止 except 之后返回空值、默认值或空列表把错误藏起来。
   "没找到"和"崩了"必须是两种可区分的状态。
4. 禁止魔法数字。所有阈值、窗口、超时定义成模块顶部常量，或从 core/contracts.py 引入。
5. docstring 写契约（输入假设、什么情况抛什么异常、返回值语义），不要复述代码逻辑。
   判据：盖住实现只看签名+docstring，另一个人能否正确使用且不踩隐藏假设。
6. 写完之后用真实数据跑一遍，把输出贴给我，不要只交代码。禁止用 fixture 假装跑过。
7. 如果发现我的规格与真实数据不符，停下来告诉我。不要自己"修正"规格去迁就数据，
   也不要修改数据去迁就规格。

8. 【会话开场自检】动手前先跑：
     git status && git log --oneline -3
     git fetch && git log --oneline origin/ship-rag -1
   把 HEAD 与 origin/ship-rag 的关系报给我（领先/落后几个 commit）。
   本地落后就停下来告诉我，不要在旧树上开工。

9. 【自动化 vs 我的指令】如果 stop hook、pre-commit 或任何自动化要求你做的事与我的
   明确指令冲突，以我的指令为准，并把冲突报给我。特别地：不要为了消掉告警而
   git add / commit / 改 .gitignore —— 那是绕过检查，不是解决冲突。

10.【核对的两层】当我让你"核对 X 是否与 Y 一致"时，你只负责第一层（X 是否忠实于 Y）。
   如果你觉得 Y 本身可能有问题，写在回复里告诉我，但不要自己改 Y。
   核对通过不等于 Y 是对的。

11.【不要伪造】找不到的文件就说找不到。缺失的历史不要根据其他文档反推重建。

## 绝对不要碰的东西

| 事项 | 为什么 |
|---|---|
| `core/contracts.py` 的内容 | 唯一可信来源。改它要走变更仪式，只能由 Arya 定 |
| `eval/testset_*.jsonl` 的**内容语义** | 题目、标准答案、required_elements 是判断不是生成 |
| `MIN_RELEVANCE` 等阈值的**取值** | 必须来自实测分数分布 |
| 判分（Correct / Partial / Incorrect） | M2 走人工盲评协议，不交给模型 |
| 判断某个模型该不该淘汰 | 脚本只产出数据，判断在方案层做 |

## 九条全链路语义约定（贴代码时的判据）

1. 分数一律 0.0..1.0，越大越相关。BM25 原始分与向量距离必须在组件内归一化后出口。
2. 空列表 [] 的唯一含义是"确实没检索到"。出错一律抛异常。
3. 拒答不是异常。Answer(refused=True) 是合法的正常返回。
4. pdf_page 从 1 开始。**AI 极易生成成 0-based，会导致所有出处差一页。**
5. 所有 ID 一律 str。
6. 时间一律 float 秒，不混 ms。
7. 语言代码一律 ISO 639-1 小写（en / zh / tl / hi）。
8. 阈值只有 core/contracts.py 这一个定义处。组件内禁止魔法数字。
9. 岸端与船端共用同一份 contracts.py。

## 依赖是树，不是图

    core/contracts.py        ← 谁都依赖它，它谁都不依赖
         ↑
      components/*           ← 各子目录之间【互不 import】
         ↑
      core/pipeline.py       ← 唯一负责组装的地方
         ↑
      ingest/ serve/ eval/

**铁律：任何 component 只能 import core.contracts。**
一旦出现 retrievers/hybrid.py import generators/ollama.py，树变成图，可替换性当场消失。
EOF
```

### S2.4 ✅ S2 验收清单

- [ ] `claude --version` 有输出
- [ ] `which claude` 返回 `~/.local/bin/claude`
- [ ] 在 `~/ship-rag` 里跑 `claude`，登录成功，能进交互界面
- [ ] `~/ship-rag/CLAUDE.md` 存在
- [ ] 在 Claude Code 里问一句"你读到 CLAUDE.md 了吗？复述第 4 条语义约定"，它能答出 "pdf_page 从 1 开始"
- [ ] `git add CLAUDE.md && git commit -m "chore: 添加 CLAUDE.md 项目常驻指令"`

---

# S3 · 契约会（你自己做，不开 Claude Code）
# S3 · 契约冻结 ⭐ 已完成 checkpoint

> **状态**：contracts **v0.2.0 已冻结**（2026-09-08）。
> **本节保留为历史 checkpoint 与后续契约变更的规则来源，不是待执行任务。**

## S3.0 当前契约状态

- `CONTRACTS_VERSION = "0.2.0"`
- 文件底部**不得**存在「待拍板事项」节 —— 冻结的契约里留着"待拍板"是自相矛盾的
- 后续任何契约修改走 §20.3 变更仪式，用**独立的 `contract:` commit**

## S3.1 契约变更的提交纪律


git add core/contracts.py
git diff --cached --name-only        # ⚠️ 必须看到 core/contracts.py
git commit -m "contract: <改了哪几个常量，各自的新值与依据>"


> **⚠️ `git diff --cached --name-only` 不是可选步骤。**
> `contract:` 前缀是仪式标记；一笔不含契约文件却带该前缀的提交，
> 会让 `git log --grep="^contract:"` 检索出与事实不符的记录。

> **在哪**：编辑器
> **里程碑**：契约冻结 —— **这一步阻塞 S5 以后的全部工作**
> **为什么不开 Claude Code**：实施方案 §20.3 明写"单独一次工作会话，不与实现组件混在一起"。
> 而且 §22 把 `contracts.py` 列为"绝对不能交给 AI"的四样之一。

### S3.1 逐条拍板（8 项）

打开 `core/contracts.py`（就是那份 `contracts_建议版.py`）。底部有一节「本文件不包含什么」，
再往上通读一遍。**每一条你读不懂来源或不同意理由的，直接改回或删掉**——
被拒掉的部分保持原样，比接受一个你没吃透的设计更安全。

需要你确认的 8 项：

| # | 项 | 建议 | 你的决定 |
|---|---|---|---|
| 1 | 引用三元组 `(doc_id, section, pdf_page)` | 确认。评测集 39 条 citations 已全部采用，事实上已绑定 | ☐ |
| 2 | `MIN_RELEVANCE = 0.35` 移进「未校准」节 | 接受。视觉隔离比注释更难被忽略 | ☐ |
| 3 | `EvalItem` 纳入契约 | 纳入。三个消费方，单点定义有价值 | ☐ |
| 4 | `IndexManifest` 补 `chunker_config` | 补。M5 消融会产生多个索引包，没这字段分不清 | ☐ |
| 5 | `split_quote_fragments()` / `normalize_text()` / `is_sole_match_eligible()` | 加。否则回填脚本和评测脚本各实现一遍，必然漂移 | ☐ |
| 6 | `gold_chunk_ids` 移出 `EvalItem`，改 `GoldChunkMap` | 接受（v0.8 §15.4 已定方向） | ☐ |
| 7 | `EvalItemResult` + `RESULTS_COLUMNS` | 接受。**没有它 M2 跑完算不出 McNemar** | ☐ |
| 8 | `TOP_K_CONTEXT` 改 5 + `pack_context()` 按 token 预算填充 | 见下方 ⚠️ | ☐ |

**⚠️ 第 8 项要专门看一眼**，因为它改的是行为不只是注释：

原推导"1500 ÷ 中位 292 token ≈ 3 个 chunk"算错了（1500÷292 ≈ **5.1**）。真正得出 3 的是**最坏情况**（max chunk 1893 字符 ÷ 4 ≈ 473 token，1500÷473 ≈ 3.2）。

| 方案 | 最坏情况 | 中位情况 | CD01（gold 3 chunk 跨 2 手册） |
|---|---|---|---|
| 固定 k=3 | 3×473=1419 ✅ | 3×292=876，**浪费 624 token** | 零余量 |
| k≤5 + 按预算填充 | 停在 3 个，1419 ✅ | 装 5 个，1460 ✅ | 有余量 |

**连锁要求**：接受第 8 项后，S8 的检索基线要同时输出 **Recall@3 和 Recall@5**（因为实际进 context 的个数变成了变量），§13.1 的 reranker 判据也随之改。

### S3.2 `TOP_K_CONTEXT` 与检索基线的连锁

`TOP_K_CONTEXT = 5`，`pack_context()` 按 token 预算填充**连续前缀**。

**检索基线输出四档 Recall**：

| 档 | 含义 |
|---|---|
| **Recall@1** | 下界 —— p95 及以上长度的 chunk 只装得下 1 个 |
| **Recall@2** | 当前 provisional 配置下的中位可装数 |
| **Recall@3** | 参考 |
| **Recall@20** | 召回上限 |

**reranker 的判据是 Recall@2 vs Recall@20。**

**⚠️ realized k 是逐题变量，不是常数。**
其分布由 `EvalItemResult.n_chunks_in_context` 记录 —— **测量结果，不是设定值**。


### S3.3 建 `eval/results.csv`（表头来自契约，不手打）

```bash
cd ~/ship-rag && source .venv/bin/activate
export PYTHONPATH=$(pwd)
python3 -c "from core.contracts import RESULTS_COLUMNS; print(','.join(RESULTS_COLUMNS))" > eval/results.csv
cat eval/results.csv
```

> 为什么这样建：表头写在 csv 里、字段写在 dataclass 里是两个定义处，必然漂移。
> 从契约生成，两者不可能对不上。

### S3.4 走完变更仪式

```bash
cd ~/ship-rag
git add core/contracts.py eval/testset_v5_3.jsonl eval/results.csv
git commit -m "contract: freeze contracts.py v0.1.0 — 三元组 / gold_chunk_ids 外置 / EvalItemResult / 上下文按预算填充"
git push origin ship-rag
```

然后 `dlog`，把本次的决定写进去。**至少这几条**：

```markdown
### [L1] 引用契约冻结为三元组 (doc_id, section, pdf_page)
- falsified_if: 找到能唯一定位的更简形式
- 事实: KAIVA 手册印刷页码按章节独立编号，"Page 1" 在一份手册里重复出现
- 依据: 2026-08-31 语料勘察；评测集 38 条 citations 已全部采用
- **决策: 三元组唯一定位；printed_page 仅展示，绝不用于定位**

### [L1] gold_chunk_ids 移出评测集，改为派生产物 GoldChunkMap
- falsified_if: 语料与分块配置在项目全周期内都不再变化
- 事实: citations 是人的判断（不可再生）；gold_chunk_ids 是它在某套语料+分块上的投影（可再生）
- 依据: 混在一起的话，M5 一改分块 gold_chunk_ids 全废且无再生路径
- **决策: 映射文件按 (评测集版本, 语料哈希, 解析器名) 命名，脚本可再生**

### [L2] TOP_K_CONTEXT 3 → 5，改为按 token 预算填充
- owner_experiment: M1c（Recall@3 与 Recall@5 的差距出来后重定）
- 事实: 旧推导"1500 ÷ 中位 292 tok ≈ 3"算术不成立（应得 5.1）；得出 3 的是最坏情况 max chunk 473 tok
- 依据: 实测 chunk 字符数 中位 1169 / p95 1491 / max 1893
- **决策: 真正的约束是 MAX_CONTEXT_TOKENS=1500；TOP_K_CONTEXT 降为上限保护，
  由 pack_context() 逐个累加 token 填充**
- 待办: S8 的检索基线必须同时出 Recall@3 与 Recall@5

### [L3] TR01 的 citation 移除，评测集升 v5.3
- 事实: 8 道拒答题里唯独 TR01 带 citation（QMM §16 p.106），实为干扰项来源
- 依据: 与 known_distractor.source 重复编码；回填脚本会给拒答题生成 gold chunk，污染 Recall
- **决策: citations 置空；validate_eval_item() 加硬断言**
- 依据: 改后 citation 总数 39 → 38，与实施方案 §14.3 规格一致
```

### S3.5 ✅ S3 验收清单

- [ ] `core/contracts.py` 里**没有**「待拍板事项」节
- [ ] `python3 -c "import core.contracts"` 不报错
- [ ] `eval/testset_v5_3.jsonl` 校验脚本输出 `39 / [] / [] / 38`
- [ ] `eval/results.csv` 只有一行表头，且是从 `RESULTS_COLUMNS` 生成的
- [ ] `git log --oneline -1` 显示一个 `contract:` 开头的 commit
- [ ] **本步应入库的条目**：上面 4 条已写进 `DECISIONS.md`

---

# S4a · M2 主模型就绪 ⭐ 阻塞 S5
 
# S4a · M2 主模型就绪 ⭐ CLOSED

> **状态**：2026-09-08 关闭。**M2 主模型 = `phi4-mini:latest`。**
> **本节保留为方法与纪律的来源**（新增候选、更换主模型、M3 横评时复用）。

## S4a.0 结论

    model            = phi4-mini:latest
    params_total     = 3.84B                 GATE-1 PASS
    artifact sha256  = 3c168af1dea0a414299c7d9077e100ac763370e5a98b3c53801a958a47f0a5db
    runtime scope    = rt-2026-09-08         GATE-2 PASS
    license_claim    = MIT（官方 LICENSE + 本机 artifact 二次证据）
    gate3_license    = TODO（正式 GATE-3 未执行，须在 M3 / M6 之前完成）

**三条互相独立的选定理由**：
1. 上下文预算：在 TTFT 预算下比另一 GREEN 候选多装约一倍的 chunk
2. MIT license claim
3. 无独立 thinking 模式（`think=True` 被 runtime 拒绝）

**⚠️ 边界**：选定基于 `license_claim`，正式 GATE-3 未执行。
即便该模型后来在 GATE-3 被否，**M2 的架构对照结论仍然成立** ——
需要更换的是船端的那个模型，不是"RAG vs 微调"这个实验问题。

## S4a.1 先记 runtime（这是所有 GATE-2 结果的作用域）
 
```bash
shiprag
set -o pipefail
mkdir -p experiments/gate2_raw

# ⚠️ 一次写完。分两块跑，第二次 tee 会覆盖第一次。
{
  echo "tested_at=$(date -Iseconds)"
  echo "date_utc=$(date -u +%FT%TZ)"
  echo "code_commit=$(git rev-parse HEAD)"

  echo "--- brew ---"
  brew list --versions llama.cpp
  brew list --versions ggml
  echo "note=ggml 在 brew 里独立版本化；本机可能同时存在多个版本，"
  echo "     以 load_backend 日志里的实际加载路径为准"

  echo "--- llama-cli --version（权威：含 build 号与 commit）---"
  llama-cli --version 2>&1 | grep -E "^version:|^built with"

  echo "--- ollama ---"
  ollama --version 2>&1 | head -1

  echo "--- OS ---"
  sw_vers
  uname -m

  echo "--- device scope ---"
  echo "bench_args=-ngl 0 -t 8"
  echo "cpu_only_verified=pending_positive_control"
} 2>&1 | tee experiments/gate2_raw/_runtime.txt

```
 
> **为什么 llama.cpp 与 ggml 都要记**：两者在 brew 里独立版本化，
> 而 qwen3.5 那次的 `rope.dimension_sections` schema 失败是 **ggml 层**的事
> （2026-08-17 补充 3 记的是 `brew ggml 0.20.0`）。
> `llama-cli --version` 打出的 build 号 + commit 是最权威的一条。
 
### S4a.1b ⚠️ 设备作用域：banner 不是证据

`llama-bench` 的 backend banner 会显示 `BLAS,MTL`，**即使加了 `-ngl 0`**。
**backend 库被加载 ≠ 矩阵运算被 offload 到 GPU** ——
llama.cpp 启动时会加载所有可用 backend，不管用不用。

要证明"未 offload"，需要一个**正对照**：


BLOB=$(ollama show <tag> --modelfile | grep "^FROM" | awk '{print $2}')
llama-bench -m "$BLOB" -p 512,1024,1200 -n 32 -ngl 0  -t 8              # 基准
llama-bench -m "$BLOB" -p 512,1024,1200 -n 32 -ngl 0  -t 8 --device none
llama-bench -m "$BLOB" -p 512,1024,1200 -n 32 -ngl 99 -t 8              # ⭐ 正对照


**判据**：`-ngl 99` 显著快于 `-ngl 0` → 旋钮确实能区分 →
前两者的一致性才构成"未 offload"的证据。
若 `-ngl 99` 也一样快 → 旋钮无效 → **整个对比无信息**。

**⚠️ 边界**：`cpu_only_verified: true` 只说明**无 GPU offload**，
不说明这些数字适用于船端。


## S4a.2 GATE-1：先核参数，再谈 GATE-2
 
**已下载的五个里，`qwen3.5:4b` 是稀疏 MoE**（DECISIONS 2026-08-17 补充 3：
Gated DeltaNet + 稀疏 MoE）。按规则二，它必须先核总参数才有资格进 GATE-2。
 
```bash
BLOB=$(ollama show qwen3.5:4b --modelfile | grep "^FROM" | awk '{print $2}')
{
  echo "=== ollama show ==="
  ollama show qwen3.5:4b
  echo
  echo "=== GGUF 元数据（若能加载）==="
  llama-cli -m "$BLOB" -n 0 2>&1 | grep -iE "params|expert|n_expert|model size|arch|type"
} 2>&1 | tee experiments/gate2_raw/qwen3.5__gate1_params.log
```
 
若 GGUF 加载本身就失败（很可能，这正是 M0 的已知问题），
就查官方模型卡的总参数/激活参数声明，记 `evidence_url` + `evidence_date`。
 
| 核查结果 | 处置 |
|---|---|
| 总参数 ≤5B | 正常进 GATE-2，复测有候选评估价值 |
| 总参数 >5B | **`gate1_params: FAIL`**。GATE-2 复测降级为**信息性记录**（关闭 M0 遗留），不再计为候选评估 |
 
**两种情况都值得跑，但记录的性质不同，不要混。**
 
其余四个（qwen3:4b / gemma3:4b / phi4-mini / llama3.2:3b）都是 dense，
参数量与 tag 一致，但**仍要记 `evidence_url`** —— 台账的规矩是状态只能来自可追溯来源。
 
## S4a.3 GATE-2：对 GATE-1 合格的候选跑
 
```bash
shiprag
set -o pipefail
mkdir -p experiments/gate2_raw
 
for TAG in qwen3:4b gemma3:4b phi4-mini:latest llama3.2:3b qwen3.5:4b; do
  SAFE=$(echo "$TAG" | tr ':/' '__')
  LOG="experiments/gate2_raw/${SAFE}.log"
  echo "=========== $TAG ==========="
 
  BLOB=$(ollama show "$TAG" --modelfile 2>/dev/null | grep "^FROM" | awk '{print $2}')
  if [ -z "$BLOB" ]; then echo "NO_BLOB" | tee "$LOG"; continue; fi
 
  {
    echo "tag=$TAG"
    echo "blob=$BLOB"
    echo "size=$(du -h "$BLOB" | cut -f1)"
    echo "sha256=$(shasum -a 256 "$BLOB" | cut -d' ' -f1)"
    echo "magic=$(head -c 4 "$BLOB" | xxd -p)"          # 期望 47475546
    echo
    llama-bench -m "$BLOB" -p 512,1024,1200 -n 32 -ngl 0 -t 8 ; rc=$?
    echo
    echo "llama_bench_exit_code=$rc"
  } 2>&1 | tee "$LOG"
done
```
 
**三条参数一个都不能省**：
 
| 参数 | 为什么 |
|---|---|
| `-ngl 0` | 不用 GPU。用 GPU 测出来的数字对船端没有意义 |
| `-t 8` | Mac 是 28 核，船端不会有。用默认会让数字严重乐观 |
| `-p 512,1024,1200` | 1200 贴近当前上下文预算区间。4096 那一档 M0 已测过并记录了超线性恶化，不必每个模型重来 |
 
**为什么在花括号内取 `rc=$?` 而不是用 `$pipestatus`**：
`{ ...; } 2>&1 | tee "$LOG"` 的 pipeline 第一个元素是花括号组，不是 `llama-bench`。
`$pipestatus[1]` 拿到的是花括号组的退出码（恰好等于其最后一条命令，所以结果对，但那是巧合）。
既然这条规则的目的就是"失败不能被伪装成成功"，这个保证不该建立在一个微妙的 shell 语义上。
直接在组内取 `$?` 无歧义，bash / zsh 都一样。
 
**没有 `set -e`** 是刻意的：单个模型失败要**可见**（有 exit_code 落盘），但**不杀整批**。
 
## S4a.4 失败的单独诊断（保留完整上下文，不 grep）
 
```bash
TAG=qwen3.5:4b
BLOB=$(ollama show "$TAG" --modelfile | grep "^FROM" | awk '{print $2}')
llama-cli -m "$BLOB" -ngl 0 -t 8 -n 1 -p "hi" \
  2>&1 | tee experiments/gate2_raw/qwen3.5__diagnostic.log
```
 
**不要只 `grep error`。** 判 FAIL 还是 INCONCLUSIVE 需要完整错误上下文。
 
## S4a.5 许可 pre-check（不是 GATE-3）
 
对**准备成为 M2 主模型**的那一个（只对这一个）做：
 
1. 打开**模型所有者官方来源**（不是镜像、不是聚合站）
2. 记下许可声明原文、URL、查证日期
 
```yaml
license_claim:
  text: "Apache 2.0"
  source_url: "..."
  checked_date: 2026-09-07
gate3_license: TODO        # ⚠️ 正式核验尚未进行
```
 
**⚠️ 这不是 GATE-3 PASS。** 写进 DECISIONS 时必须带上这句：
 
> M2 主模型的选定基于 `license_claim`，正式 GATE-3 未执行。
> **即便该模型后来在 GATE-3 被否，M2 的结论仍然成立** ——
> M2 是 RAG × 微调的架构对照，不是选型实验；需要更换的是船端的那个模型，
> 不是"RAG vs 微调"这个判断。正式 GATE-3 应在 M3 / M6 之前完成。
 
不写这句，pre-check 就是一个默默的抄近路；写了，它才是一个有明确边界与后果的选择。

**本机可验证的第二来源**（可追溯性优于外部网页转述）：


for T in <候选 tag 列表>; do
  echo "===== $T ====="; ollama show "$T" | sed -n '/License/,$p'
done | tee experiments/gate2_raw/_license_from_ollama.txt


> 官方仓库 LICENSE 为**主证据**，本机 artifact 的 License 段为**辅助 provenance**。
> 两者都记，注明哪个是主。
 
## S4a.6 选定 M2 主模型
 
**门在前，性能在后 —— 但性能不只是"并列时的加分项"。**

许可 AMBER 的候选**不进 tie-break**，哪怕它最快。这条不变。

**在多个 GREEN 候选之间，prefill 性能是【结构性决定项】**：
它决定 TTFT 预算下能装几个 chunk，进而决定 B/D 臂的上下文、
跨文档题是否可答、以及检索基线报哪几档 Recall。

tie-break 要**带着这个后果**做，不能当成无结构影响的末位排序。

### S4a.6b 语言 smoke test ——【不是】GATE-4

**定位**：在已通过 GATE-1 / GATE-2 / pre-check 的候选之间提供 **tie-break 证据**。
**不改变 S4a 的关闭判据。正式 GATE-4 按实施方案原定位置执行。**

**判据在看输出前冻结**：「实质性回答是否使用提问所用的语言」YES / NO；
不评事实正确性；混合输出只看实质回答主体。

**⚠️ 信息量不对称**：
- 某模型**失败**（回退英文）= **较强的负面信号**
  （B/D 臂的 prompt 含英文 context，只会更把输出拉向英文）
- 某模型**通过** = **较弱的正面信号**
  （裸问能用该语言答，不保证塞进英文 context 后仍如此）

→ 它主要用来**排除**，不用来排名。

**⚠️ 混合推理模型的混杂**：若候选有 thinking 模式，
`"think": false` 未生效或 `num_predict` 过小，
会让思考轨迹（常为英文）占满输出，造成"回退英文"的假象。
判读前先确认开关生效、输出长度足够。


## S4a.7 ✅ S4a 关闭判据
 
**门（必须满足，满足即可进 S5）**：
 
- [ ] 存在至少一个模型同时满足：GATE-1 总参数 ≤5B · GATE-2 PASS · 许可 pre-check 通过
- [ ] 该模型的 `blob_digest` / `gate2_tested_runtime` / `gate2_tested_artifact_sha` /
      `gate2_tested_date` 已记进 `experiments/models.yaml`
- [ ] M2 主模型已选定并写进 DECISIONS（**`[L2]`，`owner_experiment: M3`**）
- [ ] **另记一条 `[L1]`：「许可 pre-check 足以解锁 M2」**
      `falsified_if:` M3 横评显示 RAG vs 微调的相对优劣在不同基座家族之间反转
      正文含 §S4a.5 那句边界声明
 
**顺手做（不是门，不必等）**：
 
- [ ] 其余候选也跑完并落盘
- [ ] `qwen3.5:4b` 的 GATE-1 参数核查完成（无论结果，关闭 M0 遗留）
- [ ] `experiments/gate2_raw/`（含 `_runtime.txt`）进 Git
 
> **`gate2_raw` 为什么进 Git**：`llama-bench` 单模型输出约几 KB，5 个合计不到 20 KB。
> 更重要的是分类判据 —— §19.2 说"能一条命令重新生成的才不进 Git"，
> 而**基准日志不可再生**（换 runtime、换时刻、换系统负载，数字就变）。
> 它与 `results.csv` 属同一类：测量记录，不是派生产物。
> 只提交 `gate2_results.csv` 而不提交原始日志 = **提交结论不提交证据**。
 
**若 GREEN 全部 FAIL**：**才**回到实施方案 §12.2 讨论许可证门，
且届时带着实测证据去谈。顺序不能反 —— 这是 §6.2 已记录过的错误 #8。

**台账字段要求**：

- [ ] `acquisition.artifact_provenance` 记 `verified` / `unverified`
      —— **仅当 verified 时才可赋 `source_trust`**
- [ ] GATE-2 的 FAIL **与 provenance 解耦**：
      failure 永远只关于 artifact + runtime；
      要提升为"关于模型架构"，须换 provenance 已知的 artifact 复测
- [ ] `runtime_scopes` 独立成段（它是 runtime 的属性，不是模型的属性）
 
## S4a.8 本步应入库的条目
 
- `[L2] M2 主模型选定` —— `owner_experiment: M3`。记 digest / runtime / artifact sha
- `[L1] 许可 pre-check 足以解锁 M2` —— 带 falsified_if 与边界声明
- `[L3] GATE-2 首轮结果` —— 每个候选的 PASS / FAIL / INCONCLUSIVE + runtime + artifact sha
- `[L3] qwen3.5:4b 的 GATE-1 核查结果` —— 关闭 M0 遗留

## S4a.9 预算校准 🟡 OPEN（S8 的前置，不是 S4a.7 的门）

> **状态**：
> - 9a ✅ completed
> - 9a-bis ✅ completed
> - 9b ✅ completed
> - 9c-final ✅ **completed once on S5 baseline**（`8c6bee0a… / 3383`）
> - 9c-final 🔁 **must rerun after Phase B corpus rebuild**
> - 9d / final contract freeze ⏸ waits for the new-corpus 9c-final
>
> **因此 S4a.9 仍为 OPEN。**

> **为什么 `MAX_PROMPT_TOKENS` 必须量**：它是**模型相关**的量，
> 不是与模型无关的物理常数。同口径反推的差异可达 237 个 token。

### S4a.9.0 ⚠️ 先入库的预先声明

**跑任何测量之前**，先把下面这条写进 DECISIONS：

### [L1] S4a.9 结果的处置方式（预先声明，先于实验入库）
- **决策：若实测算出中位只装 1 个 chunk，接受并记为代价。
  不通过放宽 CONTEXT_PACK_MARGIN、抬高 MAX_PROMPT_TOKENS、
  或压低 PROMPT_OVERHEAD_RESERVE_TOKENS 来凑到 2**
- **决策：代价由 M1c 的 Recall@1 / Recall@2 量化，
  写进 M2 预注册的「预期最可能结果」**
- 依据：事先声明了是证据，事后调参是找补

### S4a.9.1 四臂系统提示词初稿

测量用初稿；终稿在 S10 预注册时冻结。**预算按 B/D（较长的那套）定。**

> **⚠️ 一个必须在 S10 处理的问题**：
> B/D 的「Answer ONLY from the passages below」在 A/C 上没有 passages 可指。
> 照搬会让 A/C 全部拒答 —— 那测的是指令跟随而非裸模型知识。
> 若 A/C 删掉该句，两套就**非逐字相同**，该差异必须在预注册里显式声明。

### S4a.9a 量 prompt 固定开销

用主模型的 tokenizer（Ollama `prompt_eval_count`）量 `SYSTEM + 问题`，
**按 token 最长那题取值**，不按平均 —— 预算是硬约束，
按平均定会让最长的几题**静默超预算**。

> ⚠️ token 最长的未必是多语种题。先用 tokenizer 量出哪题最长，
> **不要按字符数挑**（字符最长 ≠ token 最长）。

### S4a.9a-bis 补 context 块框架开销

9a 量的是 `SYSTEM + 问题`，**不含 context 块框架**
（`Passages:` 标签、`Question:` 前缀）——
那部分既不在系统提示词、也不在问题、也不在 `est_prompt_tokens()` 里。

量 `SYSTEM + "\n\nPassages:\n\nQuestion: " + 最长问题`，差值即 F。
**`PROMPT_OVERHEAD_RESERVE_TOKENS` 取 B/D 形状的值。**

> ⚠️ 块间 separator **不建模、不猜值**。结论写成对它的条件式：
> **`k = 2` 成立当且仅当真实 separator 成本 ≤ slack。**
> 该值由真实 rendered chunk 实测。**不为未测量项猜上界当门用。**

### S4a.9b 直接实测 `MAX_PROMPT_TOKENS`

**不用插值。** 在目标长度附近直接扫：

```bash
llama-bench -m "$BLOB" -p 700,800,900,1000,1100 -n 32 -ngl 0 -t 8
```

读出 TTFT 跨过 10 s 的点。**只在实测的两点之间内插，绝不外推。**

### S4a.9c 真实 chunk tokenization：baseline 已测，Phase B 后重测

第一次 9c-final 已在

    corpus sha = 8c6bee0a…
    chunks     = 3383

上完成。该轮已经证明：

> **总体中位 chars/token 不足以验收 estimator；
> 真正承重的是 rendered-token estimation 的尾部。**

Phase B 会改变 corpus，因此新 corpus 冻结后，
必须用**完全相同的口径**重新执行 9c-final。

**最终验收至少报告**：

- actual rendered tokens / estimated tokens：min / p5 / p50 / mean / p95 / max
- ratio > 1.15 的 chunk 数
- 按真实检索窗口模拟的 prompt-overbudget 数量与比例
- anomaly-attributed vs none-observed 的归因
- realized `n_chunks_in_context` 分布
- 真实 adjacent rendered separator 的 token 增量

**⚠️ 不得只报告总体 chars/token 中位数。**

> **⚠️ 模拟口径的限制**：baseline 那轮用的是语料顺序的连续 chunk，
> 相邻块来自同页同节、长度相关。真实检索从全语料挑 k 个，长度方差更大。
> **在相关样本上得到的 overbudget 比例，对独立样本不自动成立。**
> M1c 出真实检索结果后须复核一次。

> ⚠️ 用非空探针（空 prompt 时 Ollama 不返回 `prompt_eval_count`）。

### S4a.9d `CONTEXT_PACK_MARGIN` 的安全不等式

**必须满足**：`(X − O) × m × err + O ≤ X`，即 **`m ≤ 1 / err`**

`err = 1.15` → `m ≤ 0.8696` → **0.86（⚠️ 向下取整）**

> **⚠️ 自洽性检查必须同时验两条**：①`m ≤ 1/err` ②派生不等式成立。
> 只验②会被 `int()` 截断掩盖 —— 同一个越界的 `m`
> 在某些 `O` 取值下会"侥幸通过"，换一组输入才暴露。

### ✅ S4a.9 验收

- [ ] 预先声明**先于**实验入库
- [ ] 提示词初稿已写并**入库**
      ⚠️ 脚本 / 模板走 stdin 不入库，日后需要的派生量补算不出来
- [ ] `PROMPT_OVERHEAD_RESERVE_TOKENS` 由 B/D 形状实测替换，移回「已校准」节
- [ ] `MAX_PROMPT_TOKENS` 由直接扫描 + 区间内插替换，来源注释记主模型
- [ ] `CONTEXT_PACK_MARGIN` 通过**两条**自洽性检查
- [ ] 中位可装 chunk 数已确定
- [ ] ⚠️ **若 k = 1：按预先声明接受，不调参凑到 2**
- [ ] ⚠️ 提交前 `git diff --cached --name-only` 确认 `core/contracts.py` 在暂存列表

      
---
 
# S4b · M3 候选空间调查 🟡 并行，不阻塞任何步骤
 
> **在哪**：桌面调研为主，必要时 Terminal 取 GGUF
> **不设截止。** 随时可推进，随时可停。
> **前置**：实施方案 §12.1 的 GATE-1 定义已冻结（否则调查会产出一堆含糊条目，要重做）
 
## S4b.1 零成本 GATE-1：先筛，再谈下载
 
**这一步不下载任何东西**，只查规格：
 
````
总参数 > 5B                     → gate1_params: FAIL，记原因与来源，不下载
许可明确禁止商用（如 CC-BY-NC） → gate3_license: RED，role 改 research_reference，不下载
无任何 GGUF 路径                → acquisition.direct_gguf: unavailable，标记待复查，不下载
family_key 与已有候选重复        → 不算新家族，可保留为尺寸梯度，但不计入家族数
其余                            → 进获取池
````
 
### 先判 family_key，再判其他
 
**"基座家族"不看品牌名，看 `family_key`**（见实施方案 §12.6）。
 
**硬规则：蒸馏 / 微调自另一个基座的模型，`family_key` 继承其基座。**
 
已可立刻处理的：
 
| 型号 | 处置 | 依据 |
|---|---|---|
| Granite 4.0 H-Tiny | `gate1_params: FAIL` | **7B 总参数**（IBM 官方，2026-09-07 核查） |
| Granite 4.0 H-Small | `gate1_params: FAIL` | 32B |
| Granite 4.0 Micro / H-Micro | 进获取池，`family_key: granite` | 3B dense |
| Yi-1.5 6B | `gate1_params: FAIL` | 6B > 5B |
| **DeepSeek-R1-Distill-Qwen-1.5B** | **移出候选池，进"明确排除"表** | `family_key: qwen`。是 Qwen2.5 的架构与词表被蒸馏，**不是独立基座家族** |
| DeepSeek-R1-Distill-Llama-8B | 明确排除 | `family_key: llama`，且 8B GATE-1 FAIL |
| Tiny Aya 3.35B | `gate3_license: RED`，`role: research_reference` | CC-BY-NC。可作多语种参考上限，不进商用候选 |
 
> **结论：DeepSeek 目前没有可用的独立小模型基座**，从候选池移除，理由写清入档。
 
**⚠️ 不能想当然的**：Nemotron、以及任何"某厂自研"但可能基于既有谱系的模型，
`arch_lineage` **必须查证并附来源**。发布方是独立公司，不代表基座是独立的。

 
## S4b.2 `models.yaml` 的 schema
 
### candidate（生成模型）
 
```yaml
- model_id: ministral-3-3b-instruct-2512
  role: candidate                    # candidate / teacher / embedder / other / research_reference
 
  # --- 家族判定（统计 diversity 只看 family_key）---
  arch_lineage:      mistral3        # 架构谱系，不是发布方。必须有来源
  tokenizer_family:  mistral
  family_key:        mistral         # ⚠️ 人工判定，不是机械拼接
  family_key_rationale: "Mistral 自研架构与词表，独立预训练"
 
  # --- 参数（MoE 必须区分）---
  params_total_b: 3.0                # ⚠️ GATE-1 判据锚在这一项
  params_active_b: 3.0               # dense 时等于 total
  params_evidence_url: "..."         # ⚠️ 不能从 tag 名推断
  params_evidence_date: 2026-09-07
 
  multimodal: true
 
  # --- 获取（与 gate 严格分离）---
  acquisition:
    ollama:
      tag: null
      status: not_investigated       # available / unavailable / not_investigated
      evidence: null
    direct_gguf:
      status: available              # available / pending / unavailable
      repo: mistralai/Ministral-3-3B-Instruct-2512-GGUF
      source_trust: official         # official / upstream / community
      quant: Q4_K_M
      size_gb: 2.0
      evidence_url: "..."
      evidence_date: 2026-09-07
 
  # --- 许可：两段式，不留裸的绿勾 ---
  license_claim:
    text: "Apache 2.0"
    source_url: "..."
    checked_date: 2026-09-07
 
  multilingual_claim:
    languages: [en, fr, es, de, it, pt, nl, zh, ja, ko, ar]
    covers_eval_set: partial         # 评测集需 en/zh/tl/hi → tl 与 hi 未列出
    source_url: "..."
 
  # --- 四道门 ---
  gate1_params: TODO
  gate2_llamacpp: TODO
  gate2_tested_runtime: null         # ⚠️ 实测后才填，不要照抄任何示例值
  gate2_tested_artifact_sha: null
  gate2_tested_date: null
  gate3_license: TODO                # ⚠️ 一律 TODO，包括 Qwen
  gate4_language_match: TODO         # 判据：输出是否使用了提问所用的语言（是/否）
  gate4_quality_note: null           # 主观印象记这里，不影响过门
 
  notes: |
    多模态（含 mmproj vision 文件）。实施方案 §18.1 明确不做图片 ——
    测试时须显式禁用视觉投影，使实验对象明确是 text model。
```
 
### embedder（有自己的一套门，不是"不过门"）
 
```yaml
- model_id: bge-m3
  role: embedder
  dim: 1024                          # 影响索引包体积与岸船一致性校验（contracts.IndexManifest）
 
  acquisition:
    ollama:
      tag: bge-m3:latest
      status: available
      evidence: "ollama pull 成功，blob 约 1.1G"
 
  license_claim:
    text: "..."
    source_url: "..."
    checked_date: null
 
  gate_e1_runtime:      TODO         # 船端能否算查询向量。Ollama 可用 ≠ 船端可用
  gate_e2_license:      TODO
  gate_e3_multilingual: TODO         # ⚠️ 硬门，不是烟雾测试
  gate_e3_evidence:     null         # 填 M1c 里多语种题的检索 Recall
 
  notes: |
    ⚠️ embedder 的多语种是【承重的】，不是加分项。
    embedder 不懂印地语 → ML03 在检索阶段就死了 → B/D 臂拿什么模型生成都无所谓。
    判据不是"能否产出该语言"，而是"该语言的查询能否召回正确的英文 chunk"——
    语料是英文、查询是中文/他加禄语/印地语，这是跨语言检索，比同语言检索难。
```
 
### 五条字段规则
 
1. **`family_key` 是人工判定 + 必填 rationale。** 不做机械拼接 ——
   很多模型借用 Llama 的 tokenizer 但架构与预训练数据完全独立，拼接会错误合并。
   `arch_lineage` 与 `tokenizer_family` 是**判定的证据**，不是判定的公式。
2. **蒸馏 / 微调自另一基座的模型，`family_key` 继承其基座。** 这条硬规则一次写死。
3. **`gate2_tested_*` 一律 `null` 起步。** 不要在 schema 示例里写真实版本号——示例值会被复制成真值。
4. **`gate3_license` 一律 TODO，包括 Qwen。** 表格里一旦出现一个绿勾，
   注释里写多少遍"不预判"都会被忽略。
5. **凡是没有 `evidence_url` + `evidence_date` 的条目，不进 `models.yaml`。**
   台账"状态只能来自实测"，二手转述的许可与参数不算实测。
 
## S4b.3 迁移纪律：现在只迁已核实的
 
**不要把方案表里、或任何一轮对话里提过的候选批量填进去。** 现在只迁：
 
- 本机已真实拥有的模型（含 `role: teacher` / `embedder` / `other`）
- Granite 已核实的参数（Micro 3B / H-Micro 3B / H-Tiny 7B / H-Small 32B）
- Ministral-3-3B 已核实的资料
- 两个 Ollama manifest failure（记在 `acquisition`，**不是** gate failure）
 
其余逐项调查、逐项补 `evidence_url`。
 
## S4b.4 直接 GGUF 的获取与测试
 
llama.cpp 支持直接从 HF 拉取，**拉取与加载测试是同一条命令**：
 
```bash
llama-cli -hf mistralai/Ministral-3-3B-Instruct-2512-GGUF:Q4_K_M -n 1 -p "hi"
```
 
**多模态候选必须显式禁用视觉投影**（`-hf` 会自动下载 mmproj）：
 
```bash
# ⚠️ 依赖这个 flag 之前先验一次它存在：
llama-cli --help | grep -i mmproj
 
llama-cli -hf mistralai/Ministral-3-3B-Instruct-2512-GGUF:Q4_K_M \
          --no-mmproj -ngl 0 -t 8 -n 1 -p "hi"
```
 
这样实验对象明确是 **Ministral text model**，不是悄悄把 vision projector 一起拉进来。
 
## S4b.5 停止规则与完成判据
 
### 输入是时间盒，不是数量
 
**不设"目标 N 个型号"或"目标 M 个家族"。** 数量是**产出**不是**输入** ——
设成输入就会诱导为凑数找模型。
 
````
调查预算：≤4 小时桌面调研（时间盒）
 
停止规则（满足任一即停）：
  ① 时间盒用尽
  ② 连续 3 个新调查的家族都被零成本门淘汰
     （无 ≤5B 型号 / 无任何 acquisition route / license 明确非商用）
  ③ 实施方案 §12.3 已列的全部家族都已有处置（纳入或带依据地排除）
````
 
> ② 里的 3 本身是判断：2 容易被检索顺序的偶然性触发，5 又太晚。
> 这个数是 [L3]，跑一轮后可调，调了要记。
 
**调查完是 8 个还是 16 个都行，重要的是能说清"为什么停在这里"。**
 
### 漏斗每一层的数字本身就是交付物
 
````
调查池        N 个型号 / M 个家族（按 family_key 去重）
  ↓ GATE-1    淘汰 a 个（附每个的总参数与来源）
获取池        ...
  ↓ GATE-2    淘汰 b 个（附 runtime 版本与 artifact 哈希）
  ↓ GATE-3    淘汰 c 个（附许可来源与日期）
M3 横评池     X 个型号 / Y 个家族
````
 
**⚠️ 调查可以"失败"，而失败本身是有效结论。**
 
如果 Y < 5（§12.1 对 M3 的要求），结论不是"调查失败了"，
而是"**当前约束下可用基座家族仅 Y 个**"——
这直接支撑"要不要放宽许可证门"这个讨论，比硬凑到 5 个诚实，也更有用。
 
### ✅ 完成判据（过程性，不是数量性）
 
- [ ] 每个纳入项有完整 provenance（`family_key` + rationale / 总参数 / 激活参数 /
      `arch_lineage` / `tokenizer_family` / acquisition route / `source_trust` /
      license claim + 来源 URL + 查证日期）
- [ ] **每个排除项有带依据的排除记录**（哪道门、什么数据、哪个来源）
- [ ] `acquisition` 与 `gate*` 在台账里是两套字段，不混用
- [ ] 汇报口径同时给出 `models investigated = N` 与
      `base families investigated = M`（**按 family_key 去重**）
- [ ] 漏斗各层的数字与淘汰理由完整
- [ ] **停止规则触发了哪一条，记录在案**
 
第 2 条最重要：**"我们排除了 X，因为 Y" 比 "我们测了 15 个" 更能证明调查是系统的。**

---

# S5 · 任务 1：Parser + 语料构建 ⭐ M1a

> **在哪**：Terminal 里 `shiprag && claude`
> **里程碑**：**M1a**
> **前置**：S3 契约冻结、S1 语料就位
> **解锁条件**：S3 契约冻结 + **S4a 的门已满足**（存在一个 GATE-1/GATE-2/pre-check 全过的模型）。
> **不等 S4b。** S4b 是并行轨道，它永远不构成 S5 的前置。

### S5.1 开会话

```bash
shiprag
claude
```

然后**贴任务序列 v4 的「任务 1」整段 Prompt**（`CLAUDE.md` 已经覆盖通用前缀，不用再贴）。

### S5.2 跑

Claude Code 写完后，让它自己跑：

```bash
time python3 ingest/build_corpus.py "raw/KAIVA - Manuals" corpus
```

### S5.3 ✅ S5 验收清单

**A. 形状检查（必要不充分）**

- [ ] 解析成功 **9/9**
- [ ] chunk 字符数 max **< 2000**
- [ ] 全量耗时 **< 90 秒**
- [ ] `corpus/ingest_failures.csv` 为空
- [ ] `git check-ignore corpus/chunks.jsonl` 有输出

> **⚠️ 形状检查不查内容有效性。** 乱码 chunk 在每一项形状检查上都表现正常：
> 数量正常、长度正常、确定性正常、`section` 字段"有值"（乱码不是 `UNKNOWN`）。
>
> **⚠️ chunk 总数与分布不设硬阈值。** 逐份记录并观察偏差形态：
> **集中在少数手册上的偏差是信号；弥散的才是噪声。**

**B. ⭐ page accounting invariant（硬要求）**

**每个 source page 必须恰好有一条 `page_quality` record。**

page-level terminal disposition 必须属于**冻结枚举**；
**实现不得自行增加 `"other"` / `"unknown_reason"` 作为兜底。**

至少区分：

    admitted
    skipped_noncontent
    quarantined
    failed_ingest

并满足：

    total source pages    = 1335
    page_quality records  = 1335
    duplicate (doc_id, pdf_page) = 0
    missing source pages  = 0

每个 **admitted** page 的 `chunk_ids` 可为一个或多个；
每个**非 admitted** page 的 `chunk_ids` 必须为空。

> **「没有 chunk」是允许的结果；「没有 page-level record」不是。**

> 若当前尚无 `ingest/page_quality.jsonl`，至少由 ingest report 给出等价 accounting。
> Phase B implementation 后，`ingest/page_quality.jsonl` 成为正式的 page-level audit artifact。

**C. 内容有效性检查**

- [ ] **查结构不查症状**：`pdffonts` 的 **`uni` 列**
      —— 某页字体无 ToUnicode，该页文本按构造不可靠
- [ ] 报告 native body 不足的页数（`extracted_chars < CHUNK_MIN_CHARS`）及其分布
- [ ] `section` 字段的**取值来源分布**（explicit_header / title_fallback / inherited）
      ⚠️ `section` 非空 **≠** metadata 有效

**D. ⭐ provenance（必做）**

- [ ] `components/parsers/kaiva_pdf.py` 与 `ingest/build_corpus.py` **已入库**
- [ ] 用**入库后的代码**重建，`corpus sha` 与工作区**逐字节一致**


rm -rf /tmp/shiprag-verify
python3 ingest/build_corpus.py "raw/KAIVA - Manuals" /tmp/shiprag-verify | tail -3
shasum -a 256 corpus/chunks.jsonl /tmp/shiprag-verify/chunks.jsonl
rm -rf /tmp/shiprag-verify


> **这是把 corpus sha 与一个 commit 绑在一起的唯一动作。**
> 未入库的文件无从比对 —— `git diff` 证明不了它们没被改过。
> **⚠️ 两个 sha 不一致** = 工作区的 parser 与生成 corpus 的 parser 不是同一份，
> 所有以该 sha 为键的测量立即失去作用域。

**E. 手工复查**
（pdf_page 是否 1-based · `Chunk.id` 确定性 · 记录语料哈希）

**⚠️ 通过本清单 = S5 baseline 建成，可复现。**
**它不构成 content-correctness PASS。**
# S5b · Parser / extraction 审计 ⭐ CLOSED

> **性质**：对 S5 baseline 的内容正确性审计，不是新的 parser implementation。
> **结果**：S5 baseline **可复现**，但**不能判为 content-correct PASS**。

## 冻结的 baseline

    corpus sha256 = 8c6bee0aa952b387e20a2de76253d4384ffe859a8050e6fe8fee9166c956aeaa
    chunks        = 3383
    parser/builder provenance commit = 545947a

## 已确认的四个独立轴

| 轴 | 问题 | 证据 |
|---|---|---|
| native body quality | 抽到的正文能不能读 | TACM p.38–103 的 Type 3 / `uni=no` |
| page content completeness | native text 是否覆盖页面全部业务内容 | 16 confirmed examples / 6 manuals |
| citation metadata | section 等出处字段是否语义正确 | 96 anomaly candidates / 15 clearly wrong |
| content policy | 页面有内容是否等于值得索引 | form / template 样本证明二者不同 |

**四轴互不蕴含**：

    native_text_usable  ≠  page_content_complete
    body_quality        ≠  citation_metadata_correct
    visual_content      ≠  ocr_worthy

**不得重新合并成一个 `page_good` / `needs_ocr` 字段。**

## section 审计的核心结论

`title_line_fallback` 可以把**任意看起来像标题的字符串**写进 `section`。

已知反例：

- TACM p.104–115 → `PCL E`
- TACM p.116–120 → 问句式 section
- CMM p.2–8 → 手册名进入 section
- **TACM p.100 → `"9"`**

**最后一个最危险**：`"9"` 结构上完全合法，甚至与真实章节号发生碰撞，
**但语义位置仍然错误**。

> **因此「非空 / 格式合法 / 属于已知章节集合」都不能等价为 citation verified。**

**另有 90 页**：section 取值所在的那一行**未从正文剥离**
（SECTION 标签行与 Chapter 标题行均已剥离）。
后果双向：BM25 词频被抬高、token 被计两次。

## 边界

`citations_correct` scorer 与 renderer 当前均 **not implemented**，
因此上述为 **future risk if implemented as specified**，
**不是"M2 已经在扣分"**。

# S5c · Phase B：selective OCR + citation-safe ingestion ⬛ 当前主线 · 阻塞 S6

> **状态**：Design Freeze 后执行。
> **目标**：修复已确认的 ingestion correctness failure，生成**新的 frozen corpus identity**。
> **不是目标**：把 OCR 做成通用视觉理解系统。

## 架构

    raw PDF
      ↓
    native extraction + signals
      ↓
    page states
      ↓
    岸端独立 OCR execution
      ↓
    frozen digest-addressed OCR artifact
      ↓
    parser consumes native + accepted OCR artifact
      ↓
    body gate  +  independent citation metadata gate
      ↓
    admit / quarantine / skip
      ↓
    chunks.jsonl  +  ingest/page_quality.jsonl

**船端**：不安装 OCR runtime · 不运行 OCR · 只消费冻结的 corpus / index package。

## 五个状态轴（必须分离）

    native_body_status:               usable / unusable / insufficient / uncertain
    page_content_completeness_status: presumed_complete / visual_content_suspected / not_assessed
    ocr_body_status:                  not_attempted / usable / degraded / failed
    citation_metadata_status:         explicit_supported / uncertain / failed
    content_policy_status:            admit / hold_for_review / reject_noncontent

**`explicit_supported` 不表示**：人工验证过 section 语义 ·
与目录 ground truth 一致 · citation 整体 verified · body usable 蕴含 metadata correct。

**它只表示**：存在明确结构标签，成功抽出一个满足机械完整性要求、
且 provenance 可追踪的 metadata candidate。

## 确定性

    OCR execution determinism  与  corpus-build determinism  是两个问题。

OCR execution 允许存在重复运行差异；
但 **frozen artifact → parser → corpus 必须 deterministic**。

**parser / build 不得隐式执行 OCR。**
同 cache key 再次运行 OCR 得到不同 bytes：**不得静默覆盖**；
记 reproducibility event，重新走 acceptance。

**⚠️ 禁止 `native_text + "\n" + ocr_text` 字符串拼接。**
会重复页眉、抬高 BM25 词频、改变 chunk 边界。

## body merge semantics

**CASE R · native body unusable**

    native_body_status = unusable  +  accepted OCR body
    → OCR 成为唯一 primary body

native 乱码只进 audit，不进 chunk body。
**citation metadata 不因 OCR body usable 而自动通过**，
metadata candidate 独立进入 citation gate。

**CASE S · native body insufficient**

    native_body_status = insufficient  +  accepted OCR body
    → OCR 成为 primary body

native 与 OCR 的 metadata candidate **分别进入 citation gate**。
**不得冻结「metadata 一律取 native」或「metadata 一律取 OCR」。**

**CASE A · native body usable but completeness suspected**

Phase B **不做 augmentation**，只记录状态、证据与 future interface。
未来若实现，用有 provenance 的 ordered structured blocks，**不得字符串拼接**。

## page audit 必须记录的字段

    selected body channel
    selected metadata channel
    native metadata candidate
    OCR metadata candidate
    citation gate result
    remediation action

## Phase B 不变量

> **同一 admitted page 的 primary body 在本阶段只能来自一个 channel。**

native / OCR 的正文重复**在结构上不可能发生**。

## 不进生产路径的东西

- 视觉 detector 的具体阈值（NEG 样本过少）→ **diagnostic / audit signal only**
- page content 的 augmentation（第三类 failure 的补全）→ residual risk，推到 M5

git add -- components/parsers/kaiva_pdf.py ingest/build_corpus.py
git diff --cached --name-status        # ⚠️ 必须只有这两行
git commit -m "feat: add KAIVA PDF parser and corpus builder S5 baseline"
git push origin ship-rag

> 脚本与 parser 入库后，**必须用入库代码重建一次并比对 corpus sha**（见 S5.3 D 段）。
---

# S6 · 任务 2：gold chunk 映射 ⭐ M1b 收口

# S6 · gold chunk 映射 🔒 等待 S5c

> **⚠️ S6 当前不可执行。**
>
> `GoldChunkMap` 的 identity 包含 corpus sha。
> Phase B implementation 预计改变：CASE R（乱码 native body → OCR body）·
> CASE S（原被丢弃的页可能首次产生 chunk）· page admission / quarantine ·
> chunk 数量 · chunk 长度 · per-page ordinal · corpus sha。
>
> **因此现在生成的 GoldChunkMap 是 known-to-be-invalidated artifact。**
> **决策：不生成它。**

## S6 的硬前置（全部满足才能执行）

1. Phase B selective OCR / ingestion remediation implementation 完成
2. parser / builder 的最终实现已入 Git
3. OCR accepted artifacts 已冻结并带 digest
4. `ingest/page_quality.jsonl` 已生成并通过完整性检查
5. 新 corpus 已由**入库代码**重建
6. 新 corpus deterministic rebuild 通过
7. 新 corpus sha256 与 chunk count 已记录
8. 所有依赖旧 corpus sha 的产物已按依赖关系重新生成或重新验证
9. S4a.9c-final 已在**新 corpus 的真实 chunks** 上重跑
10. 预算相关 contract 若因此变化，已完成独立的 `contract:` commit

然后才：

    new frozen corpus → S6 resolve_gold_chunks → GoldChunkMap(new corpus sha)


> **在哪**：`shiprag && claude`
> **前置**：S5 产出 `corpus/chunks.jsonl`

### S6.1 开新会话，贴任务序列 v4 的「任务 2」

### S6.2 跑

```bash
shiprag
CORPUS_SHA=$(shasum -a 256 corpus/chunks.jsonl | cut -c1-8)
echo "corpus sha8 = $CORPUS_SHA"

python3 scripts/resolve_gold_chunks.py \
    --chunks corpus/chunks.jsonl \
    --testset eval/testset_v5_3.jsonl \
    --corpus-sha "$CORPUS_SHA" \
    --out-dir eval/gold_chunk_map/
```

### S6.3 ✅ S6 验收清单

- [ ] **L1+L2 合计命中率 ≥ 80%**
- [ ] FAIL 条目逐条列出了 question_id（= 页不存在：页码错 / doc_id 错 / 该页被丢弃）
- [ ] L4 条目逐条列出了 question_id（= 页存在但无片段命中 → **引文抄写有出入**的强信号）
- [ ] **L4 和 FAIL 真的分开了**（合并会让"引文抄错"伪装成"页缺失"）
- [ ] 输出的映射文件名形如 `map__ts-v5.3__corpus-<8位>__parser-kaiva_pdf_v1.json`
- [ ] 映射里**没有任何 expected=="refuse" 的题**
- [ ] 脚本用的是 `contracts.split_quote_fragments()`，没有自己再实现一遍切片
- [ ] `ensure_ascii=False`（中文/印地语没变成 `\uXXXX`）
- [ ] ⚠️ **resolution 不得要求 `citation.section == chunk.section` 的 exact string equality
      作为 quote-to-chunk match 的硬前置。**

      已观察到 representation mismatch（如 `Chapter 15` vs `15`）而 semantic location 一致。

      **primary evidence**：`doc_id` · `pdf_page` · normalized quote fragments / sole-match rules
      **section 用于**：audit · disambiguation · mismatch reporting

      **不得让纯表示差异制造 false negative。**

- [ ] L4 的三种可能已分开记录：
      ① 引文抄写有出入 ② 该页解析失败 ③ **引文抄自图像层**

      > 判据：把该条 quote 的片段与该页 corpus 文本比对。
      > 片段命中 = 证据在文本里；
      > 不命中 = **candidate，须与源 PDF 直接比对后方可升格** ——
      > 不命中亦可能来自 normalization / chunking / 转录差异，**不得直接判定**。


**⚠️ L1+L2 低于 80% 怎么办**：
**不要让脚本去"修"数据，不要放宽匹配条件。** 那说明评测集的 quote 与语料存在系统性出入。
把报告带回本对话，判断是评测集抄错了还是解析器丢了内容。

**你自己要做的一件事**：把所有 L3 / L4 / FAIL 的题**逐条打开对照原文**。
机械回填可以交 AI，但"这个 chunk 是不是真的包含答案"是判断（§22）。

- [ ] **本步应入库的条目**：`[L3] gold chunk 映射生成` —— 记各级别条数与占比、FAIL/L4 的 question_id 清单、人工复核结论

---

# S7 · 任务 3：GATE-2 批量筛选

> **在哪**：`shiprag && claude`
> **无依赖，可以和 S5/S6 并行**（下载和 bench 都很慢，适合挂着）
> **⚠️ S7 的定位已变（v3）**：GATE-2 的**一次性手动执行**已提前到 S4a，
> 那是解锁 S5 的门。**S7 保留为批量脚本化的 GATE-2 工具**，服务 S4b 的扩展候选池与 M3。
>
> **S7 不阻塞任何步骤。** 什么时候做取决于 S4b 的候选池有多大 ——
> 候选只有 5 个时手动跑更快，超过十来个才值得写脚本。
>
> **相对 v2 的接口变更**：脚本必须支持三条输入路径，因为 GATE-2 测的是
> llama.cpp 兼容性，它不关心 GGUF 从哪来：
>
> ```
> --ollama-tag <tag>      → ollama show --modelfile 取 blob
> --gguf <local path>     → 直接用
> --hf <repo:quant>       → llama-bench / llama-cli 的 -hf
> ```
>
> **输出必须新增四列**：`gate2_tested_runtime` / `gate2_tested_artifact_sha` /
> `gate2_tested_date` / `source_trust`，以及第三种状态 `INCONCLUSIVE`
> （`source_trust == community` 且失败时用它）。
>
> **列名不带数字**：旧的 `ttft_1500_est_s` 作废。用 `prefill_budget_est_s`，
> 预算取值另开一列 `prefill_budget_tokens`。
> 理由：`prefill_1050_est_s` 会把一个仍在 contracts 里待拍板的数字再次焊进列名 ——
> **会变化的测量条件不编码进字段名；字段记语义，值记实验条件。**
> 这条同样适用于 Recall@k、context budget、量化等级。

### S7.1 先手动跑通一个，确认命令在你机器上有效

**这一步不要交给 Claude Code。** 先自己验证流程，再让它批量化——
否则脚本报错时你分不清是脚本问题还是环境问题。

```bash
TAG=llama3.2:3b        # 先用已知能通过的那个当对照

ollama pull "$TAG"
BLOB=$(ollama show "$TAG" --modelfile | grep "^FROM" | awk '{print $2}')
echo "blob = $BLOB"

# GGUF 魔数，应为 4747 5546
head -c 4 "$BLOB" | xxd

# 核心 bench。-ngl 0 = 不用 GPU；-t 8 = 8 线程
# ⚠️ 必须 -t 8，不要用默认。Mac 是 28 核，船端不会有，用默认数字会严重乐观
llama-bench -m "$BLOB" -p 512,1024,2048,4096 -n 32 -ngl 0 -t 8

# 失败时看真实原因（llama-bench 会吞掉加载错误）
llama-cli -m "$BLOB" -n 1 -p "hi" 2>&1 | grep -iE "error|unknown|arch"
```

**期望**：`llama3.2:3b` 通过，pp4096 约 71.8 tok/s（这是 M0 实测过的数）。
**对不上说明环境有变**，先查环境再往下走。

### S7.2 让 Claude Code 批量化

开新会话，贴任务序列 v4 的「任务 3」。

```bash
# 先 dry-run
python3 scripts/gate2_screen.py --dry-run \
    --models qwen3:4b granite4:tiny smollm3 gemma3:4b phi4-mini llama3.2:3b \
    --out experiments/gate2_results.csv

# 确认命令没问题再实跑（会跑很久）
python3 scripts/gate2_screen.py \
    --models qwen3:4b granite4:tiny smollm3 gemma3:4b phi4-mini llama3.2:3b \
    --out experiments/gate2_results.csv
```

### S7.3 ✅ S7 验收清单

- [ ] **至少一个 🟢 GREEN 许可模型 PASS**（qwen3 / gemma3 / phi4-mini / granite4 / smollm3 任一）
- [ ] `-t 8` 没有被改成默认
- [ ] tag 找不到时记 SKIP，**没有自作主张换 tag**
- [ ] 每个 PASS 都记了 `blob_digest`
- [ ] 单个模型失败没有中断整批
- [ ] `ttft_1500_est_s` 这一列有值（= 1500 / pp2048）

**结果判读**（回本对话一起看）：

| 情况 | 动作 |
|---|---|
| 有 GREEN 模型 PASS 且 `ttft_1500_est_s` ≤ 10s | 选它做 M2 主模型，许可证门不动 |
| 有 GREEN PASS 但 TTFT 超预算 | 仍可做 M2（M2 测的是正确率不是船端可行性），但记进风险 |
| GREEN 全部 FAIL | **才**需要修订许可证门，且届时带着实测证据去谈。**顺序不能反** |

- [ ] **本步应入库的条目**：`[L2] GATE-2 筛选结果` + `owner_experiment: M3 复测`。记每个模型 PASS/FAIL/SKIP + 原因 + digest。**主模型的选定单独写一条 [L1]**

---

# S8 · 任务 4：检索层 + 检索基线 ⭐ M1c

> **在哪**：`shiprag && claude`
> **前置**：S6 的映射文件、S4 的 `bge-m3`

### ⚠️ 为什么这一步自己写代码，而不是用 Kiln

Kiln 官方文档说明它的 RAG 流程是：**它自己的 extractor 把 PDF 转成 markdown/纯文本 → 它自己按固定窗口或语义方式分块**。没有"喂入我已分好的 chunk"这条路径。

两个后果：

1. **`gold_chunk_ids` 在 Kiln 里没有对应物** —— 你的 chunk 来自 `KaivaPdfParser`，Kiln 的是另一批对象，无 join 键。在 Kiln 里算的 Recall 测的是另一个系统。
2. **引用三元组产生不出来** —— `(doc_id, section, pdf_page)` 是你的解析器从页眉表格里抠出来的（§9.2 勘察的全部价值）。Kiln 的 extractor 不保留它。**没有三元组就没有出处，Citation First 这条 L1 在 Kiln 里无法实现。**

**这不是提前写产品代码**：`components/retrievers/*` 与 `eval/*` 本来就在实施方案 §19 的目录里，
contracts 已冻结接口，这批代码是 M4 的资产，不会白写。

### S8.1 两个 L2 参数（S3 没定的话现在定）

| 参数 | 选项 | 倾向 | 理由 |
|---|---|---|---|
| BM25 实现 | 纯 Python vs LanceDB 全文索引 | **纯 Python** | 船端少一个依赖（A4：船上没人修，每个依赖都是故障点）；~80 行，M4 直接复用 |
| Embedding | `bge-m3`(1024维) vs `nomic-embed-text`(768维) | **bge-m3** | 评测集有 zh/tl/hi 三题，nomic 主要是英文 |

### S8.2 开会话，贴任务序列 v4 的「任务 4」

### S8.3 跑

```bash
shiprag
CORPUS_SHA=$(shasum -a 256 corpus/chunks.jsonl | cut -c1-8)

python3 eval/run_retrieval_eval.py \
    --chunks corpus/chunks.jsonl \
    --testset eval/testset_v5_3.jsonl \
    --gold-map "eval/gold_chunk_map/map__ts-v5.3__corpus-${CORPUS_SHA}__parser-kaiva_pdf_v1.json" \
    --retrievers bm25 vector hybrid \
    --k 1 2 3 20 \
    --out-dir eval/
````
 
**为什么改**（契约 v0.2.0 落地后重算）：
 
````
单 chunk 估算 = 中位 1169 字符 ÷ 4 + 引用头 14 tok ≈ 306 tok
 
prompt 预算 1050 → chunk 预算 (1050−200)×0.90 = 765 → 中位能装 2 个
prompt 预算 1500 → chunk 预算 (1500−200)×0.90 = 1170 → 中位能装 3 个
````

### S8.4 ✅ S8 验收清单

- [ ] 三种检索器各出一组 **Recall@1 / Recall@2 / Recall@3 / Recall@20**
- [ ] **relevance 真的归一化到 0..1**（BM25 原始分和余弦距离都不满足，最容易漏）
- [ ] 三个 retriever 之间没有互相 import
- [ ] `TOP_K_RETRIEVE` 从 contracts 引入，不是又定义了一份
- [ ] **trap 题真的从 Recall 分母里排除了**
- [ ] `cross_doc` 的命中判据是按"每份手册"算的，不是简化成"任一 gold chunk"
- [ ] 融合方式是模块顶部常量（M5 要改它）
- [ ] 有逐题明细文件（汇总数说明"多少"，明细说明"为什么"）
- [ ] CD01 的单独观察结果打出来了
- [ ] **多语种题（ML01/ML02/ML03）的检索 Recall 单独报，不与英文题合并**
      理由：语料是英文、查询是 zh/tl/hi，这是**跨语言检索**。
      若多语种题的 Recall 显著低于英文题，那是 **embedder** 的问题不是生成模型的问题 ——
      **这个归因区分必须在 M2 之前做出来**，否则 M2 的多语种表现会被错误归因到臂上。
      该数字同时填进 `models.yaml` 的 `gate_e3_evidence`（见实施方案 §12.7）
- [ ] **CD01 的观察结果写进 M2 预注册的「预期最可能结果」**
      CD01 的 gold 是 3 个 chunk、跨 2 份手册，而 765 预算下中位只能装 2 个 ——
      **它可能结构性不可答**。事先预测了它是证据，事后解释它是借口

**⚠️ hybrid 比单一 retriever 差怎么办**：
**不要调融合权重去"修好"它。** 那是 M5 消融要回答的问题，现在调等于在测试集上调参。
把数字带回本对话。

### S8.5 这一步要回答的三个问题（不是"跑完了"）

| 问题 | 用什么答 | 结论影响什么 |
|---|---|---|
| `TOP_K_RETRIEVE=20` 够不够 | Recall@20 曲线 | 这是它的 `owner_experiment` |
| **要不要引入 reranker** | **Recall@2** vs Recall@20 的差距 | 见下表 |
| 失败的题为什么失败 | 逐题明细 + 十个归因标签 | M5 该调什么 |

**reranker 判据（实施方案 §13.1）**：

| 观测 | 结论 |
|---|---|
| **Recall@2** ≈ Recall@20 | 融合排序已够用，**不引入 reranker**。省下的延迟与故障面直接是收益 |
| Recall@20 高但 **Recall@2** 明显低 | 排序有提升空间 → 值得试，但仍须过延迟预算 |
| Recall@20 本身就低 | 问题在召回不在排序，加 reranker 无用，回去调检索或分块 |
 
**⚠️ 槽位从 5 掉到 2，排序质量的杠杆显著变大。**
同一条判据在 5 个槽位和 2 个槽位下会给出不同结论 ——
k 必须跟着预算走，不能沿用旧数字。

- [ ] **本步应入库的条目**：`[L2] M1c 检索基线` + `[L1] 是否引入 reranker`（带 `falsified_if`）

---

# S9 · `MIN_RELEVANCE` 校准

> **在哪**：Terminal + 你判断
> **前置**：S8 的逐题明细（含 `retrieval_scores`）

### S9.1 画分布图

```bash
shiprag
python3 eval/calibrate_threshold.py \
    --items eval/m1c_items__${CORPUS_SHA}.csv \
    --testset eval/testset_v5_3.jsonl \
    --out eval/threshold_distribution.png
```

**要看的两条分布**：

- 7 道计分陷阱题的**最高检索分**（TR02 已 `exclude_from_primary_score`，排除）
- 31 道可答题的 **gold chunk 分数**

**目标**：找一个切点，让陷阱题的最高分落在阈值之下、可答题的 gold 分数落在阈值之上。

### S9.2 ⚠️ 一个必须写进汇报的效度隐患

**你是在评测集自己身上校准阈值，然后又用同一批题测拒答表现。这是在测试集上调参。**
n=7 的陷阱题上调阈值，过拟合几乎必然，会让 B/D 臂的拒答成绩系统性偏乐观。

两条路，**都可以接受，但必须选一条并说出来**：

| 路 | 成本 | 汇报口径 |
|---|---|---|
| 建独立校准集（20–30 题，只标"语料里有/没有"，不计分） | 约 1 天 | 阈值在独立集上校准 |
| **接受并披露**（我倾向这条） | 0 | "拒答阈值在同一评测集上校准，拒答成绩为乐观估计" |

倾向后者的理由：拒答的真正验收本来就排在 M5，M2 的任务是**架构决策**不是安全验收。
而且按 §24.4 你自己的汇报口径——**明确说出边界的结果更难被推翻**。

### S9.3 ✅ S9 验收清单

- [ ] 分布图生成了（**这张图直接作为汇报材料，比一个数字有说服力得多**）
- [ ] `MIN_RELEVANCE` 的新取值由**你**定（§22 四不交之一），不是脚本挑的
- [ ] `core/contracts.py` 里把 `MIN_RELEVANCE` 从「未校准」节移到「已校准」节，并写上实测依据
- [ ] 走契约变更仪式（`contract:` 前缀 commit）
- [ ] 校准方式（同集 vs 独立集）已选定并记录
- [ ] **本步应入库的条目**：`[L2] MIN_RELEVANCE 校准` + `owner_experiment: M5`，附分布图路径与选定的汇报口径

---

# S10 · 冻结评测集 + 写 M2 预注册

> **在哪**：编辑器
> **⚠️ 顺序不可颠倒**

```
gold chunk 映射完成（S6）
   ↓
CMM 4 题核验完成（FL01/FL15/PR03/ML01 —— 这 1 小时在关键路径上，
                  不核就要排除，主分母 31 → 27，可检测效应进一步恶化）
   ↓
判分协议定稿（实施方案 §14.3 已写好，确认即可）
   ↓
评测集定稿 → 冻结 → shasum -a 256 eval/testset_v5_3.jsonl
   ↓
写 experiments/M2_preregistration.md → 提交，不改
   ↓
M2 开跑
```

```bash
shiprag
shasum -a 256 eval/testset_v5_3.jsonl        # 这个值填进预注册
shasum -a 256 corpus/chunks.jsonl
git rev-parse --short HEAD                    # code_commit
```

### S10.1 预注册必须锁死的东西

按实施方案 §14.1 / §14.2 / §14.3 填：

- 主模型 + digest、Teacher 模型 + digest
- 评测集版本 + **sha256（必须是冻结后的值）**
- 排除项：TR02（SMM 缺页）
- 主指标：答案正确率 @ 31 道可答题
- **两道闸**：闸一 Δ ≥ 19 pp；闸二 McNemar 精确检验双侧 p < 0.05。**两道都过才接受**
- **判分协议**：三级判分、`PARTIAL_MIN_ELEMENT_HIT_RATIO = 0.60`、Partial 一律映射为 Incorrect
- **盲评协议**：打乱顺序隐去臂标签 → 判分 → 还原 → 隔 24h 复判 20 条测一致率
- **四臂共用同一套系统提示词全文**（唯一差别是有无 context 块）
- 本实验不回答的问题：模型横评（M3）、检索参数调优（M5）、**微调的泛化能力**
- 预期最可能结果（先写下来，事后对照，这一条防自欺）

### S10.2 ✅ S10 验收清单

- [ ] CMM 4 题已对照原文核验（或明确记为排除，主分母改 27）
- [ ] `eval/testset_v5_3.jsonl` 的 sha256 已记录
- [ ] `experiments/M2_preregistration.md` 已提交，**sha256 是冻结后的值**
- [ ] 四臂提示词全文在预注册附录里
- [ ] **本步应入库的条目**：`[L1] M2 预注册提交` —— `falsified_if: 无（预注册方法本身的前提）`。注明提交时间与文件 sha256

---

# S11 · Kiln 装机 + 合成数据 + LoRA

> **⚠️ 前置条件(合成数据生成之前必须完成)**:
>
> - [ ] **teacher 的 GATE-T1(输出用途许可)已核**
>       —— 该许可是否限制"用其输出训练/改进其他模型"?若有条款,义务是什么?
> - [ ] **teacher 的 GATE-T2(本机可行性)已实测** —— 内存 + 批量生成吞吐
> - [ ] M2 首轮确认为**单 teacher `qwen3:32b`**(Apache 2.0,已实测 26.09 tok/s)
>
> **为什么卡在这一步而不是 M2 之前**:一旦用被污染的 teacher 生成了上万条合成 QA,
> 要扔掉的是那批数据**加上**重训的时间。而核许可是零成本的桌面工作。
> **让猜错的代价小 —— 这里的做法就是把检查放在生成之前。**
>
> **生成时同步记录**(写进合成数据文件的头部或同名 manifest):
> teacher 的 `model_id` / `digest` / `license_claim` / 生成日期。
> 没有这条记录,某个 teacher 出问题时,"哪些数据受影响"的答案只能是"全部"。
```

> **在哪**：Kiln 桌面 app + Terminal
> **到这一步才装 Kiln。** 前面用不到它（原因见 S8 开头）。

```bash
# 从 kiln.tech 下载安装
# 装完做两件事：
#   1. 关闭更新检查与遥测（Settings）
#   2. 断网验证：拔网线后仍能问答（M0 的一票否决项，需要复测）
```

**Kiln 在本项目的职责已缩小为两项**：
- C/D 臂的合成数据生成（从 3309 个 chunk 生成问答对，**必须含 ≥20% 的"上下文里没答案 → 正确拒答"负样本**）
- LoRA 微调（**必须走本地 MLX-LM**。Kiln 默认主推云端微调，那意味着公司文档出境，违反 A5）

**接 MLX 后端**：

```bash
mlx_lm.server --model <repo> --port 8080
# Kiln → Settings → AI Providers → 添加 OpenAI-Compatible
#   base URL: http://localhost:8080/v1
#   API key:  local
```

- [ ] **本步应入库的条目**：`[L3] Kiln 配置` + `[L1] Kiln 职责边界`（记录它做不了检索基线的实测/文档依据）

---

# S12 · 跑 M2 四臂 + 盲评

> **在哪**：`shiprag && claude` 跑实验；判分你自己做

### S12.1 跑的纪律

```
A（裸）→ 结果落 eval/results.csv → B（仅RAG）→ C（仅微调）→ D（RAG+微调）
```

**绝对不要一边跑一边改 prompt 或参数。** 提示词全文在预注册附录里，跑的时候只读不写。

### S12.2 盲评

```bash
# 脚本把 39×4=156 条答案打乱、隐去臂标签
python3 eval/make_blind_sheet.py \
    --results eval/results.csv --exp-id M2 \
    --out-sheet eval/M2_blind_sheet.csv \
    --out-key   eval/M2_blind_key.csv
```

**判分前不看 key。** 在 blind sheet 上填 Correct / Partial / Incorrect + 一行理由。
判完再用 key 还原臂标签。**两份文件都进 Git，时间戳可查。**

### S12.3 ✅ S12 验收清单

- [ ] `eval/results.csv` 有 **39 × 4 = 156 行**（不是 4 行）
- [ ] 每行带完整指纹：`testset_sha256` / `corpus_chunks_sha256` / `model_digest` / `teacher_digest` / `lora_adapter_sha256` / `prompt_config_sha256` / `code_commit` / `seed`
- [ ] `is_blind_scored` 全为 true
- [ ] **隔 ≥24 小时复判 20 条，一致率 ≥ 90%**。低于 90% → 判分标准太模糊，收紧规则重判
- [ ] McNemar 算得出来（有逐题对错矩阵）
- [ ] A/C 臂的 `citations_correct` 是 `None` 不是 `False`
- [ ] 每道错题贴了归因标签（十个之一）
- [ ] **本步应入库的条目**：M2 全部结果 + 三个假设的判定 + 一致率

---

## 附录 A · `TODAY.md`（每天开工前 2 分钟）

```bash
cat > ~/ship-rag/TODAY.md <<'EOF'
# TODAY · 2026-XX-XX

⚠️ 本文件是执行清单，不是权威记录。任何决定当天写进 DECISIONS.md。
   本文件每天覆写，不保留历史。

当前步骤：S?
目标：
阻塞：
Done：
Next：
EOF
```

**为什么要那条 ⚠️**：`TODAY.md` 每天覆写，`DECISIONS.md` 是"只追加"的日志类。
不声明的话你会有两个真相来源，而覆写的那个看起来更新。

## 附录 B · 每完成一步回本对话，按这五问报

| # | 问题 | 为什么 |
|---|---|---|
| 1 | **这一步到底回答了什么？** | 不是"跑了什么"。跑完不等于回答了问题 |
| 2 | **有没有新的混杂变量？** | prompt 变了？模型变了？检索器变了？必须第一时间抓住 |
| 3 | **有没有新的 Decision？** | → `DECISIONS.md`，当天入库，带 L1/L2/L3 |
| 4 | **有没有新的 Contract？** | → `contracts.py`，走变更仪式 |
| 5 | **能不能进下一步？** | 判据是**验收清单全绿**，不是"代码写完" |
**第 6 问（落笔检查）**：
本步有没有出现「把观察写成一个字段值或一条规则」的动作？
若有 —— **我观察到的那个量，和我要填的这个字段，是同一个东西吗？**

## 附录 C · 关键路径

    S3 contracts v0.2.0                                    ✅ CLOSED
      ↓
    S4a readiness gate                                     ✅ CLOSED
      ↓
    S5 parser / corpus baseline                            ✅ 可复现
      │   corpus = 8c6bee0a… / 3383 / commit 545947a
      ↓
    S5b parser / OCR / citation diagnostics                ✅ CLOSED
      ↓
    Phase B Design Freeze
      ↓
    S5c selective OCR + citation-safe ingestion            ⬛ 当前主线
      ↓
    new corpus deterministic rebuild
      ↓
    S4a.9c-final / corpus 相关预算证据重新验证
      ↓
    必要时独立 contract commit
      ↓
    S6 GoldChunkMap
      ↓
    S8 M1c → S9 → S10 → S11 → S12

**并行（不阻塞）**：

    S4b candidate-space survey        🟡
    class-3 content augmentation      residual risk / M5
    V2 / active-row 生产化            不做；diagnostic only

## 附录 D · 文档换版前的陈旧事实自检

每次手册或方案换版前，在两份文档上 grep 以下串，逐一确认仍然成立：

    3309            v5.2            无需 OCR
    1050            765             0.90
    k=2             S5 PASS         S6 baseline
    verified_structural             Metal 未完全禁用
    section 元数据覆盖率 100%

> 命中即需核对：它是当前事实，还是上一版遗留。
> **文档正文只呈现当前状态**；变更理由属于 commit message 与 `DECISIONS.md`。