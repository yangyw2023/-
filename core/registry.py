"""注册表：把"组件名字"映射到"实现类"，让替换组件等价于改一行配置。

存在的理由是 C 类认知约束——不知道最终用哪个模型／解析器／检索器，
所以选择必须留在配置里，而不是硬编码在调用点。

当前为占位 stub，尚未实现。
"""


def resolve():
    """按名字查出对应的组件实现类并返回。

    契约（待 core/contracts.py 定稿后固化）：
        - 参数签名、返回类型均未定，取决于 contracts.py 里的组件接口定义。
        - 语义要求（已确定，实现时必须满足）：名字查不到必须抛异常，
          不得回退到默认实现——"配置写错了"和"用了默认值"必须可区分。

    Raises:
        NotImplementedError: 无条件抛出。本函数目前没有任何实现。
    """
    raise NotImplementedError("core.registry.resolve 尚未实现")
