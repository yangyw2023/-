# 占位文件 —— 内容由 Arya 手动贴入，请勿由工具或 AI 生成。
#
# 本文件是全项目唯一可信来源（数据结构 + 阈值 + 接口 + 九条语义约定）。
# 在真实内容贴入之前，任何 `from core.contracts import X` 都会以 ImportError 失败，
# 这是预期行为：宁可导入时炸掉，也不要让组件拿到伪造的契约继续往下跑。
#
# 改动本文件必须走契约变更仪式，见 DECISIONS.md。
