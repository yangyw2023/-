# M2 预注册（提交后不许改）

实验编号: M2
日期:
主模型 + digest:
Teacher 模型 + digest:
评测集版本 + sha256:
排除项: TR02（SMM 缺页）
主指标: 答案正确率 @ 31 道可答题

## 假设与判定阈值（跑之前锁定）

> ⚠️ 该规则待修正 —— 净差判据不适用于配对数据，应改用 McNemar 精确
> 检验；见 DECISIONS.md 2026-09-07 补充 2。填写前必须先完成该条决策。

| 编号 | 假设 | 接受 | 拒绝 |
|---|---|---|---|
| H1 | 检索带来可测量提升 | B−A ≥ 19pp | B−A < 10pp |
| H2 | 微调带来可测量提升 | C−A ≥ 19pp | C−A < 10pp |
| H3 | 两者可叠加 | D−max(B,C) ≥ 19pp | < 10pp |
（10–19pp 一律记为"提示性，不下结论"。阈值依据：n=31 时 6 题≈19pp
 才是配对 McNemar 检验意义上可靠的差异。）

## 判分协议（跑之前锁定）

| 项 | 内容 |
|---|---|
| 二元判定规则 | 待填 |
| 命中判定方式 | 待填 |
| judge 完整规格（模型 + digest + prompt + temperature + seed） | 待填 |
| judge 家族约束 | 待填 |
| 人机一致性抽检 | 待填 |

## 本实验不回答的问题
- 模型横评（M3）
- 检索参数调优（M5）

## 预期最可能结果（先写下来，事后对照，这一条防自欺）
